/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
/******/ (() => { // webpackBootstrap
/******/ 	"use strict";
/******/ 	var __webpack_modules__ = ({

/***/ "./src/app.js":
/*!********************!*\
  !*** ./src/app.js ***!
  \********************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (__WEBPACK_DEFAULT_EXPORT__)\n/* harmony export */ });\n/* harmony import */ var building_blocks__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! building-blocks */ \"./node_modules/building-blocks/index.js\");\n/* harmony import */ var _js_state_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./js/state.js */ \"./src/js/state.js\");\n/* harmony import */ var _js_midi_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./js/midi.js */ \"./src/js/midi.js\");\n/* harmony import */ var _js_effects_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./js/effects.js */ \"./src/js/effects.js\");\n/* harmony import */ var _js_effect_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./js/effect.js */ \"./src/js/effect.js\");\n/* harmony import */ var _js_script_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./js/script.js */ \"./src/js/script.js\");\n/* harmony import */ var _js_storage_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./js/storage.js */ \"./src/js/storage.js\");\n/* harmony import */ var _js_utils_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./js/utils.js */ \"./src/js/utils.js\");\n\n\n\n\n\n\n\n\nclass App extends building_blocks__WEBPACK_IMPORTED_MODULE_0__.ContextBlocks {\n  constructor() {\n    super({\n      id: (0,_js_utils_js__WEBPACK_IMPORTED_MODULE_7__.gen_id)(),\n      name: \"New Project\",\n      saved: false,\n      state: new _js_state_js__WEBPACK_IMPORTED_MODULE_1__.State(),\n      midi: new _js_midi_js__WEBPACK_IMPORTED_MODULE_2__.MIDI()\n    });\n    this.screen = null;\n  }\n  updateSelected(type, idx) {\n    this.state.selected = {\n      ...this.state.selected,\n      [type]: idx\n    };\n  }\n  loadVideo(idx, file, fileHandle) {\n    this.state.loading = idx;\n    this.state.files = this.state.files.toSpliced(idx, 1, file);\n    this.state.videos[idx].handle = fileHandle;\n    this.state.videos[idx].loaded = true;\n  }\n  updateVideoMedia(idx, source) {\n    this.state.files = this.state.files.toSpliced(idx, 1, source);\n    this.state.videos[idx].loaded = true;\n  }\n  removeVideo(idx) {\n    this.state.loading = idx;\n    this.state.files[idx] = null;\n    this.state.files = [...this.state.files];\n    this.state.videos[idx].loaded = false;\n  }\n  updateOpacity(idx, value) {\n    let {\n      shapes,\n      videos\n    } = this.state;\n    videos[idx].opacity = value;\n    for (let i = 0; i < shapes.length; i++) {\n      shapes[i].opacity[idx] = value;\n      shapes[i].opacity = [...shapes[i].opacity];\n    }\n  }\n\n  /**\n   * Sets the X or Y value for the selected effect/script/video\n   * @param {Number} idx   0 for X, 1 for Y\n   * @param {Float} value \tMust be 0-1\n   */\n  setValues(idx, value) {\n    let {\n      video,\n      slot\n    } = this.state?.selected;\n    if (slot === null || video === null) {\n      return;\n    }\n    let {\n      target\n    } = this.state.slots[slot];\n    if (target instanceof _js_effect_js__WEBPACK_IMPORTED_MODULE_4__.Effect) {\n      target.values[video][idx] = value;\n    }\n    if (target instanceof _js_script_js__WEBPACK_IMPORTED_MODULE_5__.Script) {\n      target.values[idx] = value;\n    }\n    target.values = [...target.values];\n  }\n\n  /**\n   * Sets all effects on this video to 0\n   * @param  {Number} idx [description]\n   */\n  clearValues(idx) {\n    this.state.effects.map(e => {\n      e.values[idx][0] = 0;\n      e.values[idx][1] = 0;\n      e.values = [...e.values];\n    });\n  }\n  setSlot(idx, effect) {\n    let slots = this.state.slots;\n\n    // slot.effect = effect;\n    if (effect === null) {\n      slots[idx].target = null;\n    } else if (effect === \"__script\") {\n      slots[idx].target = this.state.scripts[idx];\n      slots[idx].target.values = [0, 0];\n    } else {\n      slots[idx].target = this.state.effects.find(e => e.id === effect) || null;\n      slots[idx].target.values = new Array(6).fill(null).map(v => [0, 0]);\n    }\n    this.state.slots = [...this.state.slots];\n  }\n  updateSlot(idx, updates) {\n    let slot = this.state.slots[idx];\n    let target = slot.target;\n    if (!target) {\n      return;\n    }\n    for (let x in updates) {\n      target[x] = updates[x];\n    }\n    this.state.slots = [...this.state.slots];\n  }\n  async saveProject() {\n    let existing = await _js_storage_js__WEBPACK_IMPORTED_MODULE_6__.storage.getItem(\"projects\", this.id);\n\n    // If existing open the confirmation dialog\n\n    let project = {\n      id: this.id,\n      name: this.name,\n      shapes: this.state.shapes.map(s => {\n        return JSON.parse(JSON.stringify(s));\n      }),\n      videos: this.state.videos.map(v => {\n        return {\n          id: v.id,\n          handle: v.handle,\n          label: v.label,\n          opacity: v.opacity,\n          code: v.code\n        };\n      }),\n      effects: this.state.effects.map(e => {\n        return JSON.parse(JSON.stringify(e));\n      }),\n      scripts: this.state.scripts.map(s => {\n        return JSON.parse(JSON.stringify(s));\n      }),\n      slots: this.state.slots.map(s => {\n        return {\n          target_type: s?.target?.type || null,\n          target_id: s?.target?.id || null\n        };\n      })\n    };\n    _js_storage_js__WEBPACK_IMPORTED_MODULE_6__.storage.upsertItem(\"projects\", project);\n    this.saved = true;\n  }\n  async loadProject(project) {\n    if (!project) {\n      return;\n    }\n    this.id = project.id;\n    this.name = project.name;\n    this.state.shapes = [];\n    project.shapes.map((shape, idx) => {\n      let s = app.state.addShape(shape.type);\n      s.id = shape.id;\n      s.label = shape.label;\n      s.opacity = shape.opacity;\n      s.tris = shape.tris;\n    });\n    project.videos.map(async (video, idx) => {\n      let v = this.state.videos[idx];\n      let file = null;\n      if (video.handle !== null) {\n        if ((await video.handle.queryPermission()) === \"granted\") {\n          file = await video.handle.getFile();\n        }\n        if ((await video.handle.requestPermission()) === \"granted\") {\n          file = await video.handle.getFile();\n        }\n      }\n      v.id = video.id;\n      v.handle = null;\n      v.opacity = 0;\n      v.code = video.code;\n      this.updateOpacity(idx, video.opacity);\n      if (file !== null) {\n        this.loadVideo(idx, file, video.handle);\n      }\n    });\n    project.scripts.map((script, idx) => {\n      let s = this.state.scripts[idx];\n      s.id = script.id;\n      s.label = script.label;\n      s.disabled = script.disabled;\n      s.values = script.values;\n      s.code = script.code;\n      code: script.code;\n    });\n    project.effects.map((effect, idx) => {\n      let e = this.state.effects[idx];\n      e.id = effect.id;\n      e.label = effect.label;\n      e.values = effect.values;\n      e.code = effect.code;\n    });\n    project.slots.map((slot, idx) => {\n      let s = this.state.slots[idx];\n      s.target = null;\n      if (slot.target_type === \"effect\") {\n        s.target = this.state.effects.find(e => e.id === slot.target_id);\n      }\n      if (slot.target_type === \"script\") {\n        s.target = this.state.scripts.find(s => s.id === slot.target_id);\n      }\n    });\n    this.saved = true;\n  }\n  async deleteProject(project) {\n    _js_storage_js__WEBPACK_IMPORTED_MODULE_6__.storage.removeItem(\"projects\", project);\n  }\n}\nlet app = new App();\n/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (app);\n\n//# sourceURL=webpack://sensorycontrols/./src/app.js?");

/***/ }),

/***/ "./src/js/effect.js":
/*!**************************!*\
  !*** ./src/js/effect.js ***!
  \**************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   Effect: () => (/* binding */ Effect)\n/* harmony export */ });\n/* harmony import */ var building_blocks__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! building-blocks */ \"./node_modules/building-blocks/index.js\");\n\nclass Effect extends building_blocks__WEBPACK_IMPORTED_MODULE_0__.ContextBlocks {\n  constructor(effect, defaults) {\n    super({\n      id: effect.id,\n      type: \"effect\",\n      label: defaults?.label || effect.label,\n      values: defaults?.values || new Array(6).fill(null).map(v => [0, 0]),\n      code: defaults?.code || ``\n    });\n  }\n}\n\n//# sourceURL=webpack://sensorycontrols/./src/js/effect.js?");

/***/ }),

/***/ "./src/js/effects.js":
/*!***************************!*\
  !*** ./src/js/effects.js ***!
  \***************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   Effects: () => (/* binding */ Effects)\n/* harmony export */ });\nconst Effects = [{\n  id: \"color_opacity\",\n  label: \"Chroma Key\",\n  effect_x: \"Hue\",\n  effect_y: \"Sensitivity\",\n  defaults: [0, 0],\n  shader: `\n\t    precision mediump float;\n\t    varying vec2 v_texcoord;\n\t    uniform sampler2D u_texture;\n\n\t    float PI = 3.14159265358;\n\n\t    uniform vec2 u_dimensions; \n\t    uniform mediump float u_opacity;\n\t    uniform vec2 u_effect;\n\n\t    vec3 rgb2hsv(vec3 c)\n\t    {\n\t      vec4 K = vec4(0.0, -1.0 / 3.0, 2.0 / 3.0, -1.0);\n\t      vec4 p = mix(vec4(c.bg, K.wz), vec4(c.gb, K.xy), step(c.b, c.g));\n\t      vec4 q = mix(vec4(p.xyw, c.r), vec4(c.r, p.yzx), step(p.x, c.r));\n\n\t      float d = q.x - min(q.w, q.y);\n\t      float e = 1.0e-10;\n\t      return vec3(abs(q.z + (q.w - q.y) / (6.0 * d + e)), d / (q.x + e), q.x);\n\t    }\n\n\t    void main() {\n\t      vec4 color = texture2D(u_texture, v_texcoord);\n\t      vec3 hsv = rgb2hsv(vec3(color[0], color[1], color[2]));\n\n\t      float hue_target = u_effect[0];\n\t      float hue_dist = min(abs(hsv[0]-hue_target), 1.0 - abs(hsv[0]-hue_target));\n\t\t\t\tfloat hue_opacity = min(cos(hue_dist - PI) * 300.0 + 300.0, 1.0) + (1.0 - u_effect[1]);\n\n\t      gl_FragColor = vec4(color[0], color[1], color[2], hue_opacity * color[3]);\n\t    }\n\t  `\n}, {\n  id: \"brightness_opacity\",\n  label: \"Luma Key\",\n  effect_x: \"Brightness\",\n  effect_y: \"Sensitivity\",\n  defaults: [0, 0],\n  shader: `\n\t    precision mediump float;\n\t    varying vec2 v_texcoord;\n\t    uniform sampler2D u_texture;\n\n\t    float PI = 3.14159265358;\n\n\t    uniform vec2 u_dimensions; \n\t    uniform mediump float u_opacity;\n\t    uniform vec2 u_effect;\n\n\t    vec3 rgb2hsl( in vec3 c ){\n\t\t\t  float h = 0.0;\n\t\t\t\tfloat s = 0.0;\n\t\t\t\tfloat l = 0.0;\n\t\t\t\tfloat r = c.r;\n\t\t\t\tfloat g = c.g;\n\t\t\t\tfloat b = c.b;\n\t\t\t\tfloat cMin = min( r, min( g, b ) );\n\t\t\t\tfloat cMax = max( r, max( g, b ) );\n\n\t\t\t\tl = ( cMax + cMin ) / 2.0;\n\t\t\t\tif ( cMax > cMin ) {\n\t\t\t\t\tfloat cDelta = cMax - cMin;\n\t\t\t        \n\t\t\t        //s = l < .05 ? cDelta / ( cMax + cMin ) : cDelta / ( 2.0 - ( cMax + cMin ) ); Original\n\t\t\t\t\ts = l < .0 ? cDelta / ( cMax + cMin ) : cDelta / ( 2.0 - ( cMax + cMin ) );\n\t\t\t        \n\t\t\t\t\tif ( r == cMax ) {\n\t\t\t\t\t\th = ( g - b ) / cDelta;\n\t\t\t\t\t} else if ( g == cMax ) {\n\t\t\t\t\t\th = 2.0 + ( b - r ) / cDelta;\n\t\t\t\t\t} else {\n\t\t\t\t\t\th = 4.0 + ( r - g ) / cDelta;\n\t\t\t\t\t}\n\n\t\t\t\t\tif ( h < 0.0) {\n\t\t\t\t\t\th += 6.0;\n\t\t\t\t\t}\n\t\t\t\t\th = h / 6.0;\n\t\t\t\t}\n\t\t\t\treturn vec3( h, s, l );\n\t\t\t}\n\n\t    void main() {\n\t      vec4 color = texture2D(u_texture, v_texcoord);\n\t      vec3 hsl = rgb2hsl(vec3(color[0], color[1], color[2]));\n\n\t      \n\t\t\t\tfloat brightness_target = u_effect[0];\n\t      float brightness_dist = abs(hsl[2]-brightness_target);\n\t\t\t\tfloat brightness_opacity = min(cos(brightness_dist - PI) * 300.0 + 300.0, 1.0) + (1.0 - u_effect[1]);\n\n\t      gl_FragColor = vec4(color[0], color[1], color[2], brightness_opacity * color[3]);\n\t    }\n\t  `\n}, {\n  id: \"cosine_palette\",\n  label: \"Cosine Palette\",\n  effect_x: \"Intensity\",\n  effect_y: \"Shift\",\n  defaults: [0, 0],\n  shader: `\n\t    #ifdef GL_ES\n    \t\tprecision mediump float;\n  \t\t#endif\n\t    \n\t    varying vec2 v_texcoord;\n\t    uniform sampler2D u_texture;\n\n\t    uniform vec2 u_dimensions; \n\t    uniform float u_opacity;\n\t    uniform vec2 u_effect;\n\n\t    vec3 pal( in float t, in vec3 a, in vec3 b, in vec3 c, in vec3 d ) \n\t    {\n\t      return a + b*cos( 6.28318*(c*t+d) );\n\t    }\n\t    \n\t    vec3 rgb2hsv(vec3 c)\n\t    {\n\t      vec4 K = vec4(0.0, -1.0 / 3.0, 2.0 / 3.0, -1.0);\n\t      vec4 p = mix(vec4(c.bg, K.wz), vec4(c.gb, K.xy), step(c.b, c.g));\n\t      vec4 q = mix(vec4(p.xyw, c.r), vec4(c.r, p.yzx), step(p.x, c.r));\n\n\t      float d = q.x - min(q.w, q.y);\n\t      float e = 1.0e-10;\n\t      return vec3(abs(q.z + (q.w - q.y) / (6.0 * d + e)), d / (q.x + e), q.x);\n\t    }\n\n\t    void main() {\n\t      vec4 color = texture2D(u_texture, v_texcoord);\n\t      vec3 hsv = rgb2hsv(vec3(color[0], color[1], color[2]));\n\t      vec3 effect = pal(hsv[2] + u_effect[1], vec3(0.5,0.5,0.5),vec3(0.5,0.5,0.5),vec3(1.0,1.0,1.0),vec3(0.0,0.33,0.67) );\n\t      vec3 weighted = vec3(\n\t        color[0] * (1.0 - u_effect[0]) + effect[0] * u_effect[0],\n\t        color[1] * (1.0 - u_effect[0]) + effect[1] * u_effect[0],\n\t        color[2] * (1.0 - u_effect[0]) + effect[2] * u_effect[0]\n\t      );\n\n\t      gl_FragColor = vec4(weighted, color[3]);\n\t    }\n\t  `\n}, {\n  id: \"pixelate\",\n  label: \"Pixelate\",\n  effect_x: \"Pixel Size\",\n  effect_y: \"Pallete Depth\",\n  defaults: [0, 0],\n  shader: `\n\t\t\t#ifdef GL_ES\n    \t\tprecision mediump float;\n  \t\t#endif\n\n\t\t  varying vec2 v_texcoord;\n\t\t  uniform sampler2D u_texture;\n\n\t\t  uniform vec2 u_dimensions; \n\t\t  uniform float u_opacity;\n\t\t  uniform vec2 u_effect;\n\n\t\t  void main() {\n\t\t    float pixelateX = u_dimensions[0] * floor(max(u_effect[0] * 30.0, 1.0));\n\t\t    float pixelateY = u_dimensions[1] * floor(max(u_effect[1] * 30.0, 1.0));\n\t\t    vec2 pixel_coords = vec2(\n\t\t      v_texcoord[0] - (v_texcoord[0] - floor(v_texcoord[0]/pixelateX) * pixelateX),\n\t\t      v_texcoord[1] - (v_texcoord[1] - floor(v_texcoord[1]/pixelateY) * pixelateY)\n\t\t    );\n\n\t\t    gl_FragColor = texture2D(u_texture, pixel_coords);\n\t\t  }\n\t\t`\n}, {\n  id: \"prism\",\n  label: \"Prism\",\n  effect_x: \"Horizontal\",\n  effect_y: \"Vertical\",\n  defaults: [0, 0],\n  shader: `\n\t\t\tprecision mediump float;\n\n\t\t  varying vec2 v_texcoord;\n\t\t  uniform sampler2D u_texture;\n\n\t\t  uniform vec2 u_dimensions; \n\t\t  uniform float u_opacity;\n\t\t  uniform vec2 u_effect;\n\n\t\t  void main() {\n\t\t    vec2 prism_values = vec2(floor(u_effect[0] * 9.0) + 1.0, floor(u_effect[1] * 9.0) + 1.0);\n\t\t    vec2 prism_coords = vec2(fract(v_texcoord[0] * prism_values[0]), fract(v_texcoord[1] * prism_values[1]));\n\n\t\t    gl_FragColor = texture2D(u_texture, prism_coords);\n\t\t  }\n\t\t`\n}, {\n  id: \"kaleidoscope\",\n  label: \"Kaleidoscope\",\n  effect_x: \"Horizontal\",\n  effect_y: \"Vertical\",\n  defaults: [0, 0],\n  shader: `\n\t\t\tprecision mediump float;\n\t\t\t\n\t\t  varying vec2 v_texcoord;\n\t\t  uniform sampler2D u_texture;\n\n\t\t  uniform vec2 u_dimensions; \n\t\t  uniform float u_opacity;\n\t\t  uniform vec2 u_effect;\n\n\t\t  void main() {\n\t\t    vec2 prism_values = vec2(floor(u_effect[0] * 9.0) + 1.0, floor(u_effect[1] * 9.0) + 1.0);\n\n\t\t\t\tfloat xFlip = -1.0;\n\t\t\t\tif (mod(floor(v_texcoord[0] * prism_values[0]), 2.0) == 0.0) {\n\t\t\t\t\txFlip = 1.0;\n\t\t\t\t}\n\n\t\t\t\tfloat yFlip = -1.0;\n\t\t\t\tif (mod(floor(v_texcoord[1] * prism_values[1]), 2.0) == 0.0) {\n\t\t\t\t\tyFlip = 1.0;\n\t\t\t\t}\n\n\t\t    vec2 prism_coords = vec2(fract(v_texcoord[0] * prism_values[0] * xFlip), fract(v_texcoord[1] * prism_values[1] * yFlip));\n\n\t\t    gl_FragColor = texture2D(u_texture, prism_coords);\n\t\t  }\n\t\t`\n}, {\n  id: \"zoom\",\n  label: \"Zoom\",\n  effect_x: \"X-Zoom\",\n  effect_y: \"Y-Zoom\",\n  defaults: [0, 0],\n  shader: `\n\t\t\t#ifdef GL_ES\n    \t\tprecision mediump float;\n  \t\t#endif\n\n\t\t  varying vec2 v_texcoord;\n\t\t  uniform sampler2D u_texture;\n\n\t\t  uniform vec2 u_dimensions; \n\t\t  uniform float u_opacity;\n\t\t  uniform vec2 u_effect;\n\n\t\t  void main() {\n\t\t  \tfloat x_intensity = (1.0 - u_effect[0]);\n\t\t  \tfloat y_intensity = (1.0 - u_effect[1]);\n\n\n\t\t    vec2 prism_coords = vec2(\n\t\t    \t(v_texcoord[0] * x_intensity) + (u_effect[0] / 2.0), \n\t\t    \t(v_texcoord[1] * y_intensity) + (u_effect[1] / 2.0)\n\t\t    );\n\n\t\t    gl_FragColor = texture2D(u_texture, prism_coords);\n\t\t  }\n\t\t`\n}, {\n  id: \"shift\",\n  label: \"Shift\",\n  effect_x: \"X-Shift\",\n  effect_y: \"Y-Shift\",\n  defaults: [0.0, 0.0],\n  shader: `\n\t\t\t#ifdef GL_ES\n    \t\tprecision mediump float;\n  \t\t#endif\n  \t\t\n\t\t  varying vec2 v_texcoord;\n\t\t  uniform sampler2D u_texture;\n\n\t\t  uniform vec2 u_dimensions; \n\t\t  uniform float u_opacity;\n\t\t  uniform vec2 u_effect;\n\n\t\t  void main() {\n\t\t    vec2 shift_coords = vec2(\n\t\t    \tv_texcoord[0] - u_effect[0], \n\t\t    \tv_texcoord[1] - u_effect[1]\n\t\t    );\n\n\t\t\t\t// if (shift_coords[0] > 1.0) {\n\t\t\t\t// \tshift_coords[0] = shift_coords[0] - 1.0;\n\t\t\t\t// }\n\n\t\t\t\t// if (shift_coords[1] > 1.0) {\n\t\t\t\t// \tshift_coords[1] = shift_coords[1] - 1.0;\n\t\t\t\t// }\n\t\t\t\t\n\t\t\t\tif (shift_coords[0] < 0.0) {\n\t\t\t\t\tshift_coords[0] = 1.0 + shift_coords[0];\n\t\t\t\t}\n\n\t\t\t\tif (shift_coords[1] < 0.0) {\n\t\t\t\t\tshift_coords[1] = 1.0 + shift_coords[1];\n\t\t\t\t}\n\n\t\t    gl_FragColor = texture2D(u_texture, shift_coords);\n\t\t  }\n\t\t`\n}, {\n  id: \"color_adjust\",\n  label: \"Color Adjust\",\n  effect_x: \"Desaturate\",\n  effect_y: \"Bit Depth\",\n  defaults: [0, 0],\n  shader: `\n\t\t\tprecision mediump float;\n\t\t  varying vec2 v_texcoord;\n\t\t  uniform sampler2D u_texture;\n\n\t\t  uniform vec2 u_dimensions; \n\t\t  uniform mediump float u_opacity;\n\t\t  uniform vec2 u_effect;\n\n\t\t  vec3 rgb2hsl( in vec3 c ){\n\t\t\t  float h = 0.0;\n\t\t\t\tfloat s = 0.0;\n\t\t\t\tfloat l = 0.0;\n\t\t\t\tfloat r = c.r;\n\t\t\t\tfloat g = c.g;\n\t\t\t\tfloat b = c.b;\n\t\t\t\tfloat cMin = min( r, min( g, b ) );\n\t\t\t\tfloat cMax = max( r, max( g, b ) );\n\n\t\t\t\tl = ( cMax + cMin ) / 2.0;\n\t\t\t\tif ( cMax > cMin ) {\n\t\t\t\t\tfloat cDelta = cMax - cMin;\n\t\t\t        \n\t\t\t\t\ts = l < .0 ? cDelta / ( cMax + cMin ) : cDelta / ( 2.0 - ( cMax + cMin ) );\n\t\t\t        \n\t\t\t\t\tif ( r == cMax ) {\n\t\t\t\t\t\th = ( g - b ) / cDelta;\n\t\t\t\t\t} else if ( g == cMax ) {\n\t\t\t\t\t\th = 2.0 + ( b - r ) / cDelta;\n\t\t\t\t\t} else {\n\t\t\t\t\t\th = 4.0 + ( r - g ) / cDelta;\n\t\t\t\t\t}\n\n\t\t\t\t\tif ( h < 0.0) {\n\t\t\t\t\t\th += 6.0;\n\t\t\t\t\t}\n\t\t\t\t\th = h / 6.0;\n\t\t\t\t}\n\t\t\t\treturn vec3( h, s, l );\n\t\t\t}\n\n\t\t\tfloat hue2rgb(float p, float q, float t) {\n\t\t\t  if (t < 0.0) {\n\t\t\t    t += 1.0;\n\t\t\t  }\n\t\t\t  if (t > 1.0) {\n\t\t\t    t -= 1.0;\n\t\t\t  }\n\t\t\t  if (t < 1.0 / 6.0) {\n\t\t\t    return p + (q - p) * 6.0 * t;\n\t\t\t  }\n\t\t\t  if (t < 1.0 / 2.0) {\n\t\t\t    return q;\n\t\t\t  }\n\t\t\t  if (t < 2.0 / 3.0) {\n\t\t\t    return p + (q - p) * (2.0 / 3.0 - t) * 6.0;\n\t\t\t  }\n\n\t\t\t  return p;\n\t\t\t}\n\n\t\t\tvec3 hsl2rgb( vec3 c){\n\t\t\t\tfloat r = 0.0;\n\t\t\t\tfloat g = 0.0;\n\t\t\t\tfloat b = 0.0;\n\n\t\t\t\tif (c[1] == 0.0) {\n\t\t\t\t\tr = g = b = c[2];\n\t\t\t\t} else {\n\t\t\t\t\tfloat q = c[2] < 0.5 ? c[2] * (1.0 + c[1]) : c[2] + c[1] - c[2] * c[1];\n\t\t\t\t\tfloat p = 2.0 * c[2] - q;\n\n\t\t\t\t\tr = hue2rgb(p, q, c[0] + 1.0 / 3.0);\n\t\t\t\t\tg = hue2rgb(p, q, c[0]);\n\t\t\t\t\tb = hue2rgb(p, q, c[0] - 1.0 / 3.0);\n\t\t\t\t}\n\n\t\t\t\treturn vec3(r, g, b);\n\t\t\t}\n\n\t\t\tfloat rShift(float n, float s) {\n\t\t\t\treturn float(floor(n / pow(2.0, s))); \n\t\t\t}\n\n\t\t\tfloat lShift(float n, float s) {\n\t\t\t\treturn float(floor(n * pow(2.0, s)));\n\t\t\t}\n\n\t\t\tvec3 bitReduction(vec3 c, float s) {\n\t\t\t\tfloat r = rShift(c[0] * 255.0, s) / rShift(255.0, s);\n\t\t\t\tfloat g = rShift(c[1] * 255.0, s) / rShift(255.0, s);\n\t\t\t\tfloat b = rShift(c[2] * 255.0, s) / rShift(255.0, s);\n\n\t\t\t\treturn vec3(r, g, b);\n\t\t\t}\n\n\t    void main() {\n\t      vec4 color = texture2D(u_texture, v_texcoord);\n\t      vec3 hsl = rgb2hsl(vec3(color[0], color[1], color[2]));\n\t\t\t\tvec3 desaturated = hsl2rgb(vec3(hsl[0], hsl[1] * (1.0 - u_effect[0]), hsl[2]));\n\n\t\t\t\tfloat reduction = floor(u_effect[1] * 7.0);\n\t\t\t\tvec3 reduced = bitReduction(desaturated, reduction);\n\n\t\t    gl_FragColor = vec4(reduced[0], reduced[1], reduced[2], color[3]);\n\t\t  }\n\t\t`\n}];\n\n//# sourceURL=webpack://sensorycontrols/./src/js/effects.js?");

/***/ }),

/***/ "./src/js/midi.js":
/*!************************!*\
  !*** ./src/js/midi.js ***!
  \************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   MIDI: () => (/* binding */ MIDI)\n/* harmony export */ });\n/* harmony import */ var building_blocks__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! building-blocks */ \"./node_modules/building-blocks/index.js\");\n\nclass MIDI extends building_blocks__WEBPACK_IMPORTED_MODULE_0__.ContextBlocks {\n  constructor() {\n    super({\n      notes: {\n        buttons: {\n          48: 0,\n          53: 1,\n          50: 2,\n          51: 3,\n          49: 4,\n          52: 5\n        },\n        knobs: {\n          43: 0,\n          44: 1\n        },\n        sliders: {\n          input: {\n            56: 2,\n            54: 1,\n            55: 0\n          },\n          output: {\n            45: 0,\n            46: 1,\n            47: 2\n          }\n        }\n      }\n    });\n  }\n}\n\n//# sourceURL=webpack://sensorycontrols/./src/js/midi.js?");

/***/ }),

/***/ "./src/js/screen-ui.js":
/*!*****************************!*\
  !*** ./src/js/screen-ui.js ***!
  \*****************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   UI: () => (/* binding */ UI)\n/* harmony export */ });\nclass UI {\n  constructor(state) {\n    this.state = state;\n    this.pointSize = 10;\n    this.layer = \"output\";\n    this.layerColor = {\n      input: [255, 0, 0],\n      output: [0, 0, 255]\n    };\n    this.moving = false;\n    this.shifted = false;\n    this.selected = null;\n    this.selectedShapes = [];\n    this.elements = {\n      fps: document.querySelector(\"#fps\"),\n      ctx: document.querySelector(\"#ui\").getContext(\"2d\")\n    };\n    this.opacity = {\n      value: 0,\n      timer: null\n    };\n  }\n  resetOpacity() {\n    let {\n      opacity\n    } = this;\n    opacity.value = 1;\n    if (opacity.timer !== null) {\n      clearTimeout(opacity.timer);\n    }\n    let opacityFn = () => {\n      opacity.value -= 0.02;\n      if (opacity.value > 0) {\n        opacity.timer = setTimeout(opacityFn, 30);\n      } else {\n        opacity.value = 0;\n        opacity.timer = null;\n      }\n      this.drawUI();\n    };\n    opacity.timer = setTimeout(opacityFn, 5000);\n    this.drawUI();\n  }\n  updateState(state) {\n    this.state = state;\n  }\n  setLayer(layer) {\n    this.layer = layer;\n  }\n  startMove() {\n    this.moving = true;\n  }\n  stopMove() {\n    this.moving = false;\n  }\n  shiftDown() {\n    this.shifted = true;\n  }\n  shiftUp() {\n    this.shifted = false;\n  }\n  handleLeft() {\n    if (!!this.selected && !this.shifted) {\n      this.move([-1, 0]);\n    } else {\n      this.nextPoint(-1);\n    }\n  }\n  handleRight() {\n    console.log(!!this.selected, this.shifted);\n    if (!!this.selected && !this.shifted) {\n      return this.move([1, 0]);\n    }\n    if (this.shifted) {\n      this.nextPoint(1);\n    }\n  }\n  handleUp() {\n    if (!!this.selected && !this.shifted) {\n      this.move([0, -1]);\n    } else {\n      this.nextShape(-1);\n    }\n  }\n  handleDown() {\n    if (!!this.selected && !this.shifted) {\n      this.move([0, 1]);\n    } else {\n      this.nextShape(1);\n    }\n  }\n  drawUI() {\n    let {\n      ctx\n    } = this.elements;\n    let {\n      width,\n      height\n    } = ctx.canvas;\n    ctx.clearRect(0, 0, width, height);\n    this.drawShapes();\n  }\n  select(e) {\n    if (this.selectDisabled) {\n      return;\n    }\n    let {\n      shapes\n    } = this.state;\n    let {\n      selectedShape,\n      selectedPoint\n    } = this;\n    let relX = e.pageX / window.innerWidth;\n    let relY = e.pageY / window.innerHeight;\n\n    // TODO: So... I think we can actually use the same function\n    // For the triangles and the quads, just need to modify\n    // the check points to return an array of selected points\n    // and then the move function should move all the points\n    // in the selected array since the shared corners for the\n    // quads will just then both move\n\n    let selectedShapes = [];\n    for (var i = 0; i < shapes.length; i++) {\n      let shape = shapes[i];\n      let {\n        type\n      } = shapes[i];\n      if (type === \"triangle\" || type === \"quad\") {\n        let selected = this.checkTriangle(relX, relY, shape, i);\n        if (selected !== null) {\n          selectedShapes.push(selected);\n        }\n      }\n    }\n    selectedShapes.sort((a, b) => {\n      return (b.points || []).length - (a.points || []).length;\n    });\n    let selected = this.selected;\n    let first = selectedShapes[0] || null;\n    let current = selectedShapes.find(s => {\n      return s.shape === selected?.shape && (s.points || []).every(points => {\n        return (selected?.points || []).find(p => p[0] === points[0] && p[1] === points[1]);\n      });\n    });\n    this.selectedShapes = selectedShapes;\n    if (!current || (first?.points || []).length > (current?.points || []).length || this.shifted) {\n      this.selectNext();\n    }\n    this.drawUI();\n  }\n  selectNext() {\n    let {\n      selected,\n      selectedShapes\n    } = this;\n\n    // console.log(selectedShapes);\n\n    let current = selectedShapes.findIndex(s => {\n      return s.shape === selected?.shape && (s.points || []).every(points => {\n        return (selected?.points || []).find(p => p[0] === points[0] && p[1] === points[1]);\n      });\n    });\n\n    // console.log(current);\n\n    if (current >= 0) {\n      this.selected = selectedShapes[(current + 1) % selectedShapes.length];\n    } else {\n      this.selected = selectedShapes[0] || null;\n    }\n  }\n  checkTriangle(relX, relY, shape, shapeIdx) {\n    let {\n      tris\n    } = shape;\n    let points = this.checkTrianglePoints(relX, relY, shape, this.layer);\n    if (points.length > 0) {\n      return {\n        shape: shapeIdx,\n        points: points\n      };\n    }\n    for (let i = 0; i < tris.length; i++) {\n      let p = tris[i][this.layer];\n      let x = relX;\n      let y = relY;\n      let ax = p[0][0];\n      let ay = p[0][1];\n      let bx = p[1][0];\n      let by = p[1][1];\n      let cx = p[2][0];\n      let cy = p[2][1];\n      let det = (bx - ax) * (cy - ay) - (by - ay) * (cx - ax);\n      if (det * ((bx - ax) * (y - ay) - (by - ay) * (x - ax)) >= 0 && det * ((cx - bx) * (y - by) - (cy - by) * (x - bx)) >= 0 && det * ((ax - cx) * (y - cy) - (ay - cy) * (x - cx)) >= 0) {\n        return {\n          shape: shapeIdx,\n          points: []\n        };\n      }\n    }\n    return null;\n  }\n  checkTrianglePoints(relX, relY, shape, layer) {\n    let {\n      pointSize\n    } = this;\n    let {\n      tris\n    } = shape;\n    let selected = [];\n    let offsetX = (pointSize >> 1) * (1 / window.innerWidth);\n    let offsetY = (pointSize >> 1) * (1 / window.innerHeight);\n    for (let i = 0; i < tris.length; i++) {\n      let points = tris[i][layer];\n      for (let j = 0; j < points.length; j++) {\n        let x = points[j][0];\n        let y = points[j][1];\n        let minX = Math.min(Math.max(x - offsetX, 0), 1 - offsetX * 2);\n        let minY = Math.min(Math.max(y - offsetY, 0), 1 - offsetY * 2);\n        let maxX = Math.max(Math.min(x + offsetX, 1), offsetX * 2);\n        let maxY = Math.max(Math.min(y + offsetY, 1), offsetY * 2);\n        if (relX <= maxX && relX >= minX && relY <= maxY && relY >= minY) {\n          selected.push([i, j]);\n        }\n      }\n    }\n    return selected;\n  }\n  drawShapes() {\n    let {\n      selected\n    } = this;\n    let {\n      shapes\n    } = this.state;\n    let {\n      ctx\n    } = this.elements;\n    let {\n      width,\n      height\n    } = ctx.canvas;\n    for (var i = 0; i < shapes.length; i++) {\n      if (!!selected && selected.shape === i) {\n        continue;\n      }\n      this.drawShape(shapes[i]);\n    }\n    if (!!selected) {\n      this.drawShape(shapes[selected.shape]);\n    }\n  }\n  drawShape(shape) {\n    let {\n      type\n    } = shape;\n    if (type === \"triangle\") {\n      this.drawTri(shape);\n    }\n    if (type === \"quad\") {\n      this.drawQuad(shape);\n    }\n  }\n  drawTri(shape) {\n    let shapeIdx = this.state.shapes.indexOf(shape);\n    let {\n      ctx\n    } = this.elements;\n    let {\n      width,\n      height\n    } = ctx.canvas;\n    let {\n      tris\n    } = shape;\n    let {\n      selected\n    } = this;\n    for (var j = 0; j < tris.length; j++) {\n      let points = tris[j][this.layer];\n      for (var k = 0; k < points.length; k++) {\n        let isSelectedPoint = selected?.shape === shapeIdx && (selected?.points || []).find(t => t[0] === j && t[1] === k);\n        let x = width * points[k][0] | 0;\n        let y = height * points[k][1] | 0;\n        this.drawPoint(x, y, isSelectedPoint);\n      }\n      this.tracePoints(points[0][0], points[0][1], points[1][0], points[1][1], selected?.shape === shapeIdx);\n      this.tracePoints(points[1][0], points[1][1], points[2][0], points[2][1], selected?.shape === shapeIdx);\n      this.tracePoints(points[2][0], points[2][1], points[0][0], points[0][1], selected?.shape === shapeIdx);\n    }\n  }\n  drawQuad(shape) {\n    let shapeIdx = this.state.shapes.indexOf(shape);\n    let {\n      selected\n    } = this;\n    let {\n      ctx\n    } = this.elements;\n    let {\n      width,\n      height\n    } = ctx.canvas;\n    let {\n      tris\n    } = shape;\n    for (var j = 0; j < tris.length; j++) {\n      let points = tris[j][this.layer];\n      for (var k = 0; k < points.length; k++) {\n        let isSelectedPoint = selected?.shape === shapeIdx && (selected?.points || []).find(t => t[0] === j && t[1] === k);\n        let x = width * points[k][0] | 0;\n        let y = height * points[k][1] | 0;\n        this.drawPoint(x, y, isSelectedPoint);\n      }\n    }\n    let points1 = tris[0][this.layer];\n    let points2 = tris[1][this.layer];\n    this.tracePoints(points1[0][0], points1[0][1], points1[1][0], points1[1][1], selected?.shape === shapeIdx);\n    this.tracePoints(points1[1][0], points1[1][1], points1[2][0], points1[2][1], selected?.shape === shapeIdx);\n    this.tracePoints(points1[2][0], points1[2][1], points2[2][0], points2[2][1], selected?.shape === shapeIdx);\n    this.tracePoints(points2[2][0], points2[2][1], points1[0][0], points1[0][1], selected?.shape === shapeIdx);\n  }\n  tracePoints(x1, y1, x2, y2, selected) {\n    let {\n      ctx\n    } = this.elements;\n    let {\n      width,\n      height\n    } = ctx.canvas;\n    let clr = selected ? [255, 255, 0] : this.layerColor[this.layer];\n    ctx.strokeStyle = `rgba(${clr[0]},${clr[1]},${clr[2]},${this.opacity.value})`;\n    let originX = Math.max(width * x1 | 0, 5);\n    let originY = Math.max(height * y1 | 0, 5);\n    let destX = Math.max(width * x2 | 0, 5);\n    let destY = Math.max(height * y2 | 0, 5);\n    ctx.beginPath();\n    ctx.moveTo(originX, originY);\n    ctx.lineTo(destX, destY);\n    ctx.lineTo(originX, originY);\n    ctx.stroke();\n    ctx.closePath();\n  }\n  drawPoint(x, y, selected) {\n    let {\n      ctx\n    } = this.elements;\n    let {\n      width,\n      height\n    } = ctx.canvas;\n    let clr = selected ? [255, 255, 0] : this.layerColor[this.layer];\n    let opacity = this.opacity.value;\n    let offset = this.pointSize >> 1;\n    let minX = Math.min(Math.max(x - offset, 0), width - this.pointSize);\n    let minY = Math.min(Math.max(y - offset, 0), height - this.pointSize);\n    ctx.fillStyle = `rgba(${clr[0]},${clr[1]},${clr[2]},${opacity})`;\n    ctx.fillRect(minX, minY, this.pointSize, this.pointSize);\n    return true;\n  }\n  checkPoint(e, x, y, objIdx, pntIdx) {\n    let {\n      ctx\n    } = this.elements;\n    let {\n      width,\n      height\n    } = ctx.canvas;\n    let offset = this.pointSize >> 1;\n    let mouseX = ctx.canvas.width * (e.pageX / window.innerWidth);\n    let mouseY = ctx.canvas.height * (e.pageY / window.innerHeight);\n    let minX = Math.min(Math.max(x - offset, 0), width - this.pointSize);\n    let minY = Math.min(Math.max(y - offset, 0), height - this.pointSize);\n    let maxX = Math.max(Math.min(x + offset, width), this.pointSize);\n    let maxY = Math.max(Math.min(y + offset, height), this.pointSize);\n    if (mouseX <= maxX && mouseX >= minX && mouseY <= maxY && mouseY >= minY) {\n      this.selectedPoint = [objIdx, pntIdx];\n      return false;\n    }\n    return true;\n  }\n  move(movement) {\n    this.resetOpacity();\n    let x = movement[0];\n    let y = movement[1];\n    let {\n      selected\n    } = this;\n    if (!selected || selected.points.length < 1) {\n      return;\n    }\n    let shape = this.state.shapes[selected.shape];\n    for (let i = 0; i < selected.points.length; i++) {\n      let points = shape.tris[selected.points[i][0]][this.layer][selected.points[i][1]];\n      points[0] += 1 / window.innerWidth * x;\n      points[1] += 1 / window.innerHeight * y;\n    }\n    if (shape.type === \"quad\" && this.layer === \"input\") {\n      if (selected.points[0][0] === 0 && selected.points[0][1] === 1) {\n        shape.tris[0][this.layer][0][1] += 1 / window.innerHeight * y;\n        shape.tris[1][this.layer][0][1] += 1 / window.innerHeight * y;\n        shape.tris[0][this.layer][2][0] += 1 / window.innerWidth * x;\n        shape.tris[1][this.layer][1][0] += 1 / window.innerWidth * x;\n      }\n      if (selected.points[0][0] === 0 && selected.points[0][1] === 0) {\n        shape.tris[0][this.layer][1][1] += 1 / window.innerHeight * y;\n        shape.tris[1][this.layer][2][0] += 1 / window.innerWidth * x;\n      }\n      if (selected.points[0][0] === 0 && selected.points[0][1] === 2) {\n        shape.tris[1][this.layer][2][1] += 1 / window.innerHeight * y;\n        shape.tris[0][this.layer][1][0] += 1 / window.innerWidth * x;\n      }\n      if (selected.points[0][0] === 1 && selected.points[0][1] === 2) {\n        shape.tris[0][this.layer][2][1] += 1 / window.innerHeight * y;\n        shape.tris[1][this.layer][1][1] += 1 / window.innerHeight * y;\n        shape.tris[0][this.layer][0][0] += 1 / window.innerWidth * x;\n        shape.tris[1][this.layer][0][0] += 1 / window.innerWidth * x;\n      }\n    }\n    if (!!window.opener) {\n      window.opener.postMessage(JSON.stringify({\n        action: \"update_state\",\n        state: this.state\n      }));\n    }\n    this.drawUI();\n  }\n  movePoint(e) {\n    this.resetOpacity();\n    if (!this.moving || !this.selected) {\n      return;\n    }\n    let x = e.pageX / window.innerWidth;\n    let y = e.pageY / window.innerHeight;\n    let {\n      shapes\n    } = this.state;\n    let {\n      selected,\n      layer\n    } = this;\n    let shape = shapes[selected.shape];\n    let points = selected.points || [];\n    [x, y] = this.snapPoint(x, y);\n    for (let i = 0; i < points.length; i++) {\n      let tri = shape.tris[points[i][0]];\n      tri[layer][points[i][1]][0] = x;\n      tri[layer][points[i][1]][1] = y;\n    }\n    if (shape.type === \"quad\" && this.layer === \"input\") {\n      if (selected.points[0][0] === 0 && selected.points[0][1] === 1) {\n        shape.tris[0][this.layer][0][1] = y;\n        shape.tris[1][this.layer][0][1] = y;\n        shape.tris[0][this.layer][2][0] = x;\n        shape.tris[1][this.layer][1][0] = x;\n      }\n      if (selected.points[0][0] === 0 && selected.points[0][1] === 0) {\n        shape.tris[0][this.layer][1][1] = y;\n        shape.tris[1][this.layer][2][0] = x;\n      }\n      if (selected.points[0][0] === 0 && selected.points[0][1] === 2) {\n        shape.tris[1][this.layer][2][1] = y;\n        shape.tris[0][this.layer][1][0] = x;\n      }\n      if (selected.points[0][0] === 1 && selected.points[0][1] === 2) {\n        shape.tris[0][this.layer][2][1] = y;\n        shape.tris[1][this.layer][1][1] = y;\n        shape.tris[0][this.layer][0][0] = x;\n        shape.tris[1][this.layer][0][0] = x;\n      }\n    }\n    if (!!window.opener) {\n      window.opener.postMessage(JSON.stringify({\n        action: \"update_state\",\n        state: this.state\n      }));\n    }\n  }\n  snapPoint(relX, relY) {\n    let {\n      selected\n    } = this;\n    let {\n      shapes\n    } = this.state;\n    const getPoint = (match, shape, layer) => {\n      let triIdx = match[0];\n      let pointIdx = match[1];\n      let tri = shape.tris[triIdx][layer];\n      let point = tri[pointIdx];\n      return [...point];\n    };\n    let oppLayer = this.layer === \"input\" ? \"output\" : \"input\";\n    let matches = this.checkTrianglePoints(relX, relY, shapes[selected.shape], oppLayer);\n    if (matches.length > 0) {\n      return getPoint(matches[0], shapes[selected.shape], oppLayer);\n    }\n    for (let i = 0; i < shapes.length; i++) {\n      if (i === selected.shape) {\n        continue;\n      }\n      let matches = this.checkTrianglePoints(relX, relY, shapes[i], this.layer);\n      if (matches.length > 0) {\n        return getPoint(matches[0], shapes[i], this.layer);\n      }\n    }\n    return [relX, relY];\n  }\n  nextPoint(interval) {\n    this.resetOpacity();\n    if (this.state.shapes.length === 0) {\n      return;\n    }\n    let {\n      selected\n    } = this;\n    let shape = null;\n    let tri = null;\n    let point = null;\n    if (!selected) {\n      shape = 0;\n      tri = 0;\n      point = 2;\n    } else {\n      shape = selected.shape;\n      tri = (selected.points[0] || [])[0] ?? 0;\n      point = (selected.points[0] || [])[1] ?? 2;\n    }\n    let type = this.state.shapes[shape].type;\n    if (type === \"triangle\") {\n      let next = point + interval < 0 ? 2 : (point + interval) % 3;\n      this.selected = {\n        shape: shape,\n        points: [[tri, next]]\n      };\n    } else if (type === \"quad\") {\n      if (tri === 0 && point === 0 || tri === 1 && point === 0) {\n        if (interval > 0) {\n          this.selected = {\n            shape: shape,\n            points: [[0, 1]]\n          };\n        } else {\n          this.selected = {\n            shape: shape,\n            points: [[1, 2]]\n          };\n        }\n      } else if (tri === 0 && point === 1) {\n        if (interval > 0) {\n          this.selected = {\n            shape: shape,\n            points: [[0, 2], [1, 1]]\n          };\n        } else {\n          this.selected = {\n            shape: shape,\n            points: [[0, 0], [1, 0]]\n          };\n        }\n      } else if (tri === 0 && point === 2 || tri === 1 && point === 1) {\n        if (interval > 0) {\n          this.selected = {\n            shape: shape,\n            points: [[1, 2]]\n          };\n        } else {\n          this.selected = {\n            shape: shape,\n            points: [[0, 1]]\n          };\n        }\n      } else if (tri === 1 && point === 2) {\n        if (interval > 0) {\n          this.selected = {\n            shape: shape,\n            points: [[0, 0], [1, 0]]\n          };\n        } else {\n          this.selected = {\n            shape: shape,\n            points: [[0, 2], [1, 1]]\n          };\n        }\n      }\n    }\n    this.drawUI();\n  }\n  nextShape(interval) {\n    this.resetOpacity();\n    if (this.state.shapes.length === 0) {\n      return;\n    }\n    let {\n      selected\n    } = this;\n    let {\n      shapes\n    } = this.state;\n    let next;\n    if (!selected) {\n      console.log(shapes);\n      next = interval < 0 ? shapes.length - 1 : 0;\n    } else {\n      next = selected.shape + interval < 0 ? shapes.length - 1 : (selected.shape + interval) % shapes.length;\n    }\n    console.log(next);\n    this.selected = {\n      shape: next,\n      points: []\n    };\n    this.drawUI();\n  }\n}\n\n//# sourceURL=webpack://sensorycontrols/./src/js/screen-ui.js?");

/***/ }),

/***/ "./src/js/screen.js":
/*!**************************!*\
  !*** ./src/js/screen.js ***!
  \**************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"default\": () => (/* binding */ Screen)\n/* harmony export */ });\n/* harmony import */ var _effects_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./effects.js */ \"./src/js/effects.js\");\n/* harmony import */ var _screen_ui_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./screen-ui.js */ \"./src/js/screen-ui.js\");\n/* harmony import */ var _script_template_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./script-template.js */ \"./src/js/script-template.js\");\n/* harmony import */ var _effect_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./effect.js */ \"./src/js/effect.js\");\n/* harmony import */ var _script_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./script.js */ \"./src/js/script.js\");\n\n\n\n\n\nconst shaderMethods = {\n  pal: `\n    vec3 pal( in float t, in vec3 a, in vec3 b, in vec3 c, in vec3 d ) \n    {\n      return a + b*cos( 6.28318*(c*t+d) );\n    }\n  `,\n  rgb2hsv: `\n    vec3 rgb2hsv(vec3 c)\n    {\n      vec4 K = vec4(0.0, -1.0 / 3.0, 2.0 / 3.0, -1.0);\n      vec4 p = mix(vec4(c.bg, K.wz), vec4(c.gb, K.xy), step(c.b, c.g));\n      vec4 q = mix(vec4(p.xyw, c.r), vec4(c.r, p.yzx), step(p.x, c.r));\n\n      float d = q.x - min(q.w, q.y);\n      float e = 1.0e-10;\n      return vec3(abs(q.z + (q.w - q.y) / (6.0 * d + e)), d / (q.x + e), q.x);\n    }\n  `\n};\nconst vertexShaderSrc = `\n  attribute vec2 a_position;\n  attribute vec2 a_texcoord;\n\n  uniform float u_flipY;\n\n  varying vec2 v_texcoord;\n  \n  void main() {\n    gl_Position = vec4(a_position * vec2(1, u_flipY), 0.0, 1.0);\n    v_texcoord = a_texcoord;\n  }\n`;\nconst fragmentShader = `\n  precision mediump float;\n  varying vec2 v_texcoord;\n  uniform sampler2D u_texture;\n\n  uniform vec2 u_dimensions;\n  uniform mediump float u_opacity;\n  uniform vec2 u_effect;\n\n  // For quads\n  uniform float u_quad;\n  uniform vec2 u_translate;\n  uniform vec2 u_scale;\n  \n  uniform vec2 u_vertex_a;\n  uniform vec2 u_vertex_b;\n  uniform vec2 u_vertex_c;\n  uniform vec2 u_vertex_d;\n\n  \n  // Credit to https://www.shadertoy.com/view/lsBSDm for the invBilinear function\n  float cross2d( in vec2 a, in vec2 b ) { \n    return a.x*b.y - a.y*b.x; \n  }\n\n  vec2 invBilinear( in vec2 p, in vec2 a, in vec2 b, in vec2 c, in vec2 d ) {\n    vec2 res = vec2(-1.0);\n\n    vec2 e = b-a;\n    vec2 f = d-a;\n    vec2 g = a-b+c-d;\n    vec2 h = p-a;\n        \n    float k2 = cross2d( g, f );\n    float k1 = cross2d( e, f ) + cross2d( h, g );\n    float k0 = cross2d( h, e );\n    \n    if( abs(k2)<0.001 ) {\n        res = vec2( (h.x*k1+f.x*k0)/(e.x*k1-g.x*k0), -k0/k1 );\n    } else {\n      float w = k1*k1 - 4.0*k0*k2;\n      if ( w<0.0 ) return vec2(-1.0);\n      w = sqrt( w );\n\n      float ik2 = 0.5/k2;\n      float v = (-k1 - w)*ik2;\n      float u = (h.x - f.x*v)/(e.x + g.x*v);\n      \n      if( u<0.0 || u>1.0 || v<0.0 || v>1.0 ) {\n        v = (-k1 + w)*ik2;\n        u = (h.x - f.x*v)/(e.x + g.x*v);\n      }\n      res = vec2( u, v );\n    }\n    \n    return res;\n  }\n\n  void main() {\n    vec2 coords;\n\n    if (u_quad == 1.0) {\n      vec2 lerped = invBilinear(v_texcoord, u_vertex_a, u_vertex_b, u_vertex_c, u_vertex_d);\n      vec2 scaled = vec2(lerped[0] * u_scale[0], lerped[1] * u_scale[1]);\n      vec2 translated = vec2(scaled[0] + u_translate[0], scaled[1] + u_translate[1]);\n\n      coords = translated;\n    } else {\n      coords = v_texcoord;\n    }\n\n    \n    vec4 color = texture2D(u_texture, coords);\n    gl_FragColor = vec4(color[0], color[1], color[2], u_opacity * color[3]);\n  }\n`;\nclass Screen {\n  constructor(state, ctx, videos, options) {\n    this.options = {\n      width: 1280,\n      height: 720,\n      ...options\n    };\n\n    // this.pending_state = state;\n    this.state = state;\n    this.videos = videos;\n    this.gl = ctx;\n    this.registry = {\n      elapsed: 1\n    };\n    this.epoch = Date.now();\n    this.bpm = 0;\n    this.beat = [0, 0];\n    this.timing = [0, 0];\n    this.clock = null;\n    this.defaultClock = null;\n    this.pulseCount = 0;\n    this.beatStart = null;\n    this.beatTick = false;\n    this.resetBeat = false;\n    this.scripts = [];\n    this.effects = [];\n    this.textures = [];\n    this.glAttrs = new Array(6).fill(null);\n    this.attrs = {\n      main: null,\n      effects: []\n    };\n    this.isPlaying = false;\n    this.videos.forEach(vid => {\n      vid.addEventListener(\"loadedmetadata\", e => {\n        setTimeout(() => {\n          this.updateContext(e.target);\n          vid.play();\n        }, 100);\n      });\n    });\n    this.createContext();\n    this.updateSlots();\n  }\n  setEffectOrder() {\n    let effects = _effects_js__WEBPACK_IMPORTED_MODULE_0__.Effects.map(e => e.id);\n    let used = [...this.state.effects].filter(e => !!e);\n    let unused = effects.reduce((a, c) => {\n      if (!used.includes(c)) {\n        a.push(c);\n      }\n      return a;\n    }, []);\n  }\n  play() {\n    if (this.isPlaying) {\n      return;\n    }\n    this.isPlaying = true;\n    this.step();\n  }\n  stop() {\n    this.isPlaying = false;\n    this.gl.clear(this.gl.COLOR_BUFFER_BIT);\n  }\n  pause() {\n    this.isPlaying = false;\n  }\n  step() {\n    if (!this.isPlaying) {\n      return;\n    }\n    let len = this.videos.length - 1;\n    let state = JSON.parse(JSON.stringify(this.state));\n    let beat = this.beatTick ? this.beat : null;\n    state.elapsed = Date.now() - this.epoch;\n    state.bpm = this.bpm;\n    state.beat = beat;\n    state.timing = this.timing;\n    state.elements = this.videos;\n    for (let i = 0; i < this.scripts.length; i++) {\n      let {\n        fn,\n        idx\n      } = this.scripts[i];\n      let target = state.scripts[idx];\n      let values = target.values;\n      let disabled = target.disabled;\n      if (!disabled) {\n        try {\n          fn(state, values[0], values[1], target.id, this.registry);\n        } catch (e) {\n          console.log(e);\n        }\n      }\n    }\n    this.beatTick = false;\n    this.registry.elapsed = state.elapsed;\n    this.gl.clear(this.gl.COLOR_BUFFER_BIT);\n    this.drawFrame(state);\n    window.requestAnimationFrame(this.step.bind(this));\n  }\n  drawFrame(state) {\n    let effects = this.effects;\n    let {\n      videos,\n      shapes,\n      slots\n    } = state;\n\n    // Get the context\n    let gl = this.gl;\n    // Get the attributes\n    let attrs = this.attrs;\n    let videosLen = this.videos.length - 1;\n    // let activeBuffer = 1;\n    let activeBuffers = new Array(this.videos.length).fill(0);\n    for (var j = videosLen; j >= 0; j--) {\n      let video = videos[j];\n      let videoEl = this.videos[j];\n      let texture = this.textures[j];\n\n      // Skip if video isn't playing\n      if (videoEl === null || videoEl.currentTime === 0) {\n        continue;\n      }\n      this.updateTexture(gl, texture, videoEl);\n    }\n\n    // First draw the texture to the first frame buffer\n    for (var j = videosLen; j >= 0; j--) {\n      let video = videos[j];\n      let vals = [0, 0];\n      let videoEl = this.videos[j];\n      let texture = this.textures[j];\n\n      // Skip if video isn't playing\n      if (videoEl === null || videoEl.currentTime === 0) {\n        continue;\n      }\n      gl.bindTexture(gl.TEXTURE_2D, texture.src);\n      gl.bindFramebuffer(gl.FRAMEBUFFER, texture.attrs.buffers[0]);\n      gl.viewport(0, 0, videoEl.videoWidth, videoEl.videoHeight);\n      gl.useProgram(attrs.main.program);\n      this.drawShapes(gl, videoEl, j, attrs.main, shapes, [0, 0], -1, true);\n    }\n    for (var i = 0; i < effects.length; i++) {\n      let {\n        fx,\n        idx\n      } = effects[i];\n      let target = state.effects[idx];\n      gl.useProgram(fx.program);\n      for (var j = videosLen; j >= 0; j--) {\n        let video = videos[j];\n        let videoEl = this.videos[j];\n        let texture = this.textures[j];\n        let vals = target.values[j];\n        // Skip if this effect isn't being used\n        if (vals[0] === 0 && vals[1] === 0) {\n          continue;\n        }\n\n        // Skip if video isn't playing\n        if (videoEl === null || videoEl.currentTime === 0) {\n          continue;\n        }\n        let activeBuffer = activeBuffers[j];\n        let drawBuffer = (activeBuffer + 1) % 2;\n        gl.bindTexture(gl.TEXTURE_2D, texture.attrs.textures[activeBuffer]);\n        gl.bindFramebuffer(gl.FRAMEBUFFER, texture.attrs.buffers[drawBuffer]);\n        gl.viewport(0, 0, videoEl.videoWidth, videoEl.videoHeight);\n        gl.clearColor(0, 0, 0, 0);\n        gl.clear(gl.COLOR_BUFFER_BIT);\n        this.drawShapes(gl, videoEl, j, fx, shapes, vals, -1, true);\n        activeBuffers[j] = drawBuffer;\n      }\n    }\n    for (var j = videosLen; j >= 0; j--) {\n      let video = videos[j];\n      let vals = [0, 0];\n      let videoEl = this.videos[j];\n      let texture = this.textures[j];\n\n      // Skip if video isn't playing\n      if (videoEl === null || videoEl.currentTime === 0) {\n        continue;\n      }\n      gl.bindTexture(gl.TEXTURE_2D, texture.attrs.textures[activeBuffers[j]]);\n      gl.bindFramebuffer(gl.FRAMEBUFFER, null);\n      gl.viewport(0, 0, this.options.width, this.options.height);\n      // gl.viewport(0, 0, videoEl.videoWidth, videoEl.videoHeight);\n      gl.useProgram(attrs.main.program);\n      this.drawShapes(gl, videoEl, j, attrs.main, shapes, [0, 0], 1);\n    }\n  }\n  drawShapes(gl, video, idx, attrs, shapes, values, flip, ignoreOpacity) {\n    gl.uniform2fv(attrs.uniforms.effect, values);\n    gl.uniform1f(attrs.uniforms.flip, flip);\n    gl.uniform2fv(attrs.uniforms.dimensions, [1 / video.videoWidth, 1 / video.videoHeight]);\n    for (var i = 0; i < shapes.length; i++) {\n      let opacity = shapes[i].opacity[idx];\n      if (opacity === 0) {\n        continue;\n      }\n      let tris = shapes[i].tris;\n      if (flip === 1 && shapes[i].type === \"quad\") {\n        let vert_a = [tris[0].output[0][0], tris[0].output[0][1]];\n        let vert_b = [tris[0].output[1][0], tris[0].output[1][1]];\n        let vert_c = [tris[0].output[2][0], tris[0].output[2][1]];\n        let vert_d = [tris[1].output[2][0], tris[1].output[2][1]];\n        let translate = [tris[0].input[0][0], tris[0].input[0][1]];\n        let scale = [tris[0].input[1][0] - tris[0].input[0][0], tris[0].input[2][1] - tris[0].input[1][1]];\n        gl.uniform1f(attrs.uniforms.quad, 1);\n        gl.uniform2fv(attrs.uniforms.vert_a, vert_a);\n        gl.uniform2fv(attrs.uniforms.vert_b, vert_b);\n        gl.uniform2fv(attrs.uniforms.vert_c, vert_c);\n        gl.uniform2fv(attrs.uniforms.vert_d, vert_d);\n        gl.uniform2fv(attrs.uniforms.translate, translate);\n        gl.uniform2fv(attrs.uniforms.scale, scale);\n      } else {\n        gl.uniform1f(attrs.uniforms.quad, 0);\n      }\n      for (var j = 0; j < tris.length; j++) {\n        // If we're rendering an effect to a frame buffer,\n        // use the same points for the input and output\n        let pnts = tris[j].input;\n        let oPnts = tris[j].input;\n        // If we're rendering the final output use the output points\n        if (flip === 1) {\n          if (shapes[i].type === \"quad\") {\n            pnts = tris[j].output;\n          }\n          oPnts = tris[j].output;\n        }\n        gl.bindBuffer(gl.ARRAY_BUFFER, attrs.buffers.position);\n        gl.bufferData(gl.ARRAY_BUFFER, new Float32Array([oPnts[0][0] * 2 - 1, oPnts[0][1] * -2 + 1, oPnts[1][0] * 2 - 1, oPnts[1][1] * -2 + 1, oPnts[2][0] * 2 - 1, oPnts[2][1] * -2 + 1]), gl.DYNAMIC_DRAW);\n        gl.vertexAttribPointer(attrs.locations.position, 2, gl.FLOAT, false, 0, 0);\n        gl.bindBuffer(gl.ARRAY_BUFFER, attrs.buffers.texture);\n        gl.bufferData(gl.ARRAY_BUFFER, new Float32Array([pnts[0][0], pnts[0][1], pnts[1][0], pnts[1][1], pnts[2][0], pnts[2][1]]), gl.DYNAMIC_DRAW);\n        gl.vertexAttribPointer(attrs.locations.texture, 2, gl.FLOAT, false, 0, 0);\n        gl.uniform1f(attrs.uniforms.opacity, ignoreOpacity ? 1 : opacity);\n        gl.uniform1i(attrs.uniforms.sampler, 0);\n        gl.drawArrays(gl.TRIANGLES, 0, 3);\n      }\n    }\n  }\n  createShader(gl, type, source) {\n    let shader = gl.createShader(type);\n    gl.shaderSource(shader, source);\n    gl.compileShader(shader);\n    let success = gl.getShaderParameter(shader, gl.COMPILE_STATUS);\n    if (success) {\n      return shader;\n    }\n    gl.deleteShader(shader);\n  }\n  createProgram(id, gl, vShaderSrc, fShaderSrc) {\n    let vShader = this.createShader(gl, gl.VERTEX_SHADER, vShaderSrc);\n    let fShader = this.createShader(gl, gl.FRAGMENT_SHADER, fShaderSrc);\n    let program = gl.createProgram();\n    gl.attachShader(program, vShader);\n    gl.attachShader(program, fShader);\n    gl.linkProgram(program);\n    let success = gl.getProgramParameter(program, gl.LINK_STATUS);\n    if (success) {\n      let positionLocation = gl.getAttribLocation(program, \"a_position\");\n      let positionBuffer = gl.createBuffer();\n      gl.enableVertexAttribArray(positionLocation);\n      gl.bindBuffer(gl.ARRAY_BUFFER, positionBuffer);\n      gl.vertexAttribPointer(positionLocation, 2, gl.FLOAT, false, 0, 0);\n      let textureLocation = gl.getAttribLocation(program, \"a_texcoord\");\n      let textureBuffer = gl.createBuffer();\n      gl.enableVertexAttribArray(textureLocation);\n      gl.bindBuffer(gl.ARRAY_BUFFER, textureBuffer);\n      gl.vertexAttribPointer(textureLocation, 2, gl.FLOAT, false, 0, 0);\n      let attrs = {\n        id: id,\n        program: program,\n        locations: {\n          position: positionLocation,\n          texture: textureLocation\n        },\n        buffers: {\n          position: positionBuffer,\n          texture: textureBuffer\n        },\n        uniforms: {\n          sampler: gl.getUniformLocation(program, \"u_texture\"),\n          flip: gl.getUniformLocation(program, \"u_flipY\"),\n          opacity: gl.getUniformLocation(program, \"u_opacity\"),\n          effect: gl.getUniformLocation(program, \"u_effect\"),\n          dimensions: gl.getUniformLocation(program, \"u_dimensions\"),\n          quad: gl.getUniformLocation(program, \"u_quad\"),\n          translate: gl.getUniformLocation(program, \"u_translate\"),\n          scale: gl.getUniformLocation(program, \"u_scale\"),\n          vert_a: gl.getUniformLocation(program, \"u_vertex_a\"),\n          vert_b: gl.getUniformLocation(program, \"u_vertex_b\"),\n          vert_c: gl.getUniformLocation(program, \"u_vertex_c\"),\n          vert_d: gl.getUniformLocation(program, \"u_vertex_d\")\n        }\n      };\n      return attrs;\n    } else {\n      gl.deleteProgram(program);\n      return null;\n    }\n  }\n  createContext() {\n    this.attrs.main = this.createProgram(\"__main__\", this.gl, vertexShaderSrc, fragmentShader);\n    this.textures = new Array(6).fill(null).map(t => {\n      return this.initTexture(this.gl);\n    });\n    if (this.attrs.main !== null) {\n      this.gl.clearColor(0, 0, 0, 0);\n      this.gl.clear(this.gl.COLOR_BUFFER_BIT);\n      this.gl.disable(this.gl.DEPTH_TEST);\n      this.gl.enable(this.gl.BLEND);\n      this.gl.blendFunc(this.gl.SRC_ALPHA, this.gl.ONE_MINUS_SRC_ALPHA);\n\n      // let effects = this.state.effects;\n      for (var i = 0; i < _effects_js__WEBPACK_IMPORTED_MODULE_0__.Effects.length; i++) {\n        let effect = _effects_js__WEBPACK_IMPORTED_MODULE_0__.Effects[i];\n        this.attrs.effects.push(this.createProgram(effect.id, this.gl, vertexShaderSrc, effect.shader));\n      }\n    }\n  }\n  updateContext(video) {\n    this.gl.viewport(0, 0, this.options.width, this.options.height);\n    this.gl.clearColor(0, 0, 0, 1);\n    this.gl.clear(this.gl.COLOR_BUFFER_BIT);\n  }\n  initTexture(gl) {\n    const texture = gl.createTexture();\n    gl.bindTexture(gl.TEXTURE_2D, texture);\n    const level = 0;\n    const internalFormat = gl.RGBA;\n    const width = 1;\n    const height = 1;\n    const border = 0;\n    const srcFormat = gl.RGBA;\n    const srcType = gl.UNSIGNED_BYTE;\n    const pixel = new Uint8Array([0, 0, 255, 255]); // opaque blue\n    gl.texImage2D(gl.TEXTURE_2D, level, internalFormat, width, height, border, srcFormat, srcType, pixel);\n\n    // Turn off mips and set wrapping to clamp to edge so it\n    // will work regardless of the dimensions of the video.\n    gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_S, gl.CLAMP_TO_EDGE);\n    gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_T, gl.CLAMP_TO_EDGE);\n    gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_MIN_FILTER, gl.LINEAR);\n    // gl.pixelStorei(gl.UNPACK_FLIP_Y_WEBGL, true);\n\n    return {\n      src: texture,\n      attrs: {\n        textures: [],\n        buffers: []\n      }\n    };\n  }\n  updateTexture(gl, texture, video) {\n    if (!video || video.currentTime === 0) {\n      return;\n    }\n    const level = 0;\n    const internalFormat = gl.RGBA;\n    const srcFormat = gl.RGBA;\n    const srcType = gl.UNSIGNED_BYTE;\n    const {\n      attrs\n    } = texture;\n    if (attrs.textures.length < 2 && attrs.buffers.length < 2) {\n      for (let i = 0; i < 2; i++) {\n        const bufferTexture = gl.createTexture();\n        gl.bindTexture(gl.TEXTURE_2D, bufferTexture);\n        gl.texImage2D(gl.TEXTURE_2D, level, internalFormat, srcFormat, srcType, video);\n        gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_S, gl.CLAMP_TO_EDGE);\n        gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_T, gl.CLAMP_TO_EDGE);\n        gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_MIN_FILTER, gl.LINEAR);\n        let frameBuffer = this.initFrameBuffer(gl, bufferTexture);\n        attrs.textures.push(bufferTexture);\n        attrs.buffers.push(frameBuffer);\n      }\n    }\n    gl.bindTexture(gl.TEXTURE_2D, texture.src);\n    gl.texImage2D(gl.TEXTURE_2D, level, internalFormat, srcFormat, srcType, video);\n\n    // gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_S, gl.CLAMP_TO_EDGE);\n    // gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_WRAP_T, gl.CLAMP_TO_EDGE);\n    // gl.texParameteri(gl.TEXTURE_2D, gl.TEXTURE_MIN_FILTER, gl.LINEAR);\n  }\n  initFrameBuffer(gl, texture) {\n    let frameBuffer = gl.createFramebuffer();\n    gl.bindFramebuffer(gl.FRAMEBUFFER, frameBuffer);\n    gl.framebufferTexture2D(gl.FRAMEBUFFER, gl.COLOR_ATTACHMENT0, gl.TEXTURE_2D, texture, 0);\n    return frameBuffer;\n  }\n  loadVideo(idx, file) {\n    this.videos[idx].src = URL.createObjectURL(file);\n  }\n  removeVideo(idx) {\n    let video = this.videos[idx];\n    this.textures[idx] = null;\n    this.textures[idx] = this.initTexture(this.gl);\n    if (!!video) {\n      if (!video.stream) {\n        video.pause();\n        video.removeAttribute(\"src\");\n        video.removeAttribute(\"srcObject\");\n        video.load();\n      } else {\n        video.stream.getTracks().forEach(track => {\n          if (track.readyState == \"live\") {\n            track.stop();\n          }\n        });\n        video.src = null;\n        video.srcObject = null;\n        video.load();\n        delete video.stream;\n      }\n      this.gl.clear(this.gl.COLOR_BUFFER_BIT);\n    }\n  }\n  async setMedia(videoIdx, deviceId) {\n    let videos = document.querySelectorAll(\".videos video\");\n    let vid = videos[videoIdx];\n    let media = await navigator.mediaDevices.enumerateDevices().then(devices => {\n      return [...devices].filter(d => {\n        return d.kind === \"videoinput\";\n      });\n    });\n    let device = [...media].find(m => {\n      return m.deviceId === deviceId;\n    });\n    try {\n      let stream = await navigator.mediaDevices.getUserMedia({\n        audio: false,\n        video: {\n          deviceId: {\n            exact: device.deviceId\n          }\n        }\n      });\n      vid.srcObject = stream;\n      this.videos[videoIdx] = vid;\n      this.videos[videoIdx].stream = stream;\n    } catch (err) {\n      console.log(\"Failed to load webcam:\", err);\n    }\n  }\n  async removeMedia(videoIdx) {}\n  updateSlots() {\n    let {\n      effects,\n      scripts,\n      slots\n    } = this.state;\n    this.scripts = slots.filter(slot => {\n      return slot?.target instanceof _script_js__WEBPACK_IMPORTED_MODULE_4__.Script;\n    }).map(slot => {\n      let {\n        code\n      } = slot.target;\n      let fn = new Function(\"state\", \"effect_x\", \"effect_y\", \"script_id\", \"registry\", (0,_script_template_js__WEBPACK_IMPORTED_MODULE_2__.ScriptTemplate)(code));\n      let idx = this.state.scripts.findIndex(s => s.id = slot.target.id);\n      return {\n        idx: idx,\n        fn: fn\n      };\n    });\n    this.effects = [...slots.filter(slot => {\n      return slot?.target instanceof _effect_js__WEBPACK_IMPORTED_MODULE_3__.Effect;\n    }).map(slot => {\n      return slot.target;\n    }), ...effects.filter(effect => {\n      return !slots.find(slot => slot?.target?.id === effect.id);\n    })].map(e => {\n      let idx = this.state.effects.findIndex(f => f.id === e.id);\n      return {\n        idx: idx,\n        fx: this.attrs.effects.find(fx => fx.id === e.id)\n      };\n    });\n    slots.map((slot, idx) => {\n      if (slot?.type !== \"script\") {\n        delete this.registry[idx];\n      }\n    });\n  }\n\n  // updateScriptValues(idx, values) {}\n\n  // updateEffectValues(idx, values) {}\n\n  setClock(clock, bpm) {\n    if (clock === \"default\") {\n      this.clock = setTimeout(() => {\n        this.handleBeat({\n          data: [248]\n        });\n      }, 60 / bpm * 1000);\n      return;\n    }\n    if (!!navigator.requestMIDIAccess) {\n      navigator.requestMIDIAccess().then(midiAccess => {\n        for (const entry of midiAccess.inputs) {\n          if (entry[1].name === clock) {\n            this.clock = entry[1];\n            entry[1].onmidimessage = this.handleBeat.bind(this);\n          }\n        }\n      }, msg => {\n        console.error(`Failed to get MIDI access - ${msg}`);\n      });\n    } else {\n      console.log(\"No Midi Access\");\n    }\n  }\n  handleBeat(e) {\n    if (e.data[0] === 248) {\n      this.pulseCount = (this.pulseCount + 1) % 6;\n\n      ////////// Tracking the count //////////\n      if (this.pulseCount === 0) {\n        if (this.resetBeat) {\n          this.beat = [1, 1];\n          this.timing = [Date.now(), Date.now()];\n          this.resetBeat = false;\n        } else {\n          let next4th = this.beat[0];\n          let next16th = (this.beat[1] + 1) % 5 || 1;\n          this.timing[1] = Date.now();\n          if (next16th === 1) {\n            next4th = (next4th + 1) % 5 || 1;\n            this.timing[0] = Date.now();\n          }\n          this.beat = [next4th, next16th];\n          this.beatTick = true;\n        }\n      }\n      ////////// End Tracking counting //////////\n\n      ////////// Tracking the BPM //////////\n      if (this.beatStart === null) {\n        this.beatStart = Date.now();\n      }\n      if (this.pulseCount === 0) {\n        let diff = Date.now() - this.beatStart;\n        let bpm = 60 / (diff / 250);\n        this.bpm = bpm;\n        this.beatStart = Date.now();\n      }\n      ////////// End BPM tracking //////////\n    }\n  }\n  setOne() {\n    let duration = 125 * (this.bpm / 60);\n    if (Date.now() - this.timing[1] < duration) {\n      this.beat = [1, 1];\n    } else {\n      this.resetBeat = true;\n    }\n  }\n}\n\n//# sourceURL=webpack://sensorycontrols/./src/js/screen.js?");

/***/ }),

/***/ "./src/js/script-template.js":
/*!***********************************!*\
  !*** ./src/js/script-template.js ***!
  \***********************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   ScriptTemplate: () => (/* binding */ ScriptTemplate)\n/* harmony export */ });\nconst ScriptTemplate = function (script) {\n  return `\n\t\t// Keep track of the percs for each lfo initiated\n\t\tif (!registry[script_id]) {\n\t\t\tregistry[script_id] = {};\n\t\t}\n\n\t\tregistry[script_id].lfos = registry[script_id].lfos || [];\n\t\tregistry[script_id].beats = registry[script_id].beats || {};\n\n\t\tlet lfos = registry[script_id].lfos;\n\t\tlet lfos_count = 0;\n\n\t\tconst lfo = function(min, max, interval) {\n\t\t\tlet ms = interval * 1000;\n\t\t\tlet previous = lfos[lfos_count] || 1;\n\t\t\tlet delta = Math.abs(((state.elapsed % ms) / ms) - ((registry.elapsed % ms) / ms));\n\t\t\tlet perc = (previous + delta) % 1;\n\n\t\t\tlfos[lfos_count] = perc || 0;\n\t\t\tlfos_count++;\n\n\t\t\treturn {\n\t\t\t\tsin: function() {\n\t\t\t\t\treturn (\n\t\t\t      min +\n\t\t\t      (1 + Math.sin((perc * 360 * Math.PI) / 180)) * ((max - min) / 2)\n\t\t\t    );\n\t\t\t\t},\n\t\t\t\tcos: function() {\n\t\t\t\t\treturn (\n\t\t\t      min +\n\t\t\t      (1 + Math.cos((perc * 360 * Math.PI) / 180)) * ((max - min) / 2)\n\t\t\t    );\n\t\t\t\t},\n\t\t\t\ttri: function() {\n\t\t\t\t\treturn Math.abs(perc - 0.5) * (max - min) * 2 + min;\n\t\t\t\t},\n\t\t\t\tsaw: function() {\n\t\t\t\t\treturn perc * (max - min) + min;\n\t\t\t\t},\n\t\t\t\tsquare: function() {\n\t\t\t\t\treturn Math.round(perc) * (max - min) + min;\n\t\t\t\t},\n\t\t\t\tnoise: function() {\n\t\t\t\t\treturn Math.floor(Math.random() * (max - min + 1)) + min;\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\tconst onBeats = function(beats, on, off) {\n\t\t\tlet beat_id = ''\n\t\t\tfor (var i=0; i<beats.length; i++) {\n\t\t\t\tbeat_id = beat_id + beats[i][0] + \".\" + beats[i][1];\n\t\t\t}\n\t\t\tlet current = registry[script_id].beats[beat_id];\n\t\t\tif (state.beat !== null) {\n\t\t\t\tlet match = beats.find((beat) => {\n\t\t\t\t\tlet match1 = beat[0] === state.beat[0] || beat[0] === \"*\";\n\t\t\t\t\tlet match2 = beat[1] === state.beat[1] || beat[1] === \"*\";\n\t\t\t\t\t\n\t\t\t\t\treturn match1 && match2;\n\t\t\t\t})\n\n\t\t\t\tif (match) {\n\t\t      registry[script_id].beats[beat_id] = on(state.beat, current);\n\t\t\t\t}\n\t\t\t} else {\n\t\t\t\tif (!!off) {\n\t\t\t\t\tregistry[script_id].beats[beat_id] = off(state.beat, current);\n\t\t\t\t}\n\t\t\t}\n\n\t\t\treturn registry[script_id].beats[beat_id];\n\t\t}\n\n\t\t// Deprecated\n\t\tconst smooth_lfo = function(min, max, interval) {\n\t\t\tlet ms = interval * 1000;\n\t\t\tlet previous = lfos[lfos_count] || 1;\n\t\t\tlet delta = Math.abs(((state.elapsed % ms) / ms) - ((registry.elapsed % ms) / ms));\n\t\t\tlet perc = (previous + delta) % 1;\n\n\t\t\tlfos[lfos_count] = perc || 0;\n\t\t\tlfos_count++;\n\n\t\t\treturn {\n\t\t\t\tsin: function() {\n\t\t\t\t\treturn (\n\t\t\t      min +\n\t\t\t      (1 + Math.sin((perc * 360 * Math.PI) / 180)) * ((max - min) / 2)\n\t\t\t    );\n\t\t\t\t},\n\t\t\t\tcos: function() {\n\t\t\t\t\treturn (\n\t\t\t      min +\n\t\t\t      (1 + Math.cos((perc * 360 * Math.PI) / 180)) * ((max - min) / 2)\n\t\t\t    );\n\t\t\t\t},\n\t\t\t\ttri: function() {\n\t\t\t\t\treturn Math.abs(perc - 0.5) * (max - min) * 2 + min;\n\t\t\t\t},\n\t\t\t\tsaw: function() {\n\t\t\t\t\treturn perc * (max - min) + min;\n\t\t\t\t},\n\t\t\t\tsquare: function() {\n\t\t\t\t\treturn Math.round(perc) * (max - min) + min;\n\t\t\t\t},\n\t\t\t\tnoise: function() {\n\t\t\t\t\treturn Math.floor(Math.random() * (max - min + 1)) + min;\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\tconst setVideoOpacity = function(videos, opacity) {\n\t\t\tif (!Array.isArray(videos)) {\n\t\t\t\tvideos = [videos];\n\t\t\t}\n\n\t\t\tfor (let i=0; i<videos.length; i++) {\n\t\t\t\tlet video = videos[i];\n\n\t\t\t\tfor (var j=0; j<state.shapes.length; j++) {\n\t\t\t\t\tlet shape = state.shapes[j];\n\t\t\t\t\tshape.opacity[video] = opacity;\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\tconst getVideoOpacity = function(video) {\n\t\t\treturn state.videos[video].opacity;\n\t\t}\n\n\t\tconst setShapeOpacity = function(shapes, videos, opacity)  {\n\t\t\tif (!Array.isArray(shapes)) {\n\t\t\t\tshapes = [shapes];\n\t\t\t}\n\n\t\t\tif (!Array.isArray(videos)) {\n\t\t\t\tvideos = [videos];\n\t\t\t}\n\n\t\t\tfor (let i=0; i<shapes.length; i++) {\n\t\t\t\tlet shape = state.shapes[shapes[i]];\n\n\t\t\t\tfor (var j=0; j<videos.length; j++) {\n\t\t\t\t\tlet video = videos[j];\n\n\t\t\t\t\tshape.opacity[video] = opacity;\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\tconst getShapeOpacity = function(shape, video) {\n\t\t\treturn state.shapes[shape].opacity[video];\n\t\t}\n\n\t\tconst getValues = function(identifier) {\n\t\t\tif (typeof identifier === 'number') {\n\t\t\t\treturn [state.slots[identifier].values];\n\t\t\t}\n\n\t\t\tlet values = [];\n\t\t\tfor (let i=0; i<state.scripts.length; i++) {\n\t\t\t\tif (state.scripts[i].label.toLowerCase() === identifier.toLowerCase()) {\n\t\t\t\t\tvalues.push(state.scripts[i].values);\n\t\t\t\t}\n\t\t\t}\n\n\t\t\tfor (let i=0; i<state.effects.length; i++) {\n\t\t\t\tif (state.effects[i].label.toLowerCase() === identifier.toLowerCase()) {\n\t\t\t\t\tvalues.push(state.effects[i].values);\n\t\t\t\t}\n\t\t\t}\t\n\n\t\t\treturn values;\n\t\t}\n\n\t\tconst setValues = function(identifier, videos, x, y) {\n\t\t\tlet current = getValues(identifier);\n\n\t\t\tif (!Array.isArray(videos)) {\n\t\t\t\tvideos = [videos];\n\t\t\t}\n\n\t\t\tfor (let i=0; i<current.length; i++) {\n\t\t\t\tlet set = current[i];\n\n\t\t\t\tfor (let j=0; j<videos.length; j++) {\n\t\t\t\t\tif (x !== null) {\n\t\t\t\t\t\tset[videos[j]][0] = Math.max(0, Math.min(1, x || 0));\n\t\t\t\t\t}\n\n\t\t\t\t\tif (y !== null) {\n\t\t\t\t\t\tset[videos[j]][1] = Math.max(0, Math.min(1, y || 0));;\n\t\t\t\t\t} \n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\tconst setTime = function(videos, start) {\n\t\t\tif (!Array.isArray(videos)) {\n\t\t\t\tvideos = [videos];\n\t\t\t}\n\n\t\t\tlet element;\n\t\t\tfor (var i=0; i<videos.length; i++) {\n\t\t\t\telement = state.elements[videos[i]];\n\t\t\t\telement.currentTime = start;\n\t\t\t}\n\t\t}\n\n\t\tconst setLoop = function(videos, start, end) {\n\t\t\tif (!Array.isArray(videos)) {\n\t\t\t\tvideos = [videos];\n\t\t\t}\n\n\t\t\tlet element;\n\t\t\tfor (var i=0; i<videos.length; i++) {\n\t\t\t\telement = state.elements[videos[i]];\n\t\t\t\tend = end || element.duration;\n\t\t\t\tstart = start || 0;\n\n\t\t\t\tif (element.currentTime > end) {\n\t\t\t\t\telement.currentTime = start;\n\t\t\t\t}\n\n\t\t\t\tif (element.currentTime < start) {\n\t\t\t\t\telement.currentTime = start;\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\tconst setPlaybackRate = function(videos, rate) {\n\t\t\tif (!Array.isArray(videos)) {\n\t\t\t\tvideos = [videos];\n\t\t\t}\n\n\t\t\tlet element;\n\t\t\tfor (var i=0; i<videos.length; i++) {\n\t\t\t\telement = state.elements[videos[i]];\n\t\t\t\telement.playbackRate = rate;\n\t\t\t}\n\t\t}\n\n\t\t// Deprecated\n\t\tconst getEffectById = function(id) {\n\t\t\treturn [];\n\t\t}\n\n\t\t// Deprecated\n\t\tconst setEffectValues = function(effects, videos, x, y) {\n\t\t\treturn;\n\t\t}\n\n\t\t// Deprecated\n\t\tconst getEffectValues = function(effect, video) {\n\t\t\treturn [0, 0];\n\t\t}\n\n\t\tconst getSelectedVideo = function() {\n\t\t\treturn state.selected.video;\n\t\t}\n\n\t\tconst getSelectedSlot = function() {\n\t\t\treturn state.selected.slot;\n\t\t}\n\n\t\t// Deprecated\n\t\tconst getSelectedEffect = function() {\n\t\t\treturn state.selected.slot;\n\t\t}\n\n\t\t// Deprecated\n\t\tconst getSelectedScript = function() {\n\t\t\treturn state.selected.slot;\n\t\t}\n\n\t\tconst setInputPoints = function(shapes, fn) {\n\t\t\tif (!Array.isArray(shapes)) {\n\t\t\t\tshapes = [shapes];\n\t\t\t}\n\n\t\t\tfor (let i=0; i<shapes.length; i++) {\n\t\t\t\tlet shape = state.shapes[shapes[i]];\n\n\t\t\t\tif (!shape) {\n\t\t\t\t\tcontinue;\n\t\t\t\t}\n\n\t\t\t\tif (shape.type === \"triangle\") {\n\t\t\t\t\tfn(0, [shape.tris[0].input[0]])\n\t\t\t\t\tfn(1, [shape.tris[0].input[1]])\n\t\t\t\t\tfn(2, [shape.tris[0].input[2]])\n\t\t\t\t} else if (shape.type === \"quad\") {\n\t\t\t\t\tfn(0, [shape.tris[0].input[0], shape.tris[1].input[0]])\n\t\t\t\t\tfn(1, [shape.tris[0].input[1]])\n\t\t\t\t\tfn(2, [shape.tris[0].input[2], shape.tris[1].input[1]])\n\t\t\t\t\tfn(3, [shape.tris[1].input[2]])\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\tconst setOutputPoints = function(shapes, fn) {\n\t\t\tif (!Array.isArray(shapes)) {\n\t\t\t\tshapes = [shapes];\n\t\t\t}\n\n\t\t\tfor (let i=0; i<shapes.length; i++) {\n\t\t\t\tlet shape = state.shapes[shapes[i]];\n\t\t\t\t\n\t\t\t\tif (!shape) {\n\t\t\t\t\tcontinue;\n\t\t\t\t}\n\n\t\t\t\tif (shape.type === \"triangle\") {\n\t\t\t\t\tfn(0, [shape.tris[0].output[0]])\n\t\t\t\t\tfn(1, [shape.tris[0].output[1]])\n\t\t\t\t\tfn(2, [shape.tris[0].output[2]])\n\t\t\t\t} else if (shape.type === \"quad\") {\n\t\t\t\t\tfn(0, [shape.tris[0].output[0], shape.tris[1].output[0]])\n\t\t\t\t\tfn(1, [shape.tris[0].output[1]])\n\t\t\t\t\tfn(2, [shape.tris[0].output[2], shape.tris[1].output[1]])\n\t\t\t\t\tfn(3, [shape.tris[1].output[2]])\n\t\t\t\t}\n\t\t\t}\n\t\t}\n\n\t\tconst createShape = function(inputs, outputs, opacity) {\n\t\t\topacity = opacity || 0;\n\n\t\t\tif (!Array.isArray(inputs) || !Array.isArray(outputs)) {\n\t\t\t\treturn\n\t\t\t}\n\n\t\t\tif (inputs.length !== outputs.length) {\n\t\t\t\treturn \n\t\t\t}\n\n\t\t\tfor (let i=0; i<inputs.length; i++) {\n\t\t\t\tinputs[i][0] = Math.max(0, Math.min(1, inputs[i][0]))\n\t\t\t\tinputs[i][1] = Math.max(0, Math.min(1, inputs[i][1]))\n\t\t\t\toutputs[i][0] = Math.max(0, Math.min(1, outputs[i][0]))\n\t\t\t\toutputs[i][1] = Math.max(0, Math.min(1, outputs[i][1]))\n\t\t\t}\n\n\t\t\tlet shape = {\n\t\t\t\tid: \"\",\n        type: inputs.length === 3 ? 'triangle' : 'quad',\n        label: \"\",\n        opacity: new Array(state.videos.length).fill(opacity),\n\t\t\t};\n\n\t\t\tif (inputs.length === 3) {\n\t\t\t\tshape.tris = [\t\n\t\t\t\t\t{input: [\n\t\t\t\t\t\t[inputs[0][0], inputs[0][1]],\n\t\t\t\t\t\t[inputs[1][0], inputs[1][1]],\n\t\t\t\t\t\t[inputs[2][0], inputs[2][1]],\n\t\t\t\t\t],\n\t\t\t\t\toutput: [\n\t\t\t\t\t\t[outputs[0][0], outputs[0][1]],\n\t\t\t\t\t\t[outputs[1][0], outputs[1][1]],\n\t\t\t\t\t\t[outputs[2][0], outputs[2][1]],\n\t\t\t\t\t]}\n\t\t\t\t]\n\t\t\t} else if (inputs.length === 4) {\n\t\t\t\tshape.tris = [\n\t\t\t\t\t{input: [\n\t\t\t\t\t\t[inputs[0][0], inputs[0][1]],\n\t\t\t\t\t\t[inputs[1][0], inputs[1][1]],\n\t\t\t\t\t\t[inputs[2][0], inputs[2][1]],\n\t\t\t\t\t],\n\t\t\t\t\toutput: [\n\t\t\t\t\t\t[outputs[0][0], outputs[0][1]],\n\t\t\t\t\t\t[outputs[1][0], outputs[1][1]],\n\t\t\t\t\t\t[outputs[2][0], outputs[2][1]],\n\t\t\t\t\t]},\n\t\t\t\t\t{input: [\n\t\t\t\t\t\t[inputs[0][0], inputs[0][1]],\n\t\t\t\t\t\t[inputs[2][0], inputs[2][1]],\n\t\t\t\t\t\t[inputs[3][0], inputs[3][1]],\n\t\t\t\t\t],\n\t\t\t\t\toutput: [\n\t\t\t\t\t\t[outputs[0][0], outputs[0][1]],\n\t\t\t\t\t\t[outputs[2][0], outputs[2][1]],\n\t\t\t\t\t\t[outputs[3][0], outputs[3][1]],\n\t\t\t\t\t]}\n\t\t\t\t]\n\t\t\t} else {\n\t\t\t\treturn\n\t\t\t}\n\n\t\t\tstate.shapes.push(shape);\n\n\t\t\treturn shape;\n\t\t}\n\n\t\t${script}\n\n\t\treturn lfos;\n\t`;\n};\n\n//# sourceURL=webpack://sensorycontrols/./src/js/script-template.js?");

/***/ }),

/***/ "./src/js/script.js":
/*!**************************!*\
  !*** ./src/js/script.js ***!
  \**************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   Script: () => (/* binding */ Script)\n/* harmony export */ });\n/* harmony import */ var building_blocks__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! building-blocks */ \"./node_modules/building-blocks/index.js\");\n\nclass Script extends building_blocks__WEBPACK_IMPORTED_MODULE_0__.ContextBlocks {\n  constructor(idx, defaults) {\n    super({\n      id: `${idx}`,\n      type: \"script\",\n      label: defaults?.label || `Script ${idx + 1}`,\n      disabled: defaults?.disabled || false,\n      values: defaults?.values || [0, 0],\n      code: defaults?.code || ``\n    });\n  }\n}\n\n//# sourceURL=webpack://sensorycontrols/./src/js/script.js?");

/***/ }),

/***/ "./src/js/shape.js":
/*!*************************!*\
  !*** ./src/js/shape.js ***!
  \*************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   DefaultQuad: () => (/* binding */ DefaultQuad),\n/* harmony export */   DefaultTriangle: () => (/* binding */ DefaultTriangle),\n/* harmony export */   Shape: () => (/* binding */ Shape)\n/* harmony export */ });\n/* harmony import */ var building_blocks__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! building-blocks */ \"./node_modules/building-blocks/index.js\");\n\nconst gen_id = () => {\n  return Math.random() * 1000000 | 0;\n};\nconst DefaultTriangle = () => {\n  return [{\n    input: [[0.4, 0.4], [0.6, 0.4], [0.5, 0.6]],\n    output: [[0.4, 0.4], [0.6, 0.4], [0.5, 0.6]]\n  }];\n};\nconst DefaultQuad = () => {\n  return [{\n    input: [[0, 0], [1, 0], [1, 1]],\n    output: [[0, 0], [1, 0], [1, 1]]\n  }, {\n    input: [[0, 0], [1, 1], [0, 1]],\n    output: [[0, 0], [1, 1], [0, 1]]\n  }];\n};\nlet quadMap = [[0, 0], [1, null], [2, 1], [null, 2]];\nclass Shape extends building_blocks__WEBPACK_IMPORTED_MODULE_0__.ContextBlocks {\n  constructor(defaults) {\n    let type = defaults?.type || \"quad\";\n    let tris = defaults?.tris || (type === \"quad\" ? DefaultQuad() : DefaultTriangle());\n    super({\n      id: defaults?.id || gen_id(),\n      type: defaults?.type || \"quad\",\n      label: defaults?.label || \"\",\n      opacity: defaults?.opacity || new Array(6).fill(0),\n      tris: tris\n    });\n  }\n  setLabel(label) {\n    this.label = label;\n  }\n  setPoint(idx, layer, x, y) {\n    if (this.type === \"tri\") {\n      this.tris[0][layer][idx][0] = x;\n      this.tris[0][layer][idx][1] = y;\n    }\n    if (this.type === \"quad\") {\n      let mapping0 = quadMap[idx];\n      let mapping1 = quadMap[idx];\n      if (mapping0 !== null) {\n        this.tris[0][layer][mapping0][0] = x;\n        this.tris[0][layer][mapping0][1] = y;\n      }\n      if (mapping1 !== null) {\n        this.tris[1][layer][mapping1][0] = x;\n        this.tris[1][layer][mapping1][1] = y;\n      }\n    }\n    this.tris = [...this.tris];\n  }\n}\n\n//# sourceURL=webpack://sensorycontrols/./src/js/shape.js?");

/***/ }),

/***/ "./src/js/slot.js":
/*!************************!*\
  !*** ./src/js/slot.js ***!
  \************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   Slot: () => (/* binding */ Slot)\n/* harmony export */ });\n/* harmony import */ var building_blocks__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! building-blocks */ \"./node_modules/building-blocks/index.js\");\n\nclass Slot extends building_blocks__WEBPACK_IMPORTED_MODULE_0__.ContextBlocks {\n  constructor(idx) {\n    super({\n      id: `${idx}`,\n      target: null\n    });\n  }\n}\n\n//# sourceURL=webpack://sensorycontrols/./src/js/slot.js?");

/***/ }),

/***/ "./src/js/state.js":
/*!*************************!*\
  !*** ./src/js/state.js ***!
  \*************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   State: () => (/* binding */ State)\n/* harmony export */ });\n/* harmony import */ var building_blocks__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! building-blocks */ \"./node_modules/building-blocks/index.js\");\n/* harmony import */ var _effects__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./effects */ \"./src/js/effects.js\");\n/* harmony import */ var _script__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./script */ \"./src/js/script.js\");\n/* harmony import */ var _effect__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./effect */ \"./src/js/effect.js\");\n/* harmony import */ var _slot__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./slot */ \"./src/js/slot.js\");\n/* harmony import */ var _video__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./video */ \"./src/js/video.js\");\n/* harmony import */ var _shape__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./shape */ \"./src/js/shape.js\");\n/* harmony import */ var _utils__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./utils */ \"./src/js/utils.js\");\n\n\n\n\n\n\n\n\nconst sync_chain = [\"sync_start\", \"sync_state\", \"sync_videos\", \"sync_end\"];\n\n/**\n * Class for storing and syncing the application state across\n * all instances of the state object including in other windows\n * through the use of a SharedWorker object\n */\nclass State extends building_blocks__WEBPACK_IMPORTED_MODULE_0__.ContextBlocks {\n  constructor(defaults) {\n    super({\n      loading: null,\n      files: new Array(6).fill(null),\n      selected: {\n        video: null,\n        slot: null\n      },\n      slots: new Array(12).fill().map((_, idx) => new _slot__WEBPACK_IMPORTED_MODULE_4__.Slot(idx)),\n      scripts: new Array(12).fill().map((_, idx) => new _script__WEBPACK_IMPORTED_MODULE_2__.Script(idx)),\n      effects: _effects__WEBPACK_IMPORTED_MODULE_1__.Effects.map(e => new _effect__WEBPACK_IMPORTED_MODULE_3__.Effect(e)),\n      videos: new Array(6).fill().map((_, idx) => new _video__WEBPACK_IMPORTED_MODULE_5__.Video(idx)),\n      shapes: [new _shape__WEBPACK_IMPORTED_MODULE_6__.Shape({\n        label: \"Quad 1\",\n        type: \"quad\"\n      })]\n    });\n\n    // For syncing\n    this.loading_time = 0;\n    this.loading_index = 0;\n\n    // Set these to null initially\n    this.worker = null;\n    this.port = null;\n\n    // Should be unlocked initially\n    this.locked = false;\n\n    // If we're in the shared worker, leave the worker and port\n    // set to null. Otherwise register this state with the\n    // shared worker\n    if (typeof window !== \"undefined\") {\n      this.worker = new SharedWorker(\"./js/bridge.js\");\n      this.port = this.worker.port;\n      this.port.addEventListener(\"message\", e => {\n        this.locked = true;\n        if (typeof e.data !== \"object\") {\n          let data = JSON.parse(e.data);\n          let {\n            action\n          } = data;\n          if (sync_chain.includes(action)) {\n            return this.handleSync(data);\n          } else {\n            this.handleUpdate(data);\n          }\n        } else {\n          this.files = this.files.toSpliced(this.loading, 1, e.data);\n        }\n        this.locked = false;\n      });\n      this.port.start();\n      this.port.postMessage(JSON.stringify({\n        action: \"sync_start\"\n      }));\n    }\n\n    // If the files have changed, broadcast the new file\n    // so long as the loading slot has been set\n    this.listen(\"files\", (files, oldFiles) => {\n      if (this.loading !== null && !this.locked && !!this.port) {\n        this.port.postMessage(files[this.loading]);\n        this.loading = null;\n      } else {\n        let newMediaIdx = files.findIndex((file, idx) => {\n          return file !== null && file.kind === \"videoinput\" && file.deviceId !== oldFiles[idx]?.deviceId;\n        });\n        this.post(\"update_media\", {\n          media: files[newMediaIdx]\n        }, {\n          idx: newMediaIdx\n        });\n      }\n    });\n\n    // Set the loading slot to prepare for a video\n    // being loaded\n    this.listen(\"loading\", loading => {\n      this.post(\"update_state\", {\n        loading\n      });\n    });\n\n    // Update whatever is selected\n    this.listen(\"selected\", selected => {\n      this.post(\"update_state\", {\n        selected\n      });\n    });\n    this.listen(\"shapes\", shapes => {\n      this.post(\"update_shapes\", {\n        shapes\n      });\n    });\n\n    // Add events to the script objects\n    this.scripts.map((script, idx) => {\n      this.addScriptEvents(script, idx);\n    });\n\n    // Add events to the effect object\n    this.effects.map((effect, idx) => {\n      this.addEffectEvents(effect, idx);\n    });\n\n    // Add events to the video objects\n    this.videos.map((video, idx) => {\n      this.addVideoEvents(video, idx);\n    });\n\n    // Add events to the initial shape objects\n    this.shapes.map((shape, idx) => {\n      this.addShapeEvents(shape, idx);\n    });\n\n    // Add events to the slot objects\n    this.slots.map((slot, idx) => {\n      this.addSlotEvents(slot, idx);\n    });\n  }\n\n  // Broadcasts updates to the shared worker\n  post(action, updates, data) {\n    if (this.locked || !this.port) {\n      return;\n    }\n    this.port.postMessage(JSON.stringify({\n      action,\n      updates,\n      data\n    }));\n  }\n\n  // Adds a shape, and attaches events\n  addShape(type) {\n    let shape = new _shape__WEBPACK_IMPORTED_MODULE_6__.Shape({\n      label: `${(0,_utils__WEBPACK_IMPORTED_MODULE_7__.capitalize)(type)} ${this.shapes.length + 1}`,\n      opacity: this.videos.map(v => v.opacity),\n      type\n    });\n    let idx = this.shapes.length;\n    this.addShapeEvents(shape, idx);\n    this.shapes = [...this.shapes, shape];\n    return shape;\n  }\n\n  // Remove Shape\n  removeShape(shape) {\n    this.shapes = this.shapes.filter(s => s.id !== shape.id);\n  }\n\n  // Add broadcast events to a shape\n  addShapeEvents(shape, idx) {\n    shape.listen(\"label\", label => {\n      this.post(\"update_shape\", {\n        label\n      }, {\n        idx\n      });\n    });\n    shape.listen(\"opacity\", opacity => {\n      this.post(\"update_shape\", {\n        opacity\n      }, {\n        idx\n      });\n    });\n    shape.listen(\"tris\", tris => {\n      this.post(\"update_shape\", {\n        tris\n      }, {\n        idx\n      });\n    });\n  }\n\n  // Adds broadcast events to a video\n  addVideoEvents(video, idx) {\n    video.listen(\"label\", label => {\n      this.post(\"update_video\", {\n        label\n      }, {\n        idx\n      });\n    });\n    video.listen(\"opacity\", opacity => {\n      this.post(\"update_video\", {\n        opacity\n      }, {\n        idx\n      });\n    });\n  }\n\n  // Adds broadcast events to an effect\n  addEffectEvents(effect, idx) {\n    effect.listen(\"label\", label => {\n      this.post(\"update_effect\", {\n        label\n      }, {\n        idx\n      });\n    });\n    effect.listen(\"code\", code => {\n      this.post(\"update_effect\", {\n        code\n      }, {\n        idx\n      });\n    });\n    effect.listen(\"values\", values => {\n      this.post(\"update_effect\", {\n        values\n      }, {\n        idx\n      });\n    });\n  }\n\n  // Adds broadcast events to a script\n  addScriptEvents(script, idx) {\n    script.listen(\"label\", label => {\n      this.post(\"update_script\", {\n        label\n      }, {\n        idx\n      });\n    });\n    script.listen(\"code\", code => {\n      this.post(\"update_script\", {\n        code\n      }, {\n        idx\n      });\n    });\n    script.listen(\"values\", values => {\n      this.post(\"update_script\", {\n        values\n      }, {\n        idx\n      });\n    });\n    script.listen(\"disabled\", disabled => this.post(\"update_script\", {\n      disabled\n    }, {\n      idx\n    }));\n  }\n\n  // Adds broadcast events to a slot\n  addSlotEvents(slot, idx) {\n    slot.listen(\"target\", target => {\n      this.post(\"update_slot\", {\n        target\n      }, {\n        idx\n      });\n    });\n  }\n\n  // Updates this state's values from an update event\n  handleUpdate(msg) {\n    let {\n      action,\n      updates,\n      data\n    } = msg;\n    if (action === \"update_shapes\") {\n      return this.handleShapeUpdates(msg);\n    }\n    let target = null;\n    if (action === \"update_state\") {\n      target = this;\n    }\n    if (action === \"update_script\") {\n      target = this.scripts[data.idx];\n    }\n    if (action === \"update_effect\") {\n      target = this.effects[data.idx];\n    }\n    if (action === \"update_video\") {\n      target = this.videos[data.idx];\n    }\n    if (action === \"update_shape\") {\n      target = this.shapes[data.idx];\n    }\n    if (action === \"update_slot\") {\n      target = this.slots[data.idx];\n    }\n    if (action === \"update_media\") {\n      target = this;\n    }\n    for (let x in updates) {\n      // Special case for updating the slot target, since that\n      // should be a class instance\n      if (action === \"update_slot\" && x === \"target\") {\n        if (updates[x].type === \"effect\") {\n          target[x] = this.effects.find(e => e.id === updates[x].id);\n        }\n        if (updates[x].type === \"script\") {\n          target[x] = this.scripts[data.idx];\n        }\n      } else if (action === \"update_media\") {\n        target.files = this.files.toSpliced(data.idx, 1, updates.media);\n      } else {\n        target[x] = updates[x];\n      }\n    }\n  }\n  handleShapeUpdates(msg) {\n    let {\n      action,\n      updates,\n      data\n    } = msg;\n    let {\n      shapes\n    } = updates;\n    let deletes = this.shapes.filter(s => !shapes.find(sh => sh.id === s.id));\n    let additions = shapes.filter(s => !this.shapes.find(sh => sh.id === s.id)).map(s => {\n      let shape = new _shape__WEBPACK_IMPORTED_MODULE_6__.Shape(s);\n      this.addShapeEvents(shape);\n      return shape;\n    });\n    this.shapes = [...this.shapes.filter(s => !deletes.find(sh => sh.id == s.id)), ...additions];\n  }\n  handleSync(msg) {\n    let {\n      action,\n      updates,\n      data\n    } = msg;\n    if (action === \"sync_start\") {\n      this.locked = false;\n      this.post(\"sync_state\");\n      this.locked = true;\n    }\n    if (action === \"sync_state\") {\n      this.handleStateSync(msg);\n      this.locked = false;\n      this.post(\"sync_videos\", {\n        idx: 0\n      });\n      this.locked = true;\n    }\n    if (action === \"sync_videos\") {\n      this.loading_index = this.loading_index + 1;\n      if (this.loading_index < 6) {\n        this.locked = false;\n        this.post(\"sync_videos\", {\n          idx: this.loading_index\n        });\n        this.locked = true;\n      } else {\n        this.locked = false;\n        this.post(\"sync_end\");\n        this.locked = true;\n      }\n    }\n    if (action === \"sync_end\") {\n      this.loading_index = 0;\n      this.loading_time = 0;\n      this.loading = null;\n      this.locked = false;\n    }\n  }\n  handleStateSync(msg) {\n    let {\n      action,\n      updates,\n      data\n    } = msg;\n    let {\n      loading,\n      selected,\n      scripts,\n      effects,\n      videos,\n      shapes,\n      slots\n    } = updates;\n    this.loading = loading;\n    this.selected = {\n      ...selected\n    };\n    this.videos = videos.map((v, idx) => {\n      let video = this.videos[idx];\n      video.label = v.label;\n      video.opacity = v.opacity;\n      video.code = v.code;\n      return video;\n    });\n    this.shapes = shapes.map((s, idx) => {\n      let shape = this.shapes[idx];\n      if (!shape) {\n        shape = new _shape__WEBPACK_IMPORTED_MODULE_6__.Shape(shape);\n        this.addShapeEvents(shape);\n      }\n      shape.id = s.id;\n      shape.type = s.type;\n      shape.label = s.label;\n      shape.opacity = s.opacity;\n      shape.tris = s.tris;\n      return shape;\n    });\n    this.scripts = scripts.map((s, idx) => {\n      let script = this.scripts[idx];\n      script.type = s.type;\n      script.label = s.label;\n      script.disabled = s.disabled;\n      script.values = s.values;\n      script.code = s.code;\n      return script;\n    });\n    this.effects = effects.map((e, idx) => {\n      let effect = this.effects.find(fx => fx.id === e.id);\n      effect.type = e.type;\n      effect.label = e.label;\n      effect.values = e.values;\n      effect.code = e.code;\n      return effect;\n    });\n    this.slots = slots.map((s, idx) => {\n      let slot = this.slots[idx];\n      if (s.target === null) {\n        slot.target = null;\n      }\n      if (s.target?.type === \"script\") {\n        slot.target = this.scripts.find(script => script.id === s.target?.id);\n      }\n      if (s.target?.type === \"effect\") {\n        slot.target = this.effects.find(effect => effect.id === s.target?.id);\n      }\n      return slot;\n    });\n  }\n}\n\n//# sourceURL=webpack://sensorycontrols/./src/js/state.js?");

/***/ }),

/***/ "./src/js/storage.js":
/*!***************************!*\
  !*** ./src/js/storage.js ***!
  \***************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   storage: () => (/* binding */ storage)\n/* harmony export */ });\n/* harmony import */ var building_blocks__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! building-blocks */ \"./node_modules/building-blocks/index.js\");\n\nconst keys = [\"scripts\", \"effects\", \"shaders\", \"projects\"];\nclass StorageManager extends building_blocks__WEBPACK_IMPORTED_MODULE_0__.ContextBlocks {\n  constructor() {\n    super({\n      scripts: [],\n      effects: [],\n      shaders: [],\n      projects: [],\n      ready: false\n    });\n    this.store = null;\n    const request = window.indexedDB.open(\"SensoryControls\", 3);\n    request.onerror = event => {\n      console.warn(\"Error opening IndexDB\", event);\n    };\n    request.onsuccess = event => {\n      this.store = event.target.result;\n      this.ready = true;\n      this.init();\n    };\n    request.onupgradeneeded = event => {\n      this.store = event.target.result;\n      let keys = [\"scripts\", \"effects\", \"shaders\", \"projects\"];\n      keys.map(key => {\n        let objectStore = this.store.createObjectStore(key, {\n          keyPath: \"id\"\n        });\n      });\n      this.ready = true;\n      this.init();\n    };\n  }\n  async init() {\n    this.scripts = await this.get(\"scripts\");\n    this.effects = await this.get(\"effects\");\n    this.shaders = await this.get(\"shaders\");\n    this.projects = await this.get(\"projects\");\n  }\n  async get(key) {\n    if (!keys.includes(key)) {\n      return console.warn(\"Unknown storage key:\", key);\n    }\n    const request = this.store.transaction([key], \"readwrite\").objectStore(key).getAll();\n    return new Promise((res, rej) => {\n      request.onsuccess = event => {\n        res(event.target.result);\n        // event.target.result === customer.ssn;\n      };\n    });\n  }\n  async getItem(key, id) {\n    const request = this.store.transaction([key], \"readwrite\").objectStore(key).get(id);\n    return new Promise((res, rej) => {\n      request.onsuccess = event => {\n        res(event.target.result);\n      };\n    });\n  }\n  async upsertItem(key, obj) {\n    const request = this.store.transaction([key], \"readwrite\").objectStore(key).put(obj);\n    return new Promise(async (res, rej) => {\n      request.onsuccess = async event => {\n        this[key] = await this.get(key);\n        res();\n      };\n    });\n  }\n  async removeItem(key, obj) {\n    const request = this.store.transaction([key], \"readwrite\").objectStore(key).delete(obj.id);\n    return new Promise(async (res, rej) => {\n      request.onsuccess = async event => {\n        this[key] = await this.get(key);\n        res();\n      };\n    });\n  }\n}\nconst storage = new StorageManager();\n\n//# sourceURL=webpack://sensorycontrols/./src/js/storage.js?");

/***/ }),

/***/ "./src/js/utils.js":
/*!*************************!*\
  !*** ./src/js/utils.js ***!
  \*************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   capitalize: () => (/* binding */ capitalize),\n/* harmony export */   gen_id: () => (/* binding */ gen_id)\n/* harmony export */ });\nconst gen_id = () => {\n  return Math.random() * 1000000 | 0;\n};\nconst capitalize = str => {\n  return str.charAt(0).toUpperCase() + str.slice(1);\n};\n\n//# sourceURL=webpack://sensorycontrols/./src/js/utils.js?");

/***/ }),

/***/ "./src/js/video.js":
/*!*************************!*\
  !*** ./src/js/video.js ***!
  \*************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   Video: () => (/* binding */ Video)\n/* harmony export */ });\n/* harmony import */ var building_blocks__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! building-blocks */ \"./node_modules/building-blocks/index.js\");\n\nconst gen_id = () => {\n  return Math.random() * 1000000 | 0;\n};\n\n// 'handle' is for storing a FileSystemFileHandle obj, so that\n// the videos can be saved with the projects and loaded again\n// later. Pretty cool\n\nclass Video extends building_blocks__WEBPACK_IMPORTED_MODULE_0__.ContextBlocks {\n  constructor(idx, defaults) {\n    super({\n      id: gen_id(),\n      handle: null,\n      loaded: false,\n      label: defaults?.label || `Video ${idx + 1}`,\n      opacity: defaults?.opacity || 0,\n      code: defaults?.code || ``\n    });\n  }\n}\n\n//# sourceURL=webpack://sensorycontrols/./src/js/video.js?");

/***/ }),

/***/ "./src/main.js":
/*!*********************!*\
  !*** ./src/main.js ***!
  \*********************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var _components_main_layout_html__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./components/main-layout.html */ \"./src/components/main-layout.html\");\n/* harmony import */ var _components_video_slot_html__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./components/video-slot.html */ \"./src/components/video-slot.html\");\n/* harmony import */ var _components_vertical_range_html__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./components/vertical-range.html */ \"./src/components/vertical-range.html\");\n/* harmony import */ var _components_effect_wheel_html__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./components/effect-wheel.html */ \"./src/components/effect-wheel.html\");\n/* harmony import */ var _components_effect_slot_html__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./components/effect-slot.html */ \"./src/components/effect-slot.html\");\n/* harmony import */ var _components_output_screen_html__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./components/output-screen.html */ \"./src/components/output-screen.html\");\n/* harmony import */ var _components_side_menu_html__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./components/side-menu.html */ \"./src/components/side-menu.html\");\n/* harmony import */ var _components_main_menu_html__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./components/main-menu.html */ \"./src/components/main-menu.html\");\n/* harmony import */ var _components_script_editor_html__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ./components/script-editor.html */ \"./src/components/script-editor.html\");\n/* harmony import */ var _components_api_docs_html__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./components/api-docs.html */ \"./src/components/api-docs.html\");\n/* harmony import */ var _components_confirmation_modal_html__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ./components/confirmation-modal.html */ \"./src/components/confirmation-modal.html\");\n\n\n\n\n\n\n\n\n\n\n\n\n//# sourceURL=webpack://sensorycontrols/./src/main.js?");

/***/ }),

/***/ "./node_modules/building-blocks/index.js":
/*!***********************************************!*\
  !*** ./node_modules/building-blocks/index.js ***!
  \***********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   BuildingBlocks: () => (/* binding */ BuildingBlocks),\n/* harmony export */   ContextBlocks: () => (/* binding */ ContextBlocks)\n/* harmony export */ });\n// Just in case this is getting loaded in a context without HTMLElement\nlet BuildingBlocks\n\nif (typeof HTMLElement !== \"undefined\") {\n  BuildingBlocks = class BuildingBlocks extends HTMLElement {\n    constructor() {\n      super();\n\n      this.__template__ = null;\n      this.__connected__ = false;\n      this.__children__ = [];\n\n      // Context Variables\n      this.__bindings__ = [];\n      this.__childcontexts__ = {};\n      this.__observed__ = {};\n      this.__values__ = {};\n      this.__current_target__ = null;\n    }\n\n    static get observedProperties() {\n      return [];\n    }\n\n    listen(key, fn) {\n      const uuid = () => {\n        return \"10000000-1000-4000-8000-100000000000\".replace(/[018]/g, (c) =>\n          (\n            +c ^\n            (crypto.getRandomValues(new Uint8Array(1))[0] & (15 >> (+c / 4)))\n          ).toString(16),\n        );\n      };\n\n      let handle = uuid();\n      if (this.__observed__.hasOwnProperty(key)) {\n        let observers = this.__observed__[key];\n        let idx = observers.findIndex((o) => o.el === this && o.attr === handle);\n\n        let observer = {\n          el: this,\n          attr: handle,\n          hidden: true,\n          snippet: \"\",\n          context: this,\n          bindings: [\n            (context, values) => {\n              return fn(values[key]);\n            },\n          ],\n        };\n\n        if (idx === -1) {\n          observers.push(observer);\n        } else {\n          observers.splice(idx, 1, observer);\n        }\n      }\n    }\n\n    observe(prop, ctx) {\n      ctx = ctx || this;\n\n      ctx.__observed__[prop] = [];\n      ctx.__values__[prop] = ctx[prop];\n\n      Object.defineProperty(ctx, prop, {\n        get: () => {\n          if (\n            ctx.__current_target__ !== null &&\n            ctx.__observed__.hasOwnProperty(prop)\n          ) {\n            let { el, attr, snippet, context, bindings } = ctx.__current_target__;\n\n            let observers = ctx.__observed__[prop];\n            let idx = observers.findIndex((o) => o.el === el && o.attr === attr);\n\n            if (idx === -1) {\n              observers.push({ ...ctx.__current_target__ });\n            } else {\n              observers.splice(idx, 1, { ...ctx.__current_target__ });\n            }\n          }\n\n          return ctx.__values__[prop];\n        },\n        set: (value) => {\n          if (value !== ctx.__values__[prop]) {\n            ctx.__values__[prop] = value;\n\n            let observers = ctx.__observed__[prop] || [];\n            let len = observers.length;\n\n            for (var i = 0; i < len; i++) {\n              let { el, attr, hidden, snippet, context, bindings } = observers[i];\n              let values = context.__get_values__(ctx);\n\n              for (let i = 0; i < bindings.length; i++) {\n                snippet = bindings[i](snippet, values, attr);\n              }\n\n              if (typeof snippet === \"function\") {\n                el[attr.replace(\":\", \"\")] = snippet.bind(this);\n              } else {\n                el[attr.replace(\":\", \"\")] = snippet;\n              }\n\n              if (![\"array\", \"object\", \"function\"].includes(typeof snippet)) {\n                if (!hidden && !!el.setAttribute) {\n                  el.setAttribute(attr.replace(\":\", \"\"), snippet);\n                }\n              }\n            }\n          }\n        },\n      });\n    }\n\n    connectedCallback() {\n      const name = this.tagName.toLowerCase();\n\n      if (!this.__connected__) {\n        this.__connected__ = true;\n\n        this.constructor.observedProperties.map((prop) => {\n          this.observe(prop, this);\n        });\n\n        this.__children__ = [...this.children];\n        let template = document.createDocumentFragment();\n        if (!!this.__template__) {\n          template = this.__template__.content.cloneNode(true);\n          this.__parseTemplate__(template, this);\n        }\n\n        this.__render__(this);\n\n        this.appendChild(template);\n\n        if (!!this.connected) {\n          this.connected();\n        }\n      }\n    }\n\n    disconnectedCallback() {\n      if (!!this.disconnected) {\n        this.disconnected();\n      }\n    }\n\n    __parseTemplate__(template, ctx) {\n      const eventExp = /\\@([a-z|A-Z]+)/;\n      const attrExp = /\\:([a-z|A-Z]+)/;\n      const boundExp = /\\{\\{(.*?)\\}\\}/g;\n\n      const getBinding = (el, attr, ctx) => {\n        let binding = ctx.__bindings__.find((b) => b.el === el);\n\n        if (!binding) {\n          binding = {\n            el: el,\n            attrs: {},\n          };\n\n          ctx.__bindings__.push(binding);\n        }\n\n        if (!binding.attrs[attr]) {\n          let snippet;\n          if (/template\\-[0-9]+/i.test(attr)) {\n            let templateIdx = +attr.split(\"-\")[1];\n            snippet = el.querySelectorAll(\"template\")[templateIdx];\n          } else {\n            snippet = el[attr];\n            if (!!el.getAttribute) {\n              snippet = el.getAttribute(attr);\n            }\n          }\n\n          binding.attrs[attr] = {\n            snippet: snippet,\n            bindings: [],\n            previous: {},\n          };\n        }\n\n        return binding;\n      };\n\n      const bindValue = (el, attr, match, ctx) => {\n        let binding = getBinding(el, attr, ctx);\n\n        if (attr === \"textContent\") {\n          let fn = new Function(\n            \"scope\",\n            `\n            with(scope) {\n              return ${match[1]}\n            }`,\n          );\n\n          binding.attrs[attr].bindings.push((snippet, values) => {\n            return snippet.replace(match[0], fn(values));\n          });\n        }\n      };\n\n      const bindAttribute = (el, attr, context, ctx) => {\n        let binding = getBinding(el, attr, ctx);\n\n        let fn = new Function(\n          \"scope\",\n          `\n          with(scope) {\n            return ${context};\n          }`,\n        );\n\n        binding.attrs[attr].bindings.push((snippet, values) => {\n          return fn(values);\n        });\n      };\n\n      const bindEvent = (el, attr, context, ctx) => {\n        let fn = new Function(\n          \"scope\",\n          `         \n          with(scope) {\n            return ${context}\n          }`,\n        );\n\n        el.addEventListener(attr.replace(\"@\", \"\"), (e) => {\n          let values = ctx.__get_values__(ctx);\n\n          values[\"$event\"] = e;\n          fn(values);\n        });\n      };\n\n      const createTemplateContext = (oCtx, idx, key, value) => {\n        let values = {};\n\n        let ctx = {\n          __id__: (Math.random() * 10000000) | 0,\n          __bindings__: [],\n          __observed__: {},\n          __childbindings__: [],\n          __childcontexts__: {},\n          __values__: values,\n          __template__: null,\n          __parent__: oCtx,\n          __current_target__: null,\n\n          __get_values__: () => {\n            return this.__get_values__(ctx);\n          },\n        };\n\n        for (var key in oCtx.__observed__) {\n          ctx[key] = oCtx[key];\n          this.observe(key, ctx);\n        }\n\n        ctx[\"$idx\"] = idx;\n        this.observe(\"$idx\", ctx);\n        ctx[\"$key\"] = key;\n        this.observe(\"$key\", ctx);\n        ctx[\"$value\"] = value;\n        this.observe(\"$value\", ctx);\n\n        // console.log(ctx)\n\n        return ctx;\n      };\n\n      const updateTemplateContext = (ctx, oCtx) => {\n        for (var key in oCtx.__observed__) {\n          ctx[key] = oCtx[key];\n        }\n      };\n\n      const bindTemplate = (el, oCtx) => {\n        let templateIdx = [...el.parentNode.querySelectorAll(\"template\")].indexOf(\n          el,\n        );\n\n        let binding = getBinding(el.parentNode, `template-${templateIdx}`, oCtx);\n\n        let type = el.hasAttribute(\":for\") ? \"loop\" : \"if\";\n        if (type === \"loop\") {\n          let iteratorFn = new Function(\n            `scope`,\n            `with(scope) {\n              return ${el.getAttribute(\":for\")}\n            }`,\n          );\n\n          let idSnippet = el.getAttribute(\":id\");\n          if (!idSnippet) {\n            console.warn(\n              \"For loop templates must have an :id, index is being used as a default but that could result in unnecessary reredeners\",\n            );\n            idSnippet = \"$idx\";\n          }\n\n          let idFunction = new Function(\n            `scope`,\n            `with(scope) {\n              return ${idSnippet}\n            }`,\n          );\n\n          // Create pass through observers\n          for (let key in oCtx.__observed__) {\n            oCtx.__observed__[key].push({\n              el: el,\n              snippet: null,\n              attr: `for-loop-${templateIdx}`,\n              hidden: true,\n              context: oCtx,\n              bindings: [\n                (snippet, values) => {\n                  for (let x in oCtx.__childcontexts__) {\n                    oCtx.__childcontexts__[x][key] = values[key];\n                  }\n                },\n              ],\n            });\n          }\n\n          let fn = (snippet, values, attr) => {\n            // Get the iterator value;\n            let iterator = iteratorFn(values);\n\n            // Create a document fagment to append items to\n            let frag = document.createDocumentFragment();\n\n            // Remove all any elements created from a previous call\n            let previous = binding.attrs[attr].previous;\n            let previousIds = Object.keys(previous);\n            let deletedIds = Object.keys(previous);\n\n            if (!!iterator) {\n              let idx = 0;\n              // let child_bindings = {};\n              let child_contexts = [];\n              for (let key in iterator) {\n                // Create a new context, evaluate the idFunction with it\n                let ctx = createTemplateContext(oCtx, idx, key, iterator[key]);\n                let ctxValues = ctx.__get_values__(ctx);\n                let id = idFunction(ctxValues).toString();\n\n                child_contexts.push(ctx);\n                if (previousIds.includes(id)) {\n                  updateTemplateContext(previous[id].ctx, ctx);\n                  deletedIds = deletedIds.filter((_id) => _id !== id);\n                  idx++;\n\n                  continue;\n                }\n\n                oCtx.__childcontexts__[`${templateIdx}-${id}`] = ctx;\n\n                // Clone the template\n                let created = [];\n                let item = el.content.cloneNode(true);\n\n                // Parse the template\n                walk(item, ctx, true);\n\n                // Render the context\n                this.__render__(ctx, true);\n\n                // Keep track of created items\n                created.push(...item.childNodes);\n\n                // append then render\n                frag.append(item);\n\n                // Store the created elements and context under the id\n                previous[id] = {\n                  elements: created,\n                  ctx: ctx,\n                };\n                // increment the index\n                idx++;\n              }\n            }\n\n            // Set the created elements to be removed on the next render\n            for (let i = 0; i < deletedIds.length; i++) {\n              let id = deletedIds[i];\n              let doomed = previous[id].elements;\n              for (let j = 0; j < doomed.length; j++) {\n                doomed[j].remove();\n              }\n\n              delete oCtx.__childcontexts__[`${templateIdx}-${id}`];\n              delete previous[id];\n            }\n\n            // If this the template is being rendered create bindings on the parent scope\n            el.parentNode.insertBefore(frag, el);\n          };\n\n          binding.attrs[`template-${templateIdx}`].bindings.push(fn);\n        } else if (type === \"if\") {\n          let conditionalFn = new Function(\n            `scope`,\n            `with(scope) {\n              return ${el.getAttribute(\":if\")}\n            }`,\n          );\n\n          // Create pass through observers\n          for (let key in oCtx.__observed__) {\n            oCtx.__observed__[key].push({\n              el: el,\n              snippet: null,\n              attr: `if-block-${templateIdx}`,\n              hidden: true,\n              context: oCtx,\n              bindings: [\n                (snippet, values, attr) => {\n                  for (let x in oCtx.__childcontexts__) {\n                    oCtx.__childcontexts__[x][key] = values[key];\n                  }\n                },\n              ],\n            });\n          }\n\n          let fn = (snippet, values, attr) => {\n            // Get the iterator value;\n            let conditional = conditionalFn(values);\n\n            // Create a document fagment to append items to\n            let frag = document.createDocumentFragment();\n\n            // Get the previously created elements and context\n            let previous = binding.attrs[attr].previous[\"if-key\"] || {\n              elements: [],\n              ctx: null,\n            };\n\n            let previouslyCreated = previous.elements;\n            let previousCtx = previous.ctx;\n\n            // Scoped variables\n            let created = [];\n            let createdCtx = null;\n            // let child_bindings = {};\n\n            // Check if the test function is truthy\n            if (conditional) {\n              // Check if there is a previously created element and context\n              if (previouslyCreated.length > 0 && previousCtx !== null) {\n                // If so, all we need to do is update the values of the\n                // previously created context with the new values\n                updateTemplateContext(previousCtx, ctx);\n                return;\n              } else {\n                // If not then we need to create the element and it's bindings\n\n                // Clone the template\n                let item = el.content.cloneNode(true);\n\n                // Create a new context\n                let ctx = createTemplateContext(oCtx, null, null, null);\n\n                oCtx.__childcontexts__[`if-block-${templateIdx}`] = ctx;\n\n                // Parse the template\n                walk(item, ctx, true);\n\n                // Render the context\n                this.__render__(ctx);\n\n                // Keep track of created items\n                created.push(...item.childNodes);\n                createdCtx = ctx;\n\n                // append then render\n                frag.append(item);\n\n                // If this the template is being rendered create bindings on the parent scope\n                el.parentNode.insertBefore(frag, el);\n              }\n            } else {\n              delete oCtx.__childcontexts__[`if-block-${templateIdx}`]\n\n              for (let i = 0; i < previouslyCreated.length; i++) {\n                previouslyCreated[i].remove();\n              }\n            }\n\n\n            // Set the created elements to be removed on the next render\n            binding.attrs[attr].previous[\"if-key\"] = {\n              elements: created,\n              ctx: createdCtx,\n            };\n          };\n\n          binding.attrs[`template-${templateIdx}`].bindings.push(fn);\n        }\n      };\n\n      const walk = (el, ctx, log) => {      \n        if (!!el.tagName && (el.tagName || \"\").toLowerCase() === \"template\") {\n          return bindTemplate(el, ctx);\n        }\n\n        let attrs = [...(el.attributes || [])];\n        let children = [...(el.childNodes || [])];\n\n        if (el.nodeType === 3) {\n          let match;\n          while ((match = boundExp.exec(el.textContent)) !== null) {\n            bindValue(el, \"textContent\", match, ctx);\n          }\n        } else {\n          for (var i = 0; i < attrs.length; i++) {\n            if (attrExp.test(attrs[i].name)) {\n              bindAttribute(el, attrs[i].name, attrs[i].value, ctx);\n              // el.setAttribute(attrs[i].name.replace(\":\", \"\"), attrs[i].value);\n            }\n\n            if (eventExp.test(attrs[i].name)) {\n              bindEvent(el, attrs[i].name, attrs[i].value, ctx);\n              // el.setAttribute(attrs[i].name);\n            }\n          }\n        }\n\n        children.map((c) => {\n          walk(c, ctx, log);\n        });\n      };\n\n      walk(template, this);\n    }\n\n    __get_values__(ctx) {\n      let values = {};\n      let methods = Object.getOwnPropertyNames(\n        Object.getPrototypeOf(Object.getPrototypeOf(this)),\n      ).filter((method) => {\n        return typeof this[method] === \"function\";\n      });\n\n      for (let i = 0; i < methods.length; i++) {\n        if (typeof this[methods[i]] === \"function\") {\n          Object.defineProperty(values, methods[i], {\n            get: () => {\n              return this[methods[i]].bind(this);\n            },\n          });\n        }\n      }\n\n      let keys = Object.keys(ctx.__observed__);\n      for (let i = 0; i < keys.length; i++) {\n        Object.defineProperty(values, keys[i], {\n          get: () => {\n            return ctx[keys[i]];\n          },\n        });\n      }\n\n      return values;\n    }\n\n    __render__(ctx, log) {\n      let values = ctx.__get_values__(ctx);\n\n      for (let i = 0; i < ctx.__bindings__.length; i++) {\n        let binding = ctx.__bindings__[i];\n        let el = binding.el;\n\n        for (let attr in binding.attrs) {\n          let { snippet, bindings } = binding.attrs[attr];\n\n          ctx.__current_target__ = { el, attr, snippet, context: ctx, bindings };\n\n          for (let i = 0; i < bindings.length; i++) {\n            snippet = bindings[i](snippet, values, attr);\n          }\n\n          if (typeof snippet === \"function\") {\n            el[attr.replace(\":\", \"\")] = snippet.bind(this);\n          } else {\n            el[attr.replace(\":\", \"\")] = snippet;\n          }\n\n          if (![\"array\", \"object\", \"function\"].includes(typeof snippet)) {\n            if (!binding.hidden && !!el.setAttribute) {\n              el.setAttribute(attr.replace(\":\", \"\"), snippet);\n            }\n          }\n        }\n      }\n\n      ctx.__current_target__ = null;\n    }\n  }\n} else {\n  BuildingBlocks = class BuildingBlocks {\n    constructor() {\n      console.warn(\"Building Blocks could ironically not be built\")\n    }\n  }\n}\n\n// Simple subscribable storage, there's better out there you should probably use them\nclass ContextBlocks {\n  constructor(obj) {\n    this.__values__ = { ...obj };\n    this.__subscriptions__ = Object.keys(obj).reduce((a, c) => {\n      a[c] = [];\n      return a;\n    }, {});\n\n    this.__handles__ = {};\n    this.__bindings__ = {};\n\n    let props = Object.keys(obj);\n    for (var i = 0; i < props.length; i++) {\n      let prop = props[i];\n\n      var p = {};\n      p[prop] = {\n        get: () => {\n          return this.__values__[prop];\n        },\n\n        set: (value) => {\n          const subscribers = this.__subscriptions__[prop];\n          const oldValue = this.__values__[prop];\n\n          this.__values__[prop] = value;\n          for (let i = 0; i < subscribers.length; i++) {\n            subscribers[i](value, oldValue);\n          }\n        },\n      };\n\n      Object.defineProperties(this, p);\n    }\n  } \n\n  toJSON() {\n    return this.__values__;\n  }\n\n  bind(element, key, fn) {\n    if (!this.__subscriptions__.hasOwnProperty(key)) {\n      console.warn(`Can not bind ${element} to ${key }: no such key exists`);\n      return;\n    }\n\n    if (!element.isConnected) {\n      console.warn(`Can not bind ${element} to ${key}: the element is not connected to the DOM`)\n      return;\n    }\n    \n    let wrapped = (newValue, oldValue) => {\n      if (!element.isConnected) {\n        this.__subscriptions__[key] = this.__subscriptions__[key].filter((s) => {\n          return s !== wrapped;\n        })\n\n        return\n      } \n\n      fn(newValue, oldValue);\n    }\n\n    this.__subscriptions__[key].push(wrapped);\n    wrapped(this.__values__[key], null);\n  }\n\n  listen(key, fn, handle) {\n    if (!this.__subscriptions__.hasOwnProperty(key)) {\n      console.log(\"Can not subscribe, no key exists\");\n      return;\n    }\n\n    if (!!handle) {\n      if (this.__handles__.hasOwnProperty(handle)) {\n        console.warn(\n          `handle ${handle} already exists, you may be trying to subscribe more than once`,\n        );\n        return;\n      }\n    } else {\n      // console.warn(\n      //   `it is highly recommended you use a handler when subscribing`,\n      // );\n    }\n\n    this.__subscriptions__[key].push(fn);\n\n    if (!!handle) {\n      this.__handles__[handle] = fn;\n    }\n  }\n\n  unlisten(key, fn, handle) {\n    if (!!handle) {\n      fn = this.__handles__[handle];\n    }\n\n    let idx = this.__subscriptions__[key].findIndex((f) => f === fn);\n\n    if (idx !== -1) {\n      this.__subscriptions__.splice(idx, 1);\n    }\n  }\n\n  createListenable(prop, value) {\n    if (this.hasOwnProperty(key)) {\n      console.warn(`Watched key ${key} already exists`);\n      return;\n    }\n\n    this.__values__[prop] = value;\n    this.__subscriptions__[prop] = [];\n\n    var p = {};\n    p[prop] = {\n      get: () => {\n        return this.__values__[prop];\n      },\n\n      set: (value) => {\n        const subscribers = this.__subscriptions__[prop];\n        const oldValue = this.__values__[prop];\n\n        this.__values__[prop] = value;\n        for (let i = 0; i < subscribers.length; i++) {\n          subscribers(value, oldValue);\n        }\n      },\n    };\n\n    Object.defineProperties(this, p);\n  }\n}\n\n//# sourceURL=webpack://sensorycontrols/./node_modules/building-blocks/index.js?");

/***/ }),

/***/ "./src/components/api-docs.html":
/*!**************************************!*\
  !*** ./src/components/api-docs.html ***!
  \**************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var building_blocks__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! building-blocks */ \"./node_modules/building-blocks/index.js\");\n\n    \n    \n    (() => {\n\n      if (typeof Component === undefined) {\n        return console.warn(\"'Component' class must be loaded before individual components\");\n      }\n\n      const name = 'api-docs';\n\n      const template = document.createElement(\"template\");\n            template.innerHTML = `\n  <div class=\"api-docs\">\n    <h2>Script API Documentation</h2>\n    <ul uk-accordion=\"multiple: true\">\n      <li class=\"uk-open\">\n        <a class=\"uk-accordion-title\" href>Intro</a>\n        <div class=\"uk-accordion-content\">\n          <p>\n            With scripts you can programmatically control almost any aspect of\n            the state of your projection while it's running using javascript.\n          </p>\n          <p>\n            You can also use either of the X/Y sliders with your scripts to\n            adjust parameters on the fly as well.\n          </p>\n          <p>\n            Below are descriptions of the variables available to your scripts,\n            the methods availble, a description of LFOs and how to use them, and\n            a detailed map of the state object.\n          </p>\n        </div>\n      </li>\n      <li>\n        <a class=\"uk-accordion-title\">Available Variables:</a>\n        <div class=\"uk-accordion-content\">\n          <ul class=\"uk-list methods\">\n            <li>\n              <label>state</label>\n              <p>\n                An object representing the current state of the system (more on\n                the state's structure\n                <a href=\"#state-structure\">below</a>)\n              </p>\n            </li>\n            <li>\n              <label>effect_x</label>\n              <p>The current value (0 to 1) of the X slider</p>\n            </li>\n            <li>\n              <label>effect_y</label>\n              <p>The current value (0 to 1) of the Y slider</p>\n            </li>\n          </ul>\n        </div>\n      </li>\n      <li>\n        <a class=\"uk-accordion-title\">Methods:</a>\n        <div class=\"uk-accordion-content\">\n          <ul class=\"uk-list methods\" uk-accordion=\"multiple: true\">\n            <li>\n              <label class=\"uk-accordion-title\">lfo(min, max, interval)</label>\n              <div class=\"uk-accordion-content\">\n                <p>\n                  Creates and returns an LFO (low frequency ossilator) object\n                  for transitioning properies over time (more\n                  <a href=\"lfos\">Below</a>)\n                </p>\n                <div class=\"example\">\n                  <pre>\n/* \n  @param {float} 'min' - the minimum value for the LFO\n  @param {float} 'max' - the maximum value for the LFO \n  @param {float} 'interval' - the period (in seconds) of the LFO\n  @returns {object} - An LFO object \n*/\n                  </pre>\n                </div>\n                <p>Example:</p>\n                <div class=\"example\">\n                  <pre>\n// creates an LFO that transitions from 0 to \n// 1 using a sine function over 5 seconds \nlet timer = lfo(0, 1, 5); let\nopacity = timer.sin();\n\n// set the first and second video opacity \nsetVideoOpacity([0, 1], opacity);\n                  </pre>\n                </div>\n              </div>\n            </li>\n            <li>\n              <label class=\"uk-accordion-title\">onBeats(beats, on, off)</label>\n              <div class=\"uk-accordion-content\">\n                <p>\n                  When a midi clock is synced, this method returns a value that\n                  will persist until the next specified beat.\n                  <a href=\"lfos\">Below</a>)\n                </p>\n                <div class=\"example\">\n                  <pre>\n/*\n@param {array[array]} 'beats' \n - An array of beats. A beat is an array with the first value\n   being a value 1-4 representing a quarter notes in a measure\n   and the second value 1-4 representing the 16th notes of the \n   quater notes. Either value can also be a wildcard'*' \n@param {function(beat, value)} 'on' \n - a callback function which is called on the specified beat. The \n   return value will be persisted until the next beat specified in \n   the 'beats' array. This function is passed two arguments, the \n   current beat, and the current value\n@param {function } 'off' \n - an optional callback function which is called on every frame \n   that does NOT fall on a specified beat. It's return value\n   will persist until the next beat. It is passed the current beat \n   (which will always be null), and the persisted value, so this\n   function can be used to modify that value. \n\n@returns {any} \n - A persisted variable\n*/                  \n                  </pre>\n                </div>\n                <p>Example:</p>\n                <div class=\"example\">\n                  <pre>\n// This function will return a value of 1 at the start of \n// each beat, and fade that by 10% on each frame\nlet pixelate_value = onBeats(\n  [[\"*\", 1]],\n  (b, v) => { return 1 },\n  (b, v) => { return v * 0.9 }\n);\n\n// sets the \"pixelate\" on video 0 to the value returned \n// by the onBeats function above\nsetValues(\"Pixelate\", 0, pixelate_value, pixelate_value);\n\n// This function return a random value on beats 2 and 4\nlet random_value = onBeats(\n  [[2, 1], [4, 1]],\n  () => { return Math.random() }\n);\n\n// This sets the 'cosine palette' effect to be fully on\n// with the color rotation changing to the random value\n// set by the above onBeats function \nsetValues(\"Cosine Palette\", 0, 1, random_value);\n                  </pre>\n                </div>\n              </div>\n            </li>\n\n            <li>\n              <label class=\"uk-accordion-title\">getSelectedVideo()</label>\n              <div class=\"uk-accordion-content\">\n                <p>Returns the currently selected video</p>\n                <div class=\"example\">\n                  <pre>\n                    <code class=\"language-js\">\n                      /*\n                      @returns {number} \n                       - The index of the selected video\n                      */\n                    </code>\n                  </pre>\n                </div>\n                <p>Example:</p>\n                <div class=\"example\">\n                  <pre>\n                    <code class=\"language-js\">\n                      // Stub\n                    </code>\n                  </pre>\n                </div>\n              </div>\n            </li>\n            <li>\n              <label class=\"uk-accordion-title\">getSelectedEffect()</label>\n              <div class=\"uk-accordion-content\">\n                <p>Returns the currently selected effect</p>\n                <div class=\"example\">\n                  <pre>\n                    <code class=\"language-js\">\n                      /*\n                      @returns {number} \n                       - The index of the selected effect\n                      */\n                    </code>\n                  </pre>\n                </div>\n                <p>Example:</p>\n                <div class=\"example\">\n                  <pre>\n                    <code class=\"language-js\">\n                      // Stub\n                    </code>\n                  </pre>\n                </div>\n              </div>\n            </li>\n            <li>\n              <label class=\"uk-accordion-title\"\n                >setVideoOpacity(videos, opacity)</label\n              >\n              <div class=\"uk-accordion-content\">\n                <p>Sets the opacity for one or more videos</p>\n\n                <div class=\"example\">\n                  <pre>\n                    <code class=\"language-js\">\n                      /*\n                      @param {number|array} 'videos' \n                       - a video index, or array of video indexes \n                      @param {float} 'opacity' \n                       - the opacity value to set\n                      */\n                    </code>\n                  </pre>\n                </div>\n                <p>Example:</p>\n                <div class=\"example\">\n                  <pre>\n                    <code class=\"language-js\">\n                      // locks all videos opacities to the \n                      // first videos \n\n                      // get the first video's opacity\n                      let opacity = getVideoOpacity(0);\n\n                      // set the rest of the video's opacities\n                      setVideoOpacity([1, 2, 3, 4, 5], opacity);\n                    </code>\n                  </pre>\n                </div>\n              </div>\n            </li>\n            <li>\n              <label class=\"uk-accordion-title\">getVideoOpacity(video)</label>\n              <div class=\"uk-accordion-content\">\n                <p>Get's a video's current opacity</p>\n                <div class=\"example\">\n                  <pre>\n                    <code class=\"language-js\">\n                      /*\n                      @param {number} 'video' \n                       - the index of the video\n                      \n                      @returns {float} \n                       - The video's current opacity\n                      */\n                    </code>\n                  </pre>\n                </div>\n                <p>Example:</p>\n                <div class=\"example\">\n                  <pre>\n                    <code class=\"language-js\">\n                      // mirror the first's video's opacity onto \n                      // the second video's\n                      \n                      let opacity = getVideoOpacity(0);\n\n                      // set the second video's opacity to be \n                      // the opposite\n                      setVideoOpacity(1, 1 - opacity);\n                    </code>\n                  </pre>\n                </div>\n              </div>\n            </li>\n            <li>\n              <label class=\"uk-accordion-title\"\n                >setShapeOpacity(shapes, videos, opacity)</label\n              >\n              <div class=\"uk-accordion-content\">\n                <p>Sets the opacity for one or more shapes</p>\n\n                <div class=\"example\">\n                  <pre>\n                    <code class=\"language-js\">\n                      /*\n                      @param {number|array} 'shapes' \n                       - a shape index, or array of shape indexes\n                      @param {number|array} 'videos'\n                       - a video index, or array of video indexes \n                      @param {float} 'opacity' \n                       - the opacity value to set\n                      */\n                    </code>\n                  </pre>\n                </div>\n                <p>Example:</p>\n                <div class=\"example\">\n                  <pre>\n                    <code class=\"language-js\">\n                      // fades in one shape at a time \n\n                      // get the number of shapes in the state\n                      let shapeCount = state.shapes.length;\n\n                      // create an LFO with the number of shape\n                      // and an interval of twice your desired\n                      // fade time\n                      let shapeSelector = lfo(\n                        0, shapeCount, shapeCount * 2\n                      );\n\n                      // the shape index will be the floor of\n                      // the shapeSelector LFO\n                      let shapeIndex = Math.floor(\n                        shapeSelector.tri()\n                      )\n\n                      // Create another LFO to fade the opacity\n                      let opacityFader = lfo(0, 1, 1);\n                      let opacity = opacityFader.sin();\n\n                      // Set the selected shape's opacity\n                      setShapeOpacity(shapeIndex, opacity);\n                    </code>\n                  </pre>\n                </div>\n              </div>\n            </li>\n            <li>\n              <label class=\"uk-accordion-title\">getShapeOpacity(shape)</label>\n              <div class=\"uk-accordion-content\">\n                <p>Get's a shape's current opacity</p>\n                <div>\n                  <pre>\n                    <code class=\"language-js\">\n                      /*\n                      @param {number} 'shape' \n                       - the index of the shape\n                      \n                      @returns {float} \n                       - The shape's current opacity\n                      */\n                    </code>\n                  </pre>\n                </div>\n                <p>Example:</p>\n                <div class=\"example\">\n                  <pre>\n                    <code class=\"language-js\">\n                      // Make every shape relative to the first\n\n                      // Get the first shape's opacity\n                      let opacity = getShapeOpacity(0, 0);\n\n                      // Get the number of shapes/steps\n                      let count = state.shapes.length\n                      let step = (1/count);\n\n                      // Loop over all the other shapes\n                      for (var i=0; i&lt;count; i++) {\n                          // add the step to the opacity;\n                          let elapsed = opacity + (step * i);\n\n                          // Calculate the new opacity\n                          let perc = (elapsed % 1) / 1;\n                          let o = (1 + Math.sin((perc * 360 * Math.PI) / 180)) * 0.5\n\n                          // Set the opacity\n                          let vidIndexes = [0];\n                          setShapeOpacity(i, vidIndexes, o);\n                      }\n                    </code>\n                  </pre>\n                </div>\n              </div>\n            </li>\n\n            <li>\n              <label class=\"uk-accordion-title\">\n                setValues(effect, videos, x, y)\n              </label>\n              <div class=\"uk-accordion-content\">\n                <p>\n                  Sets the x and y values for the given effect and videos.\n                  Setting a value to null will leave it at whatever the slider's\n                  value is.\n                </p>\n\n                <div class=\"example\">\n                  <pre>\n                    <code class=\"language-js\">\n                      /*\n                      @param {string} 'effect' \n                       - the index of the effects\n                      @param {number|array} 'videos'\n                       - the index of the videos\n                      @param {float} 'x'\n                       - the x (0-1) value\n                      @param {float} 'y'\n                       - the y (0-1) value\n                      */\n                    </code>\n                  </pre>\n                </div>\n                <p>Example:</p>\n                <div class=\"example\">\n                  <pre>\n                    <code class=\"language-js\">\n                      // Sets the zoom, and effect x prism\n                      // effect values to the opacity value\n\n                      // Loop over all the videos\n                      for (let i=0; i<6; i++) {\n                        // Get the video opacity\n                        let opacity = getVideoOpacity(i);\n\n                        \n                        // Set the zoom effect (x and y)\n                        setValues(\"Zoom\", i, opacity, opacity);\n\n                        // Set the prism x value. Using null\n                        // for y value will just default it to\n                        // the slider's value\n                        setValues(\"Prism\", i, opacity/2, null);\n                      }\n                    </code>\n                  </pre>\n                </div>\n              </div>\n            </li>\n\n            <li>\n              <label class=\"uk-accordion-title\">setTime(videos, time)</label>\n              <div class=\"uk-accordion-content\">\n                <p>Jumps one or more videos to a set time</p>\n                <div>\n                  <pre>\n                    <code class=\"language-js\">\n                      /*\n                      @param {number|array} 'videos' \n                       - a video index, or array of video indexes \n                      @param {float} 'time' \n                       - the time (in seconds) to jump to    \n                      */\n                    </code>\n                  </pre>\n                </div>\n                <p>Example:</p>\n                <div class=\"example\">\n                  <pre>\n                    <code class=\"language-js\">\n                      \n                    </code>\n                  </pre>\n                </div>\n              </div>\n            </li>\n\n            <li>\n              <label class=\"uk-accordion-title\"\n                >setLoop(videos, start, end)</label\n              >\n              <div class=\"uk-accordion-content\">\n                <p>Loops a video between the start and end time</p>\n                <div>\n                  <pre>\n                    <code class=\"language-js\">\n                      /*\n                      @param {number|array} 'videos' \n                       - a video index, or array of video indexes \n                      @param {float} 'start' \n                       - the start time (in seconds) of the loop   \n                      @param {float} 'end' \n                       - the end time (in seconds) of the loop \n                      */\n                    </code>\n                  </pre>\n                </div>\n                <p>Example:</p>\n                <div class=\"example\">\n                  <pre>\n                    <code class=\"language-js\">\n                      \n                    </code>\n                  </pre>\n                </div>\n              </div>\n            </li>\n\n            <li>\n              <label class=\"uk-accordion-title\"\n                >setPlaybackRate(videos, rate)</label\n              >\n              <div class=\"uk-accordion-content\">\n                <p>Sets the playback rate of one or more videos</p>\n                <div>\n                  <pre>\n                    <code class=\"language-js\">\n                      /*\n                      @param {number|array} 'videos' \n                       - a video index, or array of video indexes \n                      @param {float} 'rate' \n                       - the playback rate   \n                      */\n                    </code>\n                  </pre>\n                </div>\n                <p>Example:</p>\n                <div class=\"example\">\n                  <pre>\n                    <code class=\"language-js\">\n                      \n                    </code>\n                  </pre>\n                </div>\n              </div>\n            </li>\n\n            <li>\n              <label class=\"uk-accordion-title\">\n                createShape(inputs, output, opacity)\n              </label>\n              <div class=\"uk-accordion-content\">\n                <p>\n                  Adds a temporary shape with the given vertecies. If three\n                  vertecies are passed in, the shape will be a triangle. If four\n                  are passed in, it will be a quad.\n                </p>\n\n                <div class=\"example\">\n                  <pre>\n                    <code class=\"language-js\">\n                      /*\n                      @param {array[array[x,y]]} 'effects' \n                       - the index of the effects\n                      @param {array[array[x,y]]} 'videos'\n                       - the index of the videos\n                      @param {number} 'opacity'\n                       - the opacity (0-1) value\n                      \n                      @returns {object[shape]} \n                       - a shape object\n                      */\n                    </code>\n                  </pre>\n                </div>\n                <p>Example:</p>\n                <div class=\"example\">\n                  <pre>\n                    <code class=\"language-js\">\n                      /////// Script for creating shapes ///////\n\n                      // Get the count of the original shapes\n                      let shapesLen = state.shapes.length;\n\n                      // Set all the opacity on those shapes to zero\n                      for (let i = 0; i < shapesLen; i++) {\n                        let shape = state.shapes[i];\n                        for (let j = 0; j < shape.opacity.length; j++) {\n                          shape.opacity[j] = 0;\n                        }\n                      }\n\n                      // Get the ratios for dividing up the screen\n                      let wr = 1 / 16;\n                      let hr = 1 / 9;\n\n                      // loop over the x and x planes\n                      for (let y = 0; y < 1; y++) {\n                        for (let x = 0; x < 1; x++) {\n                          // get the current rectangle boundaries\n                          let w1 = wr * x;\n                          let w2 = wr * (x + 1);\n                          let w3 = (w2 - w1) / 2;\n\n                          let h1 = hr * y;\n                          let h2 = hr * (y + 1);\n\n                          let opacity = x % 2 === y % 2 ? 1 : 0;\n\n                          // Create a new shape\n                          let newShape = createShape(\n                            [\n                              [w1, h1],\n                              [w2, h1],\n                              [w3, h2],\n                            ],\n                            [\n                              [w1, h1],\n                              [w2, h1],\n                              [w3, h2],\n                            ],\n                            opacity,\n                          );\n                        }\n                      }\n                    </code>\n                  </pre>\n                </div>\n              </div>\n            </li>\n            <li>\n              <label class=\"uk-accordion-title\">\n                getValues(effect, video)\n              </label>\n              <div class=\"uk-accordion-content\">\n                <p>Gets the current values (x,y) for an effect/video</p>\n\n                <div class=\"example\">\n                  <pre>\n                    <code class=\"language-js\">\n                      /*\n                      @param {number} 'effect' \n                       - the index of the an effect\n                      @param {number} 'video'\n                       - the index of a video\n                      \n                      @returns {array[float]} \n                       - and array of the effect's current [x, y] values\n                      */\n                    </code>\n                  </pre>\n                </div>\n                <p>Example:</p>\n                <div class=\"example\">\n                  <pre>\n                    <code class=\"language-js\">\n                      \n                    </code>\n                  </pre>\n                </div>\n              </div>\n            </li>\n            <li>\n              <label class=\"uk-accordion-title\">\n                setInputPoints(shapes, fn)\n              </label>\n              <div class=\"uk-accordion-content\">\n                <p>\n                  A function to loop through in a clockwise manner the input\n                  points of the shapes in order to manipulate them\n                </p>\n\n                <div class=\"example\">\n                  <pre>\n                    <code class=\"language-js\">\n                      /*\n                      @param {integer|array} 'shapes' \n                       - the indexes of the shapes you want to manipulate\n                      @param {function} 'video'\n                       - a function that will be passed an array of vertices\n                      */\n                    </code>\n                  </pre>\n                </div>\n                <p>Example:</p>\n                <div class=\"example\">\n                  <pre>\n                    <code class=\"language-js\">\n                      // A function that rotates the first shape's \n                      // input points around it's center\n                      \n                      let xRot = lfo(0, 1, effect_x * 10).sin();\n                      let yRot = lfo(0, 1, effect_y * 10).cos();\n\n                      \n                    </code>\n                  </pre>\n                </div>\n              </div>\n            </li>\n            <li>\n              <label class=\"uk-accordion-title\">\n                setOutputPoints(shapes, fn)\n              </label>\n              <div class=\"uk-accordion-content\">\n                <p>\n                  A function to loop through in a clockwise manner the output\n                  points of the shapes in order to manipulate them\n                </p>\n\n                <div class=\"example\">\n                  <pre>\n                    <code class=\"language-js\">\n                      /*\n                      @param {integer|array} 'shapes' \n                       - the indexes of the shapes you want to manipulate\n                      @param {function} 'video'\n                       - a function that will be passed an array of vertices\n                      */\n                    </code>\n                  </pre>\n                </div>\n                <p>Example:</p>\n                <div class=\"example\">\n                  <pre>\n                    <code class=\"language-js\">\n                      // A function that rotates the first shape's \n                      // output points around it's center\n\n                      // function to get the approximate center \n                      // of a shape, by averaging it's points\n                      let getCenter = function(idx) {\n                        let shape = state.shapes[idx];\n                        let xSum = 0;\n                        let xLen = 0;\n\n                        let ySum = 0;\n                        let yLen = 0; \n                        for (let i=0; i&lt;shape.tris.length; i++) {\n                          let tri = shape.tris[i];\n\n                          for (let j=0; j&lt;tri.output.length; j++) {\n                            xSum += tri.output[j][0];\n                            ySum += tri.output[j][1];\n\n                            xLen++;\n                            yLen++;\n                          }\n                        }\n                            \n                        let avgX = xSum / xLen;\n                        let avgY = ySum / yLen;\n\n                        return [avgX, avgY]\n                      } \n\n                      // Get a saw lfo\n                      let rotator = lfo(0, 1, effect_x * 10).saw();\n                      // Get the rotation in radians                \n                      let rotation =  rotator * (Math.PI * 2);\n                      // Get the center point\n                      let center = getCenter(0);\n\n                      // Set up the function to rotate the\n                      // shape's points. The function passed\n                      // into setOutputPoints will be passed\n                      // all the vertices in the shape\n                      setOutputPoints(0, function(idx, points) {\n                        // Some math, getting the sin/cos of\n                        // the angle of rotation\n                        let c = Math.cos(rotation);\n                        let s = Math.sin(rotation);\n                        \n                        // Get the original point. Note that\n                        // for quads some vertices will have\n                        // more than one point since a quad is \n                        // made up of two triangles, but here\n                        // we only need the first, since they'll\n                        // both be the same\n                        let oX = points[0][0];\n                        let oY = points[0][1];\n                        \n                        // Just separating out the center array\n                        let cX = center[0];\n                        let cY = center[1];\n                        \n                        // Math to get the new X/Y coordinates\n                        let x = (c * (oX - cX)) + (s * (oY - cY)) + cX;\n                        let y = (c * (oY - cY)) - (s * (oX - cX)) + cY;\n                        \n                        // Loop through all the points in this \n                        // vertex and set them to the rotated \n                        // coordinates\n                        for (var i=0; i&lt;points.length; i++) {\n                          points[i][0] = x;\n                          points[i][1] = y;\n                        } \n                      });\n                    </code>\n                  </pre>\n                </div>\n              </div>\n            </li>\n          </ul>\n        </div>\n      </li>\n\n      <li id=\"lfos\">\n        <a class=\"uk-accordion-title\">LFOs:</a>\n        <div class=\"uk-accordion-content\">\n          <p>\n            LFO (Low Frequency Ossilators) are a way of transitioning properties\n            over time. For instance, smoothly fading a video in and out using a\n            sine function, or creating a rapid on/off strobe using a square\n            function.\n          </p>\n\n          <p>Under Construction</p>\n        </div>\n      </li>\n\n      <li id=\"state-structure\">\n        <a class=\"uk-accordion-title\">State Object:</a>\n        <div class=\"uk-accordion-content\">\n          <p>Below is the structure of the state object</p>\n\n          <p>Under Construction</p>\n        </div>\n      </li>\n    </ul>\n  </div>\n`;\n\n      const ElementClass = \n  class ApiDocs extends building_blocks__WEBPACK_IMPORTED_MODULE_0__.BuildingBlocks {\n    static get observedProperties() {\n      return [];\n    }\n\n    constructor() {\n      super();\n\n      this.editors = [];\n    }\n\n    connected() {\n      let blocks = this.querySelectorAll(\"pre\");\n\n      blocks.forEach((block) => {\n        let editor = ace.edit(block);\n        editor.setOptions({\n          mode: \"ace/mode/javascript\",\n          maxLines: Infinity,\n          showGutter: false,\n          readOnly: true,\n          wrap: false,\n        });\n        let originalCode = editor.getValue();\n        let trimmedCode = originalCode.trim();\n        editor.setValue(trimmedCode);\n\n        this.editors.push(editor);\n      });\n    }\n\n    disconnected() {\n      for (let i = 0; i < this.editors.length; i++) {\n        let editor = this.editors[i];\n        editor.destroy();\n        this.editors[i] = null;\n      }\n\n      this.editors = [];\n    }\n  }\n;\n\n      customElements.define(name, class extends ElementClass {\n        constructor() {\n          super();\n\n          this.__template__ = template;\n        }    \n      });      \n    })();\n\n//# sourceURL=webpack://sensorycontrols/./src/components/api-docs.html?");

/***/ }),

/***/ "./src/components/confirmation-modal.html":
/*!************************************************!*\
  !*** ./src/components/confirmation-modal.html ***!
  \************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var building_blocks__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! building-blocks */ \"./node_modules/building-blocks/index.js\");\n/* harmony import */ var _app__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../app */ \"./src/app.js\");\n/* harmony import */ var _js_storage__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../js/storage */ \"./src/js/storage.js\");\n\n    \n    \n    \n    (() => {\n\n      if (typeof Component === undefined) {\n        return console.warn(\"'Component' class must be loaded before individual components\");\n      }\n\n      const name = 'confirmation-modal';\n\n      const template = document.createElement(\"template\");\n            template.innerHTML = `\n  <div class=\"confirmation-modal\">\n    <template :if=\"!!headline\">\n      <h3>{{headline}}</h3>\n    </template>\n    <template :if=\"!!msg\">\n      <p>{{msg}}</p>\n    </template>\n    <div class=\"actions\">\n      <template :for=\"(options || [])\">\n        <div class=\"btn\" @click=\"$value.action()\">{{$value.label}}</div>\n      </template>\n    </div>\n  </div>\n`;\n\n      const ElementClass = \n  \n  \n\n  class ConfirmationModal extends building_blocks__WEBPACK_IMPORTED_MODULE_0__.BuildingBlocks {\n    constructor() {\n      super();\n    }\n\n    static get observedProperties() {\n      return [\"options\", \"msg\", \"headline\"];\n    }\n  }\n;\n\n      customElements.define(name, class extends ElementClass {\n        constructor() {\n          super();\n\n          this.__template__ = template;\n        }    \n      });      \n    })();\n\n//# sourceURL=webpack://sensorycontrols/./src/components/confirmation-modal.html?");

/***/ }),

/***/ "./src/components/effect-slot.html":
/*!*****************************************!*\
  !*** ./src/components/effect-slot.html ***!
  \*****************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var building_blocks__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! building-blocks */ \"./node_modules/building-blocks/index.js\");\n/* harmony import */ var _app__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../app */ \"./src/app.js\");\n/* harmony import */ var _js_storage__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../js/storage */ \"./src/js/storage.js\");\n\n    \n    \n    \n    (() => {\n\n      if (typeof Component === undefined) {\n        return console.warn(\"'Component' class must be loaded before individual components\");\n      }\n\n      const name = 'effect-slot';\n\n      const template = document.createElement(\"template\");\n            template.innerHTML = `\n  <div class=\"select\">\n    <div\n      href=\"#\"\n      :class=\"'select-index ' + (target?.disabled ? 'disabled' : '')\"\n      uk-tooltip=\"Toggle Disabled\"\n      @click=\"toggleDisabled($event)\"\n    >\n      {{idx + 1}}\n    </div>\n    <div class=\"select-display\">{{target?.label || \"Unassigned\"}}</div>\n    <div class=\"select-list\">\n      <ul class=\"uk-list\">\n        <li @click=\"updateSelection(null)\">Empty</li>\n        <li @click=\"updateSelection('__script')\">Script</li>\n        <template :for=\"options\" :id=\"$value.id\">\n          <li @click=\"updateSelection($value.id)\">{{$value.label}}</li>\n        </template>\n      </ul>\n    </div>\n    <div class=\"select-actions\">\n      <template :if=\"isScript(target)\">\n        <div class=\"select-handle\" @click=\"openEdit($event)\">\n          <span data-uk-icon=\"icon: pencil\"></span>\n        </div>\n      </template>\n      <div class=\"select-handle\" @click=\"toggleActive($event)\">\n        <span data-uk-icon=\"icon: triangle-down\"></span>\n      </div>\n    </div>\n  </div>\n`;\n\n      const ElementClass = \n  \n  \n\n  class SlotList extends building_blocks__WEBPACK_IMPORTED_MODULE_0__.BuildingBlocks {\n    constructor() {\n      super();\n    }\n\n    static get observedProperties() {\n      return [\"idx\", \"options\", \"slot\", \"target\"];\n    }\n\n    connected() {\n      this.options = _app__WEBPACK_IMPORTED_MODULE_1__[\"default\"].state.effects;\n      this.slot = _app__WEBPACK_IMPORTED_MODULE_1__[\"default\"].state.slots[this.idx];\n\n      this.slot.listen(\"target\", (target) => {\n        this.target = target;\n      });\n    }\n\n    isScript(target) {\n      return target?.values?.length === 2;\n    }\n\n    openEdit(e) {\n      e.preventDefault();\n      e.stopPropagation();\n      let editor = document.createElement(\"script-editor\");\n      editor.save = this.editScript.bind(this);\n      editor.code = this.slot.target.code;\n      editor.label = this.slot.target.label;\n      editor.session = true;\n\n      document.body.appendChild(editor);\n    }\n\n    editScript(label, code, id) {\n      _app__WEBPACK_IMPORTED_MODULE_1__[\"default\"].updateSlot(this.idx, { label, code });\n\n      if (!!id) {\n        _js_storage__WEBPACK_IMPORTED_MODULE_2__.storage.upsertItem(\"scripts\", { id, label, code });\n      }\n    }\n\n    toggleActive(e) {\n      e.preventDefault();\n      e.stopPropagation();\n      this.querySelector(\".select\").classList.add(\"active\");\n      window.addEventListener(\"click\", () => {\n        this.querySelector(\".select\").classList.remove(\"active\");\n      });\n    }\n\n    toggleDisabled(e) {\n      e.preventDefault();\n      e.stopPropagation();\n      _app__WEBPACK_IMPORTED_MODULE_1__[\"default\"].updateSlot(this.idx, { disabled: !this.slot.disabled });\n    }\n\n    updateSelection(id) {\n      _app__WEBPACK_IMPORTED_MODULE_1__[\"default\"].setSlot(this.idx, id);\n    }\n  }\n;\n\n      customElements.define(name, class extends ElementClass {\n        constructor() {\n          super();\n\n          this.__template__ = template;\n        }    \n      });      \n    })();\n\n//# sourceURL=webpack://sensorycontrols/./src/components/effect-slot.html?");

/***/ }),

/***/ "./src/components/effect-wheel.html":
/*!******************************************!*\
  !*** ./src/components/effect-wheel.html ***!
  \******************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var building_blocks__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! building-blocks */ \"./node_modules/building-blocks/index.js\");\n/* harmony import */ var _app_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../app.js */ \"./src/app.js\");\n\n    \n    \n    (() => {\n\n      if (typeof Component === undefined) {\n        return console.warn(\"'Component' class must be loaded before individual components\");\n      }\n\n      const name = 'effect-wheel';\n\n      const template = document.createElement(\"template\");\n            template.innerHTML = `\n  <template :for=\"range\" :id=\"range[$idx]\">\n    <effect-slot\n      :idx=\"getIndex($idx)\"\n      :class=\"selected?.slot === getIndex($idx) ? 'selected' : ''\"\n      @click=\"select('slot', getIndex($idx))\"\n    ></effect-slot>\n  </template>\n`;\n\n      const ElementClass = \n  \n\n  class EffectWheel extends building_blocks__WEBPACK_IMPORTED_MODULE_0__.BuildingBlocks {\n    constructor() {\n      super();\n    }\n\n    static get observedProperties() {\n      return [\"range\", \"order\", \"selected\"];\n    }\n\n    connected() {\n      _app_js__WEBPACK_IMPORTED_MODULE_1__[\"default\"].state.bind(this, \"selected\", (s) => {\n        this.selected = s;\n      });\n    }\n\n    getIndex(idx) {\n      if (Array.isArray(this.range) && Array.isArray(this.order)) {\n        return this.range[this.order[idx]];\n      }\n\n      return null;\n    }\n\n    select(type, idx) {\n      _app_js__WEBPACK_IMPORTED_MODULE_1__[\"default\"].updateSelected(type, idx);\n    }\n  }\n;\n\n      customElements.define(name, class extends ElementClass {\n        constructor() {\n          super();\n\n          this.__template__ = template;\n        }    \n      });      \n    })();\n\n//# sourceURL=webpack://sensorycontrols/./src/components/effect-wheel.html?");

/***/ }),

/***/ "./src/components/main-layout.html":
/*!*****************************************!*\
  !*** ./src/components/main-layout.html ***!
  \*****************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var building_blocks__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! building-blocks */ \"./node_modules/building-blocks/index.js\");\n/* harmony import */ var _app_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../app.js */ \"./src/app.js\");\n/* harmony import */ var _js_effect_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../js/effect.js */ \"./src/js/effect.js\");\n/* harmony import */ var _js_script_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../js/script.js */ \"./src/js/script.js\");\n\n    \n    \n    \n    \n    (() => {\n\n      if (typeof Component === undefined) {\n        return console.warn(\"'Component' class must be loaded before individual components\");\n      }\n\n      const name = 'main-layout';\n\n      const template = document.createElement(\"template\");\n            template.innerHTML = `\n  <header>\n    <div class=\"left-nav\">\n      <h2 class=\"fancy\">Sensory Controls</h2>\n    </div>\n    <div class=\"right-nav\">\n      <a\n        @click=\"save()\"\n        :class='saved ? \"saved\" : \"\"'\n        id=\"save_btn\"\n        href=\"javascript:void(0)\"\n        uk-icon=\"icon: check\"\n        aria-label=\"Save Project\"\n        uk-tooltip=\"Save Project\"\n      ></a>\n      <input\n        id=\"project_name\"\n        type=\"text\"\n        :value=\"name\"\n        @input=\"updateName($event)\"\n      />\n      <a\n        id=\"open_menu\"\n        uk-icon=\"icon: menu\"\n        @click=\"openMenu($event)\"\n        aria-label=\"Open Menu\"\n      ></a>\n      <main-menu></main-menu>\n    </div>\n  </header>\n  <div class=\"sidebar\">\n    <side-menu></side-menu>\n  </div>\n  <div class=\"controls uk-width-1-1\">\n    <div class=\"uk-width-1-1 uk-flex effects\">\n      <div class=\"uk-width-1-2\">\n        <effect-wheel :range=\"[0, 1, 2, 3, 4, 5]\" :order=\"[0, 1, 5, 2, 4, 3]\" />\n      </div>\n      <div class=\"uk-width-1-2\">\n        <effect-wheel\n          :range=\"[6, 7, 8, 9, 10, 11]\"\n          :order=\"[0, 1, 5, 2, 4, 3]\"\n        />\n      </div>\n    </div>\n    <div class=\"uk-width-1-1 uk-flex videos\">\n      <div class=\"videos uk-width-3-4\">\n        <div class=\"video-slots uk-child-width-1-3@s uk-grid-collapse uk-grid\">\n          <template :for=\"videos\" :id=\"$value.id\">\n            <video-slot\n              :video=\"$value\"\n              :idx=\"$idx\"\n              :class=\"selected?.video === $idx ? 'selected' : ''\"\n              @click='select(\"video\", $idx)'\n            ></video-slot>\n          </template>\n        </div>\n      </div>\n      <div class=\"sliders uk-width-1-4 uk-flex uk-flex-around\">\n        <vertical-range\n          :value=\"Array.isArray(values) ? values[0] : 0\"\n          :update=\"setValues.bind(null, 0)\"\n        ></vertical-range>\n        <vertical-range\n          :value=\"Array.isArray(values) ? values[1] : 0\"\n          :update=\"setValues.bind(null, 1)\"\n        ></vertical-range>\n      </div>\n    </div>\n  </div>\n`;\n\n      const ElementClass = \n  \n\n  \n  \n\n  class MainLayout extends building_blocks__WEBPACK_IMPORTED_MODULE_0__.BuildingBlocks {\n    constructor() {\n      super();\n    }\n\n    static get observedProperties() {\n      return [\"videos\", \"values\", \"selected\", \"saved\", \"name\"];\n    }\n\n    connected() {\n      this.values = [0, 0];\n\n      _app_js__WEBPACK_IMPORTED_MODULE_1__[\"default\"].state.bind(this, \"videos\", (v) => {\n        this.videos = v;\n      });\n\n      _app_js__WEBPACK_IMPORTED_MODULE_1__[\"default\"].bind(this, \"saved\", (s) => {\n        this.saved = s;\n      });\n\n      _app_js__WEBPACK_IMPORTED_MODULE_1__[\"default\"].bind(this, \"name\", (n) => {\n        this.name = n;\n      });\n\n      _app_js__WEBPACK_IMPORTED_MODULE_1__[\"default\"].state.bind(this, \"selected\", (s) => {\n        this.selected = s;\n\n        let { slot, video } = this.selected;\n        let { slots, effects, scripts } = _app_js__WEBPACK_IMPORTED_MODULE_1__[\"default\"].state;\n        let target = slots[slot]?.target;\n\n        if (!!target && video !== null) {\n          if (target instanceof _js_script_js__WEBPACK_IMPORTED_MODULE_3__.Script) {\n            this.values = [...target.values];\n          }\n\n          if (target instanceof _js_effect_js__WEBPACK_IMPORTED_MODULE_2__.Effect) {\n            this.values = [...target.values[video]];\n          }\n        } else {\n          this.values = [0, 0];\n        }\n      });\n\n      window.addEventListener(\"click\", (e) => {\n        this.closeMenu();\n      });\n    }\n\n    updateName(e) {\n      _app_js__WEBPACK_IMPORTED_MODULE_1__[\"default\"].name = e.target.value;\n    }\n\n    save() {\n      _app_js__WEBPACK_IMPORTED_MODULE_1__[\"default\"].saveProject();\n    }\n\n    openMenu(e) {\n      e.stopPropagation();\n\n      this.querySelector(\"main-menu\").classList.add(\"active\");\n    }\n\n    closeMenu() {\n      this.querySelector(\"main-menu\").classList.remove(\"active\");\n    }\n\n    select(type, idx) {\n      _app_js__WEBPACK_IMPORTED_MODULE_1__[\"default\"].updateSelected(type, idx);\n    }\n\n    setValues(idx, e) {\n      this.values[idx] = e.target.value;\n      this.values = [...this.values];\n\n      _app_js__WEBPACK_IMPORTED_MODULE_1__[\"default\"].setValues(idx, e.target.value);\n    }\n  }\n;\n\n      customElements.define(name, class extends ElementClass {\n        constructor() {\n          super();\n\n          this.__template__ = template;\n        }    \n      });      \n    })();\n\n//# sourceURL=webpack://sensorycontrols/./src/components/main-layout.html?");

/***/ }),

/***/ "./src/components/main-menu.html":
/*!***************************************!*\
  !*** ./src/components/main-menu.html ***!
  \***************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var building_blocks__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! building-blocks */ \"./node_modules/building-blocks/index.js\");\n/* harmony import */ var _app_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../app.js */ \"./src/app.js\");\n/* harmony import */ var _js_storage_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../js/storage.js */ \"./src/js/storage.js\");\n\n    \n    \n    \n    (() => {\n\n      if (typeof Component === undefined) {\n        return console.warn(\"'Component' class must be loaded before individual components\");\n      }\n\n      const name = 'main-menu';\n\n      const template = document.createElement(\"template\");\n            template.innerHTML = `\n  <div @click=\"stop($event)\">\n    <section>\n      <label>Saved Projects</label>\n      <a\n        @click=\"uploadProject()\"\n        uk-icon=\"icon: upload\"\n        aria-label=\"Upload Project\"\n        uk-tooltip=\"Upload Project\"\n      ></a>\n    </section>\n\n    <div class=\"obj-list\">\n      <template :if=\"projects?.length === 0\">\n        <div class=\"obj project\">\n          <div>No Projects Saved</div>\n        </div>\n      </template>\n      <template :for=\"projects\" :id=\"$value.id\">\n        <div class=\"obj project\" @click=\"loadProject($value)\">\n          <div>{{$value.name}}</div>\n          <a\n            @click=\"downloadProject($value)\"\n            uk-icon=\"icon: download\"\n            aria-label=\"Download Project\"\n            uk-tooltip=\"Download Project\"\n          ></a>\n          <a\n            href=\"javascript:void(0)\"\n            uk-icon=\"icon: trash\"\n            @click=\"deleteProject($value, $event)\"\n            aria-label=\"Delete Project\"\n            uk-tooltip=\"Delete Project\"\n          ></a>\n        </div>\n      </template>\n    </div>\n\n    <section>\n      <label>Midi Controllers</label>\n      <a\n        @click=\"refreshMidi()\"\n        uk-icon=\"icon: refresh\"\n        aria-label=\"Refresh Midi\"\n        uk-tooltip=\"Refresh Midi\"\n      ></a>\n    </section>\n    <div class=\"obj-list\">\n      <template :if=\"devices?.length === 0\">\n        <div class=\"obj project\">\n          <div>No Midi Controllers Found</div>\n        </div>\n      </template>\n    </div>\n\n    <section>\n      <label>Midi Timeclock Input</label>\n      <a\n        @click=\"refreshMidi()\"\n        uk-icon=\"icon: refresh\"\n        aria-label=\"Refresh Midi\"\n        uk-tooltip=\"Refresh Midi\"\n      ></a>\n    </section>\n    <div class=\"obj-list\">\n      <template :if=\"devices?.length === 0\">\n        <div class=\"obj project\">\n          <div>No Midi Controllers Found</div>\n        </div>\n      </template>\n    </div>\n  </div>\n`;\n\n      const ElementClass = \n  \n  \n\n  class MainMenu extends building_blocks__WEBPACK_IMPORTED_MODULE_0__.BuildingBlocks {\n    constructor() {\n      super();\n    }\n\n    static get observedProperties() {\n      return [\"projects\", \"devices\"];\n    }\n\n    connected() {\n      this.devices = [];\n\n      _js_storage_js__WEBPACK_IMPORTED_MODULE_2__.storage.bind(this, \"projects\", (projects) => {\n        this.projects = projects;\n      });\n    }\n\n    stop(evt) {\n      evt.stopPropagation();\n    }\n\n    uploadProject() {\n      console.log(\"Upload Project\");\n    }\n\n    downloadProject(project) {\n      console.log(\"Download Project:\", project);\n    }\n\n    refreshMidi() {\n      console.log(\"Refresh Midi\");\n    }\n\n    loadProject(project) {\n      _app_js__WEBPACK_IMPORTED_MODULE_1__[\"default\"].loadProject(project);\n    }\n\n    deleteProject(project, evt) {\n      evt.stopPropagation();\n      _app_js__WEBPACK_IMPORTED_MODULE_1__[\"default\"].deleteProject(project);\n    }\n  }\n;\n\n      customElements.define(name, class extends ElementClass {\n        constructor() {\n          super();\n\n          this.__template__ = template;\n        }    \n      });      \n    })();\n\n//# sourceURL=webpack://sensorycontrols/./src/components/main-menu.html?");

/***/ }),

/***/ "./src/components/output-screen.html":
/*!*******************************************!*\
  !*** ./src/components/output-screen.html ***!
  \*******************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var building_blocks__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! building-blocks */ \"./node_modules/building-blocks/index.js\");\n/* harmony import */ var _js_screen_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../js/screen.js */ \"./src/js/screen.js\");\n/* harmony import */ var _js_state_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../js/state.js */ \"./src/js/state.js\");\n\n    \n    \n    \n    (() => {\n\n      if (typeof Component === undefined) {\n        return console.warn(\"'Component' class must be loaded before individual components\");\n      }\n\n      const name = 'output-screen';\n\n      const template = document.createElement(\"template\");\n            template.innerHTML = `\n  <div>\n    <section class=\"videos\">\n      <video playsinline loop muted></video>\n      <video playsinline loop muted></video>\n      <video playsinline loop muted></video>\n      <video playsinline loop muted></video>\n      <video playsinline loop muted></video>\n      <video playsinline loop muted></video>\n    </section>\n    <section class=\"screen\">\n      <canvas :width=\"width\" :height=\"height\"></canvas>\n    </section>\n    <section class=\"ui\">\n      <canvas :width=\"width\" :height=\"height\"></canvas>\n    </section>\n  </div>\n`;\n\n      const ElementClass = \n  \n  \n\n  class OutputScreen extends building_blocks__WEBPACK_IMPORTED_MODULE_0__.BuildingBlocks {\n    constructor() {\n      super();\n\n      this.ctx = null;\n      this.screen = null;\n      this.window = null;\n\n      this.state = new _js_state_js__WEBPACK_IMPORTED_MODULE_2__.State();\n    }\n\n    static get observedProperties() {\n      return [\"width\", \"height\"];\n    }\n\n    connected() {\n      this.videos = [...this.querySelectorAll(\".videos video\")];\n      this.ctx = this.querySelector(\".screen canvas\").getContext(\"webgl\");\n      this.screen = new _js_screen_js__WEBPACK_IMPORTED_MODULE_1__[\"default\"](this.state, this.ctx, this.videos, {\n        width: this.width,\n        height: this.height,\n      });\n      this.screen.play();\n\n      this.state.listen(\"files\", (files, oldFiles) => {\n        if (!this.screen) {\n          return;\n        }\n\n        let { loading } = this.state;\n\n        if (loading !== null) {\n          let file = files[loading];\n\n          if (!!file) {\n            this.screen.loadVideo(loading, file);\n          } else {\n            this.screen.removeVideo(loading);\n          }\n        } else {\n          let newMediaIdx = files.findIndex((file, idx) => {\n            return (\n              file !== null &&\n              file.kind === \"videoinput\" &&\n              file.deviceId !== oldFiles[idx]?.deviceId\n            );\n          });\n\n          this.screen.setMedia(newMediaIdx, files[newMediaIdx].deviceId);\n        }\n      });\n\n      this.state.slots.map((s) => {\n        s.listen(\"target\", () => {\n          if (!this.screen) {\n            return;\n          }\n\n          this.screen.updateSlots();\n        });\n      });\n\n      this.state.scripts.map((s) => {\n        s.listen(\"code\", () => {\n          if (!this.screen) {\n            return;\n          }\n\n          this.screen.updateSlots();\n        });\n      });\n    }\n\n    removeVideo(idx) {\n      this.screen.removeVideo(idx);\n    }\n\n    loadVideo(idx, file) {\n      this.screen.loadVideo(idx, file);\n    }\n\n    popout() {\n      if (this.window !== null) {\n        console.log(\"Window already loaded\");\n        return;\n      }\n\n      this.screen.stop();\n      // this.screen = null;\n      this.window = window.open(\"./screen.html\");\n\n      let poll = () => {\n        if (!!this.window && this.window.closed) {\n          this.window = null;\n          this.screen = new _js_screen_js__WEBPACK_IMPORTED_MODULE_1__[\"default\"](this.state, this.ctx, this.videos, {\n            width: this.width,\n            height: this.height,\n          });\n          this.screen.play();\n        } else {\n          setTimeout(() => {\n            poll();\n          }, 100);\n        }\n      };\n\n      poll();\n    }\n  }\n;\n\n      customElements.define(name, class extends ElementClass {\n        constructor() {\n          super();\n\n          this.__template__ = template;\n        }    \n      });      \n    })();\n\n//# sourceURL=webpack://sensorycontrols/./src/components/output-screen.html?");

/***/ }),

/***/ "./src/components/script-editor.html":
/*!*******************************************!*\
  !*** ./src/components/script-editor.html ***!
  \*******************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var building_blocks__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! building-blocks */ \"./node_modules/building-blocks/index.js\");\n/* harmony import */ var _js_storage_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../js/storage.js */ \"./src/js/storage.js\");\n/* harmony import */ var _js_utils_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../js/utils.js */ \"./src/js/utils.js\");\n\n    \n    \n    \n    (() => {\n\n      if (typeof Component === undefined) {\n        return console.warn(\"'Component' class must be loaded before individual components\");\n      }\n\n      const name = 'script-editor';\n\n      const template = document.createElement(\"template\");\n            template.innerHTML = `\n  <div class=\"modal\">\n    <div class=\"container\">\n      <div class=\"header\"></div>\n      <div class=\"dialog\">\n        <div class=\"sidebar\">\n          <api-docs></api-docs>\n        </div>\n\n        <div class=\"editor\">\n          <div class=\"editor-header\">\n            <form>\n              <div class=\"input-wrapper\">\n                <label>Script Name</label>\n                <input\n                  type=\"text\"\n                  class=\"label\"\n                  :value=\"label\"\n                  @input=\"onLabel($event)\"\n                />\n              </div>\n              <div class=\"input-wrapper\">\n                <label>Template</label>\n                <select @input=\"onTemplate($event)\">\n                  <option value=\"\">No Template</option>\n                  <template :for=\"scripts\" :id=\"$value.id\">\n                    <option :value=\"$value.id\">{{$value.label}}</option>\n                  </template>\n                </select>\n              </div>\n            </form>\n            <div :class='!!error ? \"error \" : \"error hidden\"'>\n              {{(error || '').toString()}}\n            </div>\n          </div>\n          <div class=\"code-box\">\n            <div class=\"editor\"></div>\n          </div>\n        </div>\n      </div>\n      <div class=\"footer\">\n        <div class=\"fit\">\n          <template :if=\"session\">\n            <div :class=\"'btn' + (!dirty ? ' disabled' : '')\" @click=\"onSave()\">\n              Save\n            </div>\n          </template>\n\n          <div\n            :class=\"'btn' + (!dirty ? ' disabled' : '')\"\n            @click=\"onSaveTemplate($event)\"\n          >\n            Save Template\n          </div>\n        </div>\n        <div>\n          <div class=\"btn\" @click=\"onCancel()\">Close</div>\n        </div>\n      </div>\n    </div>\n    <div class=\"overlay\"></div>\n  </div>\n`;\n\n      const ElementClass = \n  \n  \n\n  class CodeEditor extends building_blocks__WEBPACK_IMPORTED_MODULE_0__.BuildingBlocks {\n    constructor() {\n      super();\n\n      this.label = \"\";\n      this.scripts = [];\n    }\n\n    static get observedProperties() {\n      return [\n        \"session\",\n        \"dirty\",\n        \"error\",\n        \"label\",\n        \"code\",\n        \"scripts\",\n        \"save\",\n        \"id\",\n      ];\n    }\n\n    connected() {\n      this.dirty = false;\n      this.editor = ace.edit(this.querySelector(\".code-box .editor\"));\n      this.editor.session.setMode(\"ace/mode/javascript\");\n      this.editor.setValue(this.code, -1);\n      this.editor.getSession().on(\"change\", () => {\n        this.dirty = true;\n      });\n\n      _js_storage_js__WEBPACK_IMPORTED_MODULE_1__.storage.bind(this, \"scripts\", (scripts) => {\n        this.scripts = scripts;\n      });\n    }\n\n    disconnected() {\n      this.editor.destroy();\n      this.editor = null;\n    }\n\n    onLabel(evt) {\n      this.label = evt.target.value;\n      this.dirty = true;\n    }\n\n    async onTemplate(evt) {\n      let script = await _js_storage_js__WEBPACK_IMPORTED_MODULE_1__.storage.getItem(\"scripts\", evt.target.value);\n\n      this.id = script.id;\n      this.label = script.label;\n      this.editor.setValue(script.code, -1);\n      this.dirty = true;\n    }\n\n    onSave() {\n      if (!this.dirty) {\n        return;\n      }\n\n      let code = this.editor.getValue();\n\n      if (!!this.save) {\n        this.save(this.label, code);\n      }\n\n      this.dirty = false;\n    }\n\n    async onSaveTemplate(e) {\n      if (!this.dirty) {\n        return;\n      }\n\n      let code = this.editor.getValue();\n      let existing = await _js_storage_js__WEBPACK_IMPORTED_MODULE_1__.storage.getItem(\"scripts\", this.id);\n\n      if (!this.id) {\n        this.id = (0,_js_utils_js__WEBPACK_IMPORTED_MODULE_2__.gen_id)();\n      }\n\n      if (!!this.save) {\n        if (existing !== null) {\n          document.querySelectorAll(\"confirmation-modal\").forEach((m) => {\n            m.remove();\n          });\n\n          let confirmation = document.createElement(\"confirmation-modal\");\n          let coords = e.target.getBoundingClientRect();\n\n          confirmation.headline =\n            \"Save over existing template, or create a new template?\";\n          confirmation.options = [\n            {\n              action: () => {\n                this.save(this.label, code, this.id);\n                confirmation.parentNode.removeChild(confirmation);\n              },\n              label: \"Save Existing\",\n            },\n            {\n              action: () => {\n                this.save(this.label, code, (0,_js_utils_js__WEBPACK_IMPORTED_MODULE_2__.gen_id)());\n                confirmation.parentNode.removeChild(confirmation);\n              },\n              label: \"Save New\",\n            },\n            {\n              action: () => {\n                confirmation.parentNode.removeChild(confirmation);\n              },\n              label: \"Cancel\",\n            },\n          ];\n\n          document.body.appendChild(confirmation);\n\n          confirmation.style.top = coords.y - 10;\n          confirmation.style.left = coords.x + coords.width + 10;\n\n          return;\n        }\n\n        this.save(this.label, code, this.id);\n      }\n\n      this.dirty = false;\n    }\n\n    onCancel() {\n      this.remove();\n    }\n  }\n;\n\n      customElements.define(name, class extends ElementClass {\n        constructor() {\n          super();\n\n          this.__template__ = template;\n        }    \n      });      \n    })();\n\n//# sourceURL=webpack://sensorycontrols/./src/components/script-editor.html?");

/***/ }),

/***/ "./src/components/side-menu.html":
/*!***************************************!*\
  !*** ./src/components/side-menu.html ***!
  \***************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var building_blocks__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! building-blocks */ \"./node_modules/building-blocks/index.js\");\n/* harmony import */ var _app_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../app.js */ \"./src/app.js\");\n/* harmony import */ var _js_storage_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../js/storage.js */ \"./src/js/storage.js\");\n/* harmony import */ var _js_utils_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../js/utils.js */ \"./src/js/utils.js\");\n\n    \n    \n    \n    \n    (() => {\n\n      if (typeof Component === undefined) {\n        return console.warn(\"'Component' class must be loaded before individual components\");\n      }\n\n      const name = 'side-menu';\n\n      const template = document.createElement(\"template\");\n            template.innerHTML = `\n  <div class=\"preview\">\n    <output-screen\n      :width=\"360\"\n      :height=\"240\"\n      :fullscreen=\"false\"\n    ></output-screen>\n    <a\n      href=\"javascript:void(0)\"\n      uk-icon=\"icon: arrow-up-right\"\n      @click=\"popout()\"\n    ></a>\n  </div>\n  <ul class=\"tabs\">\n    <li :class=\"tabClass('shapes')\" @click=\"updateTab('shapes')\">\n      <a href=\"#\">Shapes</a>\n    </li>\n    <li :class=\"tabClass('scripts')\" @click=\"updateTab('scripts')\">\n      <a href=\"#\">Scripts</a>\n    </li>\n    <li :class=\"tabClass('effects')\" @click=\"updateTab('effects')\">\n      <a href=\"#\">Effects</a>\n    </li>\n    <li :class=\"tabClass('shaders')\" @click=\"updateTab('shaders')\">\n      <a href=\"#\">Shaders </a>\n    </li>\n  </ul>\n  <ul class=\"uk-list tab-content\">\n    <template :if=\"tab === 'shapes'\">\n      <li>\n        <div class=\"uk-width-1-1 uk-flex actions\">\n          <div class=\"btn\" @click=\"addShape('quad')\">Add Quad</div>\n          <div class=\"btn\" @click=\"addShape('tri')\">Add Tri</div>\n        </div>\n        <div class=\"obj-list\">\n          <template :for=\"shapes\" :id=\"$value.id\">\n            <div class=\"obj\">\n              <div>{{$value.label}}</div>\n              <a\n                href=\"javascript:void(0)\"\n                uk-icon=\"icon: trash\"\n                @click=\"deleteShape($value)\"\n              ></a>\n            </div>\n          </template>\n        </div>\n      </li>\n    </template>\n    <template :if=\"tab === 'scripts'\">\n      <li>\n        <div class=\"uk-width-1-1 uk-flex actions\">\n          <div class=\"btn\" @click=\"openNewScript()\">Add Script</div>\n        </div>\n        <div class=\"obj-list\">\n          <template :for=\"scripts\" :id=\"$value.id\">\n            <div class=\"obj\">\n              <div>{{$value.label}}</div>\n              <a\n                href=\"javascript:void(0)\"\n                uk-icon=\"icon: pencil\"\n                @click=\"openEditScript($value)\"\n              ></a>\n              <a\n                href=\"javascript:void(0)\"\n                uk-icon=\"icon: trash\"\n                @click=\"deleteScript($value)\"\n              ></a>\n            </div>\n          </template>\n        </div>\n      </li>\n    </template>\n    <template :if=\"tab === 'effects'\">\n      <li :class=\"tabClass('effects')\">list of effects (not implemented)</li>\n    </template>\n    <template :if=\"tab === 'shaders'\">\n      <li :class=\"tabClass('shaders')\">list of shaders (not implemented)</li>\n    </template>\n  </ul>\n`;\n\n      const ElementClass = \n  \n  \n  \n\n  class SideMenu extends building_blocks__WEBPACK_IMPORTED_MODULE_0__.BuildingBlocks {\n    constructor() {\n      super();\n      this.tab = \"shapes\";\n    }\n\n    static get observedProperties() {\n      return [\"shapes\", \"scripts\", \"tab\", \"shaders\", \"effects\"];\n    }\n\n    connected() {\n      _js_storage_js__WEBPACK_IMPORTED_MODULE_2__.storage.bind(this, \"scripts\", (scripts) => {\n        this.scripts = [...scripts];\n      });\n      _app_js__WEBPACK_IMPORTED_MODULE_1__[\"default\"].state.bind(this, \"shapes\", (shapes) => {\n        this.shapes = shapes;\n      });\n    }\n\n    tabClass(tab) {\n      return tab === this.tab ? \"active\" : \"\";\n    }\n\n    updateTab(tab) {\n      this.tab = tab;\n    }\n\n    addShape(type) {\n      _app_js__WEBPACK_IMPORTED_MODULE_1__[\"default\"].state.addShape(type);\n    }\n\n    deleteShape(shape) {\n      _app_js__WEBPACK_IMPORTED_MODULE_1__[\"default\"].state.removeShape(shape);\n    }\n\n    openNewScript() {\n      let editor = document.createElement(\"script-editor\");\n      editor.save = this.editScript.bind(this);\n      editor.code = `// start typing your script here. Documentation can be found to the left`;\n      editor.label = `Script ${this.scripts.length + 1}`;\n      editor.id = (0,_js_utils_js__WEBPACK_IMPORTED_MODULE_3__.gen_id)();\n      document.body.appendChild(editor);\n    }\n\n    openEditScript(script) {\n      let editor = document.createElement(\"script-editor\");\n      editor.save = this.editScript.bind(this);\n      editor.code = script.code;\n      editor.label = script.label;\n      editor.id = script.id;\n      document.body.appendChild(editor);\n    }\n\n    editScript(label, code, id) {\n      _js_storage_js__WEBPACK_IMPORTED_MODULE_2__.storage.upsertItem(\"scripts\", { id, label, code });\n    }\n\n    deleteScript(script) {\n      _js_storage_js__WEBPACK_IMPORTED_MODULE_2__.storage.removeItem(\"scripts\", script);\n    }\n\n    popout() {\n      this.querySelector(\"output-screen\").popout();\n    }\n  }\n;\n\n      customElements.define(name, class extends ElementClass {\n        constructor() {\n          super();\n\n          this.__template__ = template;\n        }    \n      });      \n    })();\n\n//# sourceURL=webpack://sensorycontrols/./src/components/side-menu.html?");

/***/ }),

/***/ "./src/components/vertical-range.html":
/*!********************************************!*\
  !*** ./src/components/vertical-range.html ***!
  \********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var building_blocks__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! building-blocks */ \"./node_modules/building-blocks/index.js\");\n\n    \n    \n    (() => {\n\n      if (typeof Component === undefined) {\n        return console.warn(\"'Component' class must be loaded before individual components\");\n      }\n\n      const name = 'vertical-range';\n\n      const template = document.createElement(\"template\");\n            template.innerHTML = `\n\t<div class=\"range-input\">\n\t\t<input\n\t\t\ttype=\"range\"\n\t\t\tmin=\"0\"\n\t\t\tmax=\"1\"\n\t\t\tstep=\"0.01\"\n\t\t\t:value=\"value ?? 0.0\"\n\t\t\t@input=\"update($event)\"\n\t\t\t:style=\"'background: linear-gradient(to right, #ccc ' + (value * 100) + '%, #555 ' + value * 100 + '%)'\"\n\t\t/>\n\t</div>\n`;\n\n      const ElementClass = \n\tclass VerticalRange extends building_blocks__WEBPACK_IMPORTED_MODULE_0__.BuildingBlocks {\n\t\tconstructor() {\n\t\t\tsuper();\n\t\t}\n\n\t\tstatic get observedProperties() {\n\t\t\treturn [\"update\", \"value\", \"style\"];\n\t\t}\n\t}\n;\n\n      customElements.define(name, class extends ElementClass {\n        constructor() {\n          super();\n\n          this.__template__ = template;\n        }    \n      });      \n    })();\n\n//# sourceURL=webpack://sensorycontrols/./src/components/vertical-range.html?");

/***/ }),

/***/ "./src/components/video-slot.html":
/*!****************************************!*\
  !*** ./src/components/video-slot.html ***!
  \****************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var building_blocks__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! building-blocks */ \"./node_modules/building-blocks/index.js\");\n/* harmony import */ var _app_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../app.js */ \"./src/app.js\");\n\n    \n    \n    (() => {\n\n      if (typeof Component === undefined) {\n        return console.warn(\"'Component' class must be loaded before individual components\");\n      }\n\n      const name = 'video-slot';\n\n      const template = document.createElement(\"template\");\n            template.innerHTML = `\n  <div class=\"video\">\n    <div class=\"left\">\n      <div class=\"video-header uk-width-1-1 uk-flex uk-flex-middle\">\n        <div class=\"uk-width-1-2 uk-flex uk-flex-inline uk-flex-middle\">\n          <h2>{{+idx + 1}}</h2>\n          <a\n            @click=\"showMedia()\"\n            class=\"remove-video\"\n            href=\"javascript:void(0)\"\n            data-uk-icon=\"icon: video-camera\"\n            aria-label=\"Use Webcam\"\n            uk-tooltip=\"Use Webcam\"\n          ></a>\n          <a\n            @click=\"openEditShader()\"\n            class=\"edit-shader\"\n            href=\"javascript:void(0)\"\n            data-uk-icon=\"icon: pencil\"\n            aria-label=\"Edit Shader (Not Implemented)\"\n            uk-tooltip=\"Edit Shader (Not Implemented)\"\n          ></a>\n        </div>\n        <div class=\"uk-width-1-2 uk-text-right\">\n          <a\n            @click=\"onRemove()\"\n            class=\"remove-video\"\n            href=\"javascript:void(0)\"\n            data-uk-icon=\"icon: trash\"\n            aria-label=\"Clear Video\"\n            uk-tooltip=\"Clear Video\"\n          ></a\n          ><a\n            @click=\"onClear()\"\n            class=\"clear-video\"\n            href=\"javascript:void(0)\"\n            data-uk-icon=\"icon: ban\"\n            aria-label=\"Clear Effects\"\n            uk-tooltip=\"Clear Effects\"\n          ></a>\n        </div>\n      </div>\n      <div class=\"video-body\">\n        <div :class=\"'preview upload-wrapper' + (!loaded ? ' no-video' : '')\">\n          <video></video>\n          <div>{{stream?.label || \"\"}}</div>\n          <p>No Video</p>\n          <label\n            class=\"uk-label\"\n            :for=\"'video-' + video.id + '-input'\"\n            @click=\"getFile()\"\n          ></label>\n        </div>\n      </div>\n    </div>\n    <div class=\"right\">\n      <div>\n        <vertical-range\n          :update=\"updateOpacity\"\n          :value=\"opacity\"\n        ></vertical-range>\n      </div>\n    </div>\n  </div>\n`;\n\n      const ElementClass = \n  \n\n  class VideoSlot extends building_blocks__WEBPACK_IMPORTED_MODULE_0__.BuildingBlocks {\n    constructor() {\n      super();\n    }\n\n    static get observedProperties() {\n      return [\"video\", \"loaded\", \"idx\", \"opacity\", \"stream\"];\n    }\n\n    connected() {\n      this.opacity = this.video.opacity;\n\n      this.video.bind(this, \"opacity\", (o) => {\n        this.opacity = o;\n      });\n\n      this.video.bind(this, \"loaded\", (l) => {\n        console.log(l);\n        if (!this.loaded && l) {\n          this.setPreview();\n        }\n\n        this.loaded = l;\n      });\n    }\n\n    setPreview() {\n      let file = _app_js__WEBPACK_IMPORTED_MODULE_1__[\"default\"].state.files[this.idx];\n      if (file instanceof File) {\n        this.querySelector(\"video\").src = URL.createObjectURL(file);\n        this.querySelector(\"video\").currentTime = 50;\n        this.querySelector(\".preview\").classList.remove(\"no-video\");\n      }\n    }\n\n    onRemove() {\n      this.loaded = false;\n      this.stream = null;\n\n      this.querySelector(\"video\").src = \"\";\n      this.querySelector(\".preview\").classList.add(\"no-video\");\n\n      _app_js__WEBPACK_IMPORTED_MODULE_1__[\"default\"].removeVideo(this.idx);\n    }\n\n    async getFile() {\n      if (this.loaded) {\n        return;\n      }\n\n      const [fileHandle] = await window.showOpenFilePicker();\n      const file = await fileHandle.getFile();\n\n      this.querySelector(\"video\").src = URL.createObjectURL(file);\n\n      this.querySelector(\"video\").currentTime = 50;\n      this.querySelector(\".preview\").classList.remove(\"no-video\");\n      // this.querySelector(\"input[type='file']\").disabled = true;\n\n      _app_js__WEBPACK_IMPORTED_MODULE_1__[\"default\"].loadVideo(this.idx, file, fileHandle);\n    }\n\n    async showMedia() {\n      let media = [];\n      try {\n        media = await navigator.mediaDevices\n          .enumerateDevices()\n          .then((devices) => {\n            return [...devices].filter((d) => {\n              return d.kind === \"videoinput\";\n            });\n          });\n      } catch (e) {\n        console.log(\"Couldn't access media devices\");\n      }\n\n      let container = document.createElement(\"ul\");\n      container.className = \"media-list\";\n      container.addEventListener(\"click\", (e) => {\n        e.preventDefault();\n        e.stopPropagation();\n      });\n\n      [...media].map((source) => {\n        let item = document.createElement(\"li\");\n        item.innerHTML = source.label;\n        item.addEventListener(\"click\", () => {\n          container.parentNode.removeChild(container);\n          _app_js__WEBPACK_IMPORTED_MODULE_1__[\"default\"].updateVideoMedia(this.idx, source);\n\n          this.querySelector(\".preview\").classList.remove(\"no-video\");\n          // this.querySelector(\"input[type='file']\").disabled = true;\n\n          this.stream = source;\n        });\n\n        container.appendChild(item);\n      });\n\n      this.appendChild(container);\n\n      let remove = () => {\n        if (!!container.parentNode) {\n          container.parentNode.removeChild(container);\n        }\n\n        window.removeEventListener(\"click\", remove);\n      };\n      window.addEventListener(\"click\", remove);\n    }\n\n    updateOpacity(e) {\n      _app_js__WEBPACK_IMPORTED_MODULE_1__[\"default\"].updateOpacity(this.idx, e.target.value);\n\n      this.opacity = e.target.value;\n    }\n\n    openEditShader() {}\n\n    onClear() {\n      _app_js__WEBPACK_IMPORTED_MODULE_1__[\"default\"].clearValues(this.idx);\n    }\n  }\n;\n\n      customElements.define(name, class extends ElementClass {\n        constructor() {\n          super();\n\n          this.__template__ = template;\n        }    \n      });      \n    })();\n\n//# sourceURL=webpack://sensorycontrols/./src/components/video-slot.html?");

/***/ })

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId](module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/define property getters */
/******/ 	(() => {
/******/ 		// define getter functions for harmony exports
/******/ 		__webpack_require__.d = (exports, definition) => {
/******/ 			for(var key in definition) {
/******/ 				if(__webpack_require__.o(definition, key) && !__webpack_require__.o(exports, key)) {
/******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
/******/ 				}
/******/ 			}
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	(() => {
/******/ 		__webpack_require__.o = (obj, prop) => (Object.prototype.hasOwnProperty.call(obj, prop))
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	(() => {
/******/ 		// define __esModule on exports
/******/ 		__webpack_require__.r = (exports) => {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	})();
/******/ 	
/************************************************************************/
/******/ 	
/******/ 	// startup
/******/ 	// Load entry module and return exports
/******/ 	// This entry module can't be inlined because the eval devtool is used.
/******/ 	var __webpack_exports__ = __webpack_require__("./src/main.js");
/******/ 	
/******/ })()
;